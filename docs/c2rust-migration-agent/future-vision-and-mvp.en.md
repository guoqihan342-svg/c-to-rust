# C-to-Rust Future Vision and MVP Roadmap

This document is the English mirror of `future-vision-and-mvp.md`. The Chinese document is the canonical backlog and the only entrypoint for current status, execution order, and capability boundaries. Detailed implementation history remains in Git, the coverage matrix, and machine-readable evidence instead of being duplicated here.

Last updated: 2026-07-16.

## 1. Current Status

The goal is an auditable end-to-end pipeline:

```text
input.c + compile context
  -> ContextPack (source / headers / flags / ABI / diagnostics)
  -> GLM-5.1 primary candidate
  -> typed IR / C2Rust alternate candidates
  -> bounded verify-and-repair loop
  -> C oracle + Rust replay + diff + negative diff
  -> unsafe ledger + final verification
  -> accepted / refused / blocked
```

| Item | Current value | Exact meaning |
| --- | ---: | --- |
| `translator_generated_semantic_pass_count` | 37 | Coverage-ledger-derived count; it does not mean the current strict full regression is green or that whole-project translation is complete |
| `accepted_evidence_semantic_pass_count` | 2 | Accepted-evidence-ledger-derived count; both remain separate from the translator numerator and do not make generated candidates semantic passes |
| Current AI candidate state | `GLM 0 / fixed auxiliary 6/12 / libuv AI-first 0/1 exact` | Competition GLM still has no candidate because balance is unavailable, and the fixed 12 cases have not been rerun as a full suite after A18c8a. The fresh libuv DeepSeek auxiliary run completes one initial call plus three repairs, is provider-ready, but does not pass exact gates and is competition-ineligible |
| Active translator track | P0-A19 / P0-A10 / P0-A18c | h5a-h5d close compiler, runtime, and ABI repair facts plus compiler-header declaration-only classification. Next move to unfamiliar-repository whole-project discovery, decomposition, parallel candidates, Cargo integration, and project-level repair instead of extending syntax around one known slice |
| External parallel track | P0-H9 | Revalidate the exact OpenCode + GLM-5.1 contract on the real competition host |
| Latest development stage | P0-A19 BuildIR/frontier/verifier convergence | Arbitrary-repository inventory, SCC/DAGs, explicit sandboxed Make collection, actual C-toolchain syntax witnesses, paged ContextPacks, isolated OpenCode, SQLite state, RustProjectIR/Cargo generations, whole-cohort Cargo classification, raw-byte CAS, fixed Cargo linker traces, actual ELF/ar reopening, ABI/order/symbol gates, and project-level native settlement are implemented. A producer for toolchain-provided external-link argument resolutions, valid interface-completeness transitions, runtime AI symbol candidates, an independent verifier process, final CompletionCoordinator wiring, complete frontier-lazy retrieval, and real held-out semantic acceptance remain open. Volatile test totals belong only to commit-bound stage evidence, not this status row |
| Latest recorded strict-evidence snapshot | `25/33` | Immutable historical run `20260711T-finite-p0-t31`; `stress_loops=0`, and eight evidence drifts remain in that snapshot. Close P0-C1 through a new replacement run rather than rewriting this history |
| Current proof class | `wsl-local-simulation` | Valid for development and approximation, but not `competition-exact` |

`validation/translator-coverage-matrix.json` is the machine-readable source of truth for capability counts. A native C build, typed-IR unit test, rustc compile, C2Rust output, or LLM output alone is candidate evidence only.

## 2. Competition Environment

### 2.1 Official Target Baseline

The machine-readable source of truth is `config/competition-env/environment.json`, with profile id `huawei-competition-ubuntu-24.04` and current raw-bytes SHA-256 `8f2233e9c8c4e7676524a72bf8db919b83e89366ec3cc76d3673d07f32488160`. Development, CI, and WSL may simulate this profile, but only an attested real host may claim `competition-exact`.

| Item | Competition target |
| --- | --- |
| OS | Ubuntu `24.04.4 LTS`, codename `noble` |
| Kernel | `5.10.0-182.0.0.95.r194_123.hce2.x86_64` |
| Architecture | `x86_64` |
| Python / pip | `3.12.3` / `24.0` |
| Rust / Cargo | `1.96.0` / `1.96.0` |
| gcc / g++ / Make | `13.3.0` / `13.3.0` / `4.3` |
| Node.js / npm | `v24.13.0` / `11.6.2` |
| Java / Maven | Bisheng OpenJDK `21.0.10` / Maven `3.9.11` |
| `MAVEN_HOME` | `/usr/local/maven3` |
| Go / CMake | Unavailable: Go is not installed and CMake is not found |
| clang | Not required by default; explicitly required by the typed-IR competition clang lane |

The profile fixes these mirrors:

| Ecosystem | Mirror |
| --- | --- |
| APT | `http://mirrors.tools.huawei.com/ubuntu` |
| PyPI | `https://mirrors.tools.huawei.com/pypi/simple` |
| npm | `https://mirrors.tools.huawei.com/npm/` |
| Cargo | `sparse+http://rust.inhuawei.com/crates.io-index/` |

`source config/competition-env/env.sh` sets the profile id, mirrors, `MAVEN_HOME`, and repo-local `CARGO_HOME=config/competition-env/cargo`, avoiding dependence on a user's global Cargo configuration.

### 2.2 Competition Source, clang, and OpenCode Contracts

The FlashDB competition input is fixed to:

| Field | Fixed value |
| --- | --- |
| repository | `https://gitcode.com/xwxf/FlashDB.git` |
| branch | `competition` |
| commit | `f9d0421315c564fb890a1b14eee77b290e0d7bbe` |
| checkout | `git checkout -B competition f9d0421315c564fb890a1b14eee77b290e0d7bbe` |

Every new competition-targeted extraction must provide repository, branch, and `--require-source-commit`, and verify the checkout before generating a slice spec. Historical evidence may bind older commits, but it cannot represent new competition input.

The competition clang lane is not a default dependency. With `auto_migrate.py --competition-clang-lane`, clang must come from `CLANG_PATH` or one of these repo-local paths:

```text
tools/llvm/bin/clang-18
tools/llvm/bin/clang
tools/clang/bin/clang
```

Availability requires both `clang -print-resource-dir` and a minimum JSON AST dump including `stdint.h` and `stddef.h`. A successful `clang --version` is insufficient. `LIBCLANG_PATH` is not an input to this lane. Missing clang must produce `missing_clang_path`, not a fallback presented as typed-IR clang execution.

The competition agent contract is fixed:

| Field | Requirement |
| --- | --- |
| command | `opencode` |
| logical model | `GLM-5.1` |
| repo-owned agent | `c2rust-migrator` |
| variant | `max` |
| retry cap | At most five rounds, one minimal patch per round |
| preflight | `opencode models` in the same run/runtime environment must list exact GLM-5.1 and produce a hash-bound passed report/marker |

A provider-qualified id may be the resolved model id, but the logical model, probe output, preflight, worker argv, resume data, judge bundle, and public packet must agree. Current WSL `opencode models` prints lowercase `zai/glm-5.1`; the repository's strict probe requires a case-sensitive final token exactly equal to `GLM-5.1`, so `toolchain-check.sh` still reports "GLM-5.1 not listed." This shows that the provider exposes the corresponding model, but it does not pass the current competition OpenCode contract. OpenCode conversation text is not semantic evidence either.

### 2.3 Current WSL Versus the Competition Target

The current WSL probe on 2026-07-11 reports:

| Item | Current WSL | Competition target | Result |
| --- | --- | --- | --- |
| OS | Ubuntu `24.04.3 LTS` | `24.04.4 LTS` | Minor version differs |
| Kernel | `6.6.87.2-microsoft-standard-WSL2` | Huawei HCE `5.10.0-...` | Different; not equivalent |
| Architecture | `x86_64` | `x86_64` | Match |
| Python | `3.12.3` | `3.12.3` | Match |
| Rust / Cargo | `1.95.0` / `1.95.0` | `1.96.0` / `1.96.0` | Differ |
| gcc / g++ / Make | `13.3.0` / `13.3.0` / `4.3` | Same | Match |
| Node.js / npm | `v22.22.0` / `10.9.4` | `v24.13.0` / `11.6.2` | Differ |
| Java / Maven | Ubuntu OpenJDK `17.0.19` / Maven `3.8.7` | Bisheng OpenJDK `21.0.10` / Maven `3.9.11` | Differ |
| Go / CMake | Go absent / CMake `3.28.3` | Both unavailable | CMake does not match |
| OpenCode | `1.17.18`, models prints `zai/glm-5.1` | Binary version not pinned; strict GLM-5.1 launch contract | Strict model probe fails |
| clang | System `/usr/bin/clang` `18.1.3` | Optional, with explicit source and complete smoke | Present but not auto-selected by `env.sh` |

Running `source config/competition-env/env.sh && bash config/competition-env/toolchain-check.sh` in current WSL returns exit code 1 with ten mismatches. System clang exists, but `env.sh` auto-selects only `CLANG_PATH` or a repo-local vendored clang. With explicit `export CLANG_PATH=/usr/bin/clang`, the observed resource dir is `/usr/lib/llvm-18/lib/clang/18` and the minimum-TU JSON AST smoke passes, but the other ten environment differences still fail the overall check.

Current WSL results therefore remain `wsl-local-simulation`. They can verify Linux paths, gcc/explicit clang, the Python harness, Rust replay, FlashDB build/smoke, and most of the evidence chain. They cannot prove equivalence for the target kernel, Rust 1.96, Node 24, Java/Maven, the no-CMake baseline, the strict OpenCode/GLM host, or competition resource limits.

### 2.4 Self-Check, Simulation, and Exact Run Entrypoints

Start a competition-host or Linux/WSL session with:

```bash
source config/competition-env/env.sh
bash config/competition-env/toolchain-check.sh
```

`toolchain-check.sh` checks tool versions, mirrors, Go/CMake absence, and OpenCode model status. When clang is found, it also runs resource-dir and minimum-TU AST smoke. Missing OpenCode/GLM is informational by default; enable the competition-agent hard gate with:

```bash
REQUIRE_OPENCODE_GLM=1 bash config/competition-env/toolchain-check.sh
```

Run the current WSL simulation smoke with:

```bash
bash config/competition-env/smoke.sh \
  wsl-local-simulation \
  target/competition-smoke-wsl
```

This smoke verifies environment and lightweight evidence gates only. It does not translate a new slice or create semantic acceptance.

Run every judge entrypoint on the real competition host with:

```bash
export COMPETITION_EXACT_HOST=1
python3 -B -m validation.tools.run_judge_entrypoints \
  --config config/competition-env/judge-entrypoints/flashdb-harness.json \
  --proof-class competition-exact
```

Public judge before/after entrypoints:

```bash
python3 -B -m validation.tools.judge_demo --profile config/competition-env/planned-batches/flashdb-fdb-utils-before-after.json --run-id competition-flashdb-before-after-exhibit --out-root target/competition-out-flashdb-before-after-exhibit --review-checklist config/competition-env/review-checklists/flashdb-harness-internal-review.json
python3 -B -m validation.tools.opencode_agent_harness run-batch-profile --profile config/competition-env/planned-batches/flashdb-fdb-utils-before-after.json --run-id competition-flashdb-before-after-exhibit --out-root target/competition-out-flashdb-before-after-exhibit
python3 -B validation/tools/milestone_release_report.py --competition-summary target/competition-out-flashdb-before-after-exhibit/summary/competition-run-summary.json --batch-profile-report target/competition-out-flashdb-before-after-exhibit/harness/batch-profile-report.json --review-checklist target/competition-out-flashdb-before-after-exhibit/summary/milestone-review-checklist.json --output target/competition-out-flashdb-before-after-exhibit/summary/milestone-release-report.json
```

The public FlashDB artifacts are `target/competition-out-flashdb-before-after-exhibit/summary/before-after-exhibit.json`, `target/competition-out-flashdb-before-after-exhibit/summary/judge-demo-report.json`, and `target/competition-out-flashdb-before-after-exhibit/summary/milestone-review-checklist.json`. The fallback entrypoint and artifacts are:

```bash
python3 -B -m validation.tools.judge_demo --profile config/competition-env/planned-batches/demo-store-add-one-before-after.json --run-id competition-demo-before-after-exhibit --out-root target/competition-out-demo-before-after-exhibit
```

`target/competition-out-demo-before-after-exhibit/summary/before-after-exhibit.json` and `target/competition-out-demo-before-after-exhibit/summary/judge-demo-report.json` are hash-bound exhibits only. `judge-demo-report.json.repair_summary` aggregates bound repair facts; it does not replace the validator/oracle or increase `translation_coverage_numerator`.

After execution, deeply validate local artifacts:

```bash
python3 -B -m validation.tools.validate_judge_entrypoints \
  --config config/competition-env/judge-entrypoints/flashdb-harness.json \
  --require-local-artifacts
```

Use `--dry-run` for plan inspection and `--entrypoint-id` for focused debugging. Neither focused mode replaces the full competition-exact release packet.

### 2.5 Currently Proven and Unproven Boundaries

The WSL competition-like lane currently proves the P0-T20 C oracle, generated Rust replay, diff, negative mutation, unsafe gates, and strict validator. Routine full regression runs no loop stress step, and loop stress is no longer a normal acceptance condition. The runner is capped at three repeated rounds, separately approved diagnostic stress is capped at 100 loops, and 1,000/10,000-iteration runs are rejected. Volatile test counts are recorded only in section 5 with a run or commit binding.

Still unproven are the target kernel, Rust/Cargo 1.96, target Node/npm, real Huawei host package/runtime differences, competition resource limits, real-host `COMPETITION_EXACT_HOST=1` attestation, a complete OpenCode preflight marker, GLM-5.1 worker/session artifacts, and the full competition-exact judge bundle/public packet. P0-H9 therefore remains open.

## 3. Active Execution Queue

### 3.1 Current Priority Board

Execute strictly from top to bottom. A successful lower-level sample cannot close a higher-level item.

| Order | Work item | State | Completion criterion for this stage |
| ---: | --- | --- | --- |
| 1 | P0-A18c7 cross-project AI-input and evidence closure | Complete | The 125-test gate passes on Windows and WSL; all 12 fixed cases generate candidates, contract failures are zero, and six pass exact gates |
| 2 | P0-A19 unfamiliar-C-project whole-project AI harness | In progress: AI behavioral context and runtime stability | Repository discovery, paged DAGs, role portfolios, SQLite state, Cargo generations, actual C toolchain binding, CMake/Ninja/read-only Meson/link/archive facts, and fail-closed sandbox contracts exist. The current priority is same-unit multi-candidate selection, automatic project knowledge, shared budgets/convergence windows, and independent verifiers. Completed test contracts, repair-channel signatures, and concurrent worker batches improve first-pass information and runtime stability but do not replace real held-out build/oracle acceptance. Project/function/path/fixture identity dispatch is forbidden |
| 3 | P0-A18c8 remaining exact-failure closure | In progress | h5a-h5d are complete without expanding semantic coverage. Keep the parent boundary open and revisit it only when P0-A19 project-level gates expose a real blocker instead of adding sequential rules around one known slice |
| 4 | P0-A10 finite held-out cross-project acceptance | Pending | Keep the fixed 12 cases as a regression baseline and add finite whole-project acceptance that did not participate in rule development. Run the finite set once and never inflate success through repeated rounds |
| 5 | P0-H9 competition-host verification | Externally blocked | Real-host attestation, OpenCode preflight, GLM-5.1 session, and release packet all close |
| 6 | P0-C stage closure | Pending | Repair historical evidence drift and clear the remaining all-feature Clippy items |

Checkbox rule: implementation closes only its subitem. A parent closes only after every acceptance gate and evidence binding under it closes. A targeted sample, AI prose, candidate compilation, WSL simulation, or auxiliary-model success cannot independently close P0-A18c, P0-A19, P0-A10, or P0-H9.

### 3.2 P0-T: Deterministic Translation Support Lane

- [x] **P0-T21: compose the `:1868-:1874` zero-start and next-address branches**

  Put the `sector.addr + SECTOR_HDR_DATA_SIZE` write for `kv->addr.start == 0` in the same generic carrier as the P0-T20 assignment-call else-if, reset, traversed-length add, and `continue` path.

  The discriminating set is fixed at five cases: zero-start non-wrap, zero-start wrap, sentinel hit, zero-return miss, and ordinary miss. Zero-start must skip the external call and must not produce the P0-T20 `continue` observation.

  Preserve the owner interior alias, local sector copy, exact `db -> itr` noalias relation, mutually exclusive branches, exact-u32/wrapping behavior, and current-level `continue`.

  Explicitly out of scope: real `SECTOR_HDR_DATA_SIZE` expansion, real `get_next_kv_addr` semantics, the `read_kv` loop from line 1875, complete `fdb_kv_iterate`, whole FlashDB, and record layout/ABI.

  | Stage | Status | Completion criterion |
  | --- | --- | --- |
  | source boundary | Complete | Bind exactly `fdb_kvdb.c:1868-1874` without extending into line 1875 |
  | generic implementation | Complete | Commit the project-independent typed-IR carrier and schema-v2 reporter |
  | generic tests | Complete | Positive, adjacent fail-closed, and all-feature translator tests pass |
  | source-backed inputs | Complete | Spec, fixture, and source-fragment hash are reproducible |
  | semantic evidence | Complete | C oracle, Rust replay, diff, negative, and unsafe ledger are generated and cross-bound |
  | strict acceptance | Complete | Every section 7 gate passes and the matrix-derived count is 34 |

- [x] **P0-T22: next-slice decision gate**

  - Slice id: `real-fdb-kv-iterate-kv-tail`.
  - Real source span: `src/fdb_kvdb.c:1885` at the pinned FlashDB commit, limited to the empty-body do-while tail assignment-call.
  - Construct gap: a mutable alias derived from an owner interior projection is both the nested-u32 assignment target and the unique same-alias argument of the same direct call.
  - Nearest negative: `typed_ir_rejects_assignment_call_sibling_read_when_owner_is_mutably_borrowed`; same-owner sibling reads remain refused.
  - Stop condition: generate a generic candidate only. Lines 1876-1884, real `get_next_kv_addr`, the complete function, ABI, and the FlashDB project remain out of scope. Fail closed if function-name matching or weaker noalias evidence would be required.

  Project-independent no-clang AST lowering, Rust emission/runtime, and non-empty-body/comparison-drift negatives are complete. Status remains `candidate_context_only`, and the semantic count remains 33.

- [x] **P0-T23: source-backed semantic closure for `fdb_kvdb.c:1885`**

  The source-bound spec, fixture, C oracle, Rust replay, schema diff, negative diff, unsafe ledger, route/profile, and final verification are complete. All 12 strict semantic-binding checks pass under the WSL `--competition-clang-lane`, with `semantic_pass=true` and `generated_draft_semantic_pass=true`. The claim remains fixed to line 1885 and excludes the real `get_next_kv_addr` and whole function. The matrix-derived count moved from 33 to 34.

- [x] **P0-T24: next-slice decision gate**

  - Slice id: `real-fdb-kv-iterate-read-kv-body-call`.
  - Real source span: `src/fdb_kvdb.c:1876` at the pinned FlashDB commit; normalized-line SHA-256 is `10948836c8ce66dea9de8d52dd04882b110c70acdabd785634c9e6d2660f2940`.
  - Construct gap: exactly one ordered direct-call statement with a discarded return value in the do-while body of an owner interior alias, followed by the existing assignment-call tail.
  - Generic implementation: accept only `[Expr(direct Call), normalized tail assignment-call]`; the body-call arguments must be the proven-noalias independent call root and the same interior alias. The legacy empty-body shape remains accepted.
  - Fail closed: a second body statement, non-call expression, nested call, extra or repeated mutable root, owner sibling read, and comparison drift remain rejected.
  - Stop boundary: lines 1877-1884, real `read_kv` semantics, callee side effects, the whole loop, and FlashDB ABI remain excluded. Project names, function names, slice ids, and fixture constants must not drive behavior.

  Project-independent no-clang AST lowering, emitted-Rust runtime order checks, and adjacent negatives are complete. Status is `candidate_context_only`; the semantic count remains 34.

- [x] **P0-T25: source-backed semantic closure for `fdb_kvdb.c:1876`**

  The source-bound spec, three bounded fixtures, fixture-only `read_kv`/`get_next_kv_addr` dual-call contract, C oracle, generated Rust replay, schema diff, body-call-suppression negative diff, unsafe ledger, route/profile, and final verification are complete. All 12 strict binding checks pass in the WSL competition clang lane with `semantic_pass=true` and `generated_draft_semantic_pass=true`, moving the matrix-derived count from 34 to 35. The claim covers only the line-1876 call shape, argument forwarding, and fixture order; line 1885 is synthetic scaffold, and real callee side effects and whole-loop semantics remain excluded.

- [x] **P0-T26: next-slice decision gate for `fdb_kvdb.c:1877-1883`**

  - The selected construct is line 1880, `itr->iterated_cnt++`: a statement-position postfix increment whose value is discarded and whose target is a u32 field on a direct mutable record-pointer root.
  - A focused no-clang AST fixture proves normalization to field assignment plus unsigned add. The Rust candidate uses `wrapping_add(1u32)` and runs `0 -> 1`, an ordinary value, and `u32::MAX -> 0`.
  - Existing fail-closed boundaries remain pinned for const record pointers, nested/non-direct bases, and missing mutable ownership evidence.
  - The stop boundary excludes lines 1876-1879 and 1881-1885, real `read_kv` side effects, the condition and early return, owner-interior-alias composition, the whole loop, and FlashDB ABI. Production behavior must not depend on project, function, slice, field, or fixture names.

  This stage is project-independent candidate generation only. Status remains `candidate_context_only`, and the semantic count remains 35.

- [x] **P0-T27: source-backed semantic closure for `fdb_kvdb.c:1880`**

  The line-1880 source span and normalized hash, three bounded fixtures, C oracle, generated Rust replay, schema diff, `wrapping_add` to `wrapping_sub` negative mutation, unsafe ledger, route/profile, and final verification are complete. All 12 strict binding checks pass in the WSL competition clang lane with `semantic_pass=true` and `generated_draft_semantic_pass=true`, moving the matrix-derived count from 35 to 36. The claim covers only a direct u32 field postfix increment through one mutable record root over zero, ordinary, and wrap cases; the enclosing branch/loop, whole function, layout, ABI, and FlashDB project remain excluded.

- [x] **P0-T28: generic owner-interior sibling accumulation candidate for `fdb_kvdb.c:1881`**

  - The selected construct is line 1881, `itr->iterated_obj_bytes += kv->len`. Because `kv` comes from `&itr->curr_kv`, this is one mutable owner with a scoped interior alias read and a non-overlapping sibling write, not two noalias pointer roots.
  - Generic frontend/typed-IR generation requires an explicit LP64 target ABI, a u32 source field, a `size_t`/usize target field, pointer-typedef alias provenance, non-overlapping paths, and no additional alias use or side effect before it emits `wrapping_add(kv.len as usize)`.
  - A renamed no-clang AST fixture and six focused tests pin ordinary, zero, and LP64 wrap cases plus fail-closed behavior for missing ABI, overlapping paths, a second pointer hop, alias reuse, type drift, fictional noalias, and terminal-statement drift.

  This stage is project-independent candidate generation only. Production behavior does not depend on project, function, field, slice, or fixture names, and the semantic count remains 36.

- [x] **P0-T29: source-backed semantic closure for `fdb_kvdb.c:1881`**

  The exact line-1881 source span, pointer-typedef carrier, and LP64 ABI are bound to three finite fixtures covering ordinary, zero-addend, and usize-wrap cases. The actually compiled and executed C oracle, generated Rust replay, schema diff, negative mutation, unsafe ledger, route/profile, and final verification all pass. The WSL competition clang lane reports `schema_status=passed`, `semantic_pass=true`, and `generated_draft_semantic_pass=true` across all 12 strict binding classes, moving the matrix-derived count from 36 to 37. The claim covers only one owner, one scoped interior alias, and one u32-to-usize sibling wrapping add at line 1881; lines 1882-1883, the enclosing branch/loop, complete function, real layout/whole ABI, and the FlashDB project remain excluded.

- [x] **P0-T30: generic composition candidate for `fdb_kvdb.c:1882-1883` and `:1880-1883`**

  A project-independent bounded ordered sequence now supports one direct u32 wrapping increment, two distinct u32 fields read through one scoped interior alias and added into distinct LP64 usize sibling fields, and a fixed `return true`. Production analysis checks only AST, type, ABI, alias, and effect constraints; it does not read project, function, field, slice, or fixture names. A renamed no-clang AST, runtime positives, and adjacent shape/alias/type/effect negatives pass. This remains candidate-only, so the count stays 37.

- [x] **P0-T31: source-backed semantic closure for `fdb_kvdb.c:1880-1883`**

  The exact source span, single-owner/interior-alias carrier, and LP64 ABI are bound to four finite fixtures. The compiled C oracle, generated Rust replay, schema diff, negative mutation, unsafe ledger, route/profile, and final verification pass. The WSL competition clang lane reports `schema_status=passed`, `semantic_pass=true`, and `generated_draft_semantic_pass=true` across all 12 strict binding classes, moving the matrix-derived count from 37 to 38. Line 1877, the enclosing branch/loop, complete function, real layout/whole ABI, and whole-project FlashDB translation remain excluded.

- [ ] **P0-T32: compose the `fdb_kvdb.c:1877` condition with the `:1880-1883` body**

  Pause this as the active track. Keep it as a real golden case for comparing GLM-5.1, typed IR, and C2Rust candidates. Increase the numerator only after the common gates pass, and do not recount the accepted stats body.

### 3.3 P0-A: AI-first Harness Track

Close work in executable dependency order so external model or checkout blockers do not stall local development:

| Order | Work | Current stop condition |
| ---: | --- | --- |
| 1 | P0-A11 provider admission and zero-call evidence | Complete; only regression fixes remain |
| 2 | P0-A12 file-based prompt transport | Complete; only transport/CLI compatibility regressions remain |
| 3 | P0-A13 content-addressed AI candidate cache | Complete; only cache binding/schema/validator regressions remain |
| 4 | P0-A14 bounded compile response-file expansion | Complete; only dialect/schema/binding regressions remain |
| 5 | P0-A15 recomputable AI caller/callee context | Complete; only ContextPack/prompt-scope/validator regressions remain |
| 6 | P0-A16 transparent auxiliary-model validation | Complete; auxiliary models can produce local evaluation evidence only and cannot impersonate GLM |
| 7 | P0-A17 isolated auxiliary suite and strict input binding | Complete; DeepSeek and other substitutes produce only separate auxiliary reports |
| 8 | P0-A19 unfamiliar-project whole-project harness | Active local track; close BuildIR/toolchain, frontier retrieval, single state authority, and independent verifier capabilities before expanding project-level AI reasoning |
| 9 | P0-A18c8 remaining exact failures | Revisit only when P0-A19 project gates expose a reproducible generic blocker; do not append rules around fixed slices |
| 10 | P0-A10 finite held-out acceptance | Depends on the project-level contracts above; run each fixed finite set once and publish reproducible metrics |
| 11 | P0-A6 / P0-H9 competition-host replay | A valid resource package and competition-exact host are available; simulation cannot substitute |

Each stage runs the finite construct set once, then expands translator/ContextPack support from observed failure frequency. Do not resume FlashDB line-number rule accumulation, and do not pause independent harness work while waiting for external resources.

- [ ] **P0-A6: real GLM-5.1 candidate generation lane**

  OpenCode `zai/glm-5.1` consumes a hash-bound ContextPack and emits one structured Rust candidate. Record provider, logical/resolved model, variant, prompt, input, raw response, parse result, and candidate SHA-256. Model output, chat text, and file writes remain `semantic_gate=false`. Missing credentials, timeout, malformed response, or missing candidate must become structured blocked results; a silent fallback must not claim that AI ran.

  Current progress: the candidate generator, schema-v7 manifest, sensitive/host-path cleanup, strict JSON parsing, candidate materialization SHA check, source-span provider readiness, balance/auth/launch/timeout classification, and `auto_migrate --ai-first-candidate` are implemented. A missing, escaping, hash-drifted, oversized, or unsupported-encoding source span becomes `context_not_provider_ready` before OpenCode starts and records `provider_invocations=0`; the summary validator reopens the hash-bound ContextPack and independently recomputes preflight and invocation count. LF/CRLF equivalence is checked by streaming full-source hashing plus span hashing. A fragment wrapper must also pass carrier, containing-function, and real upstream fragment SHA/text/claim contracts plus `verbatim_once` embedding before becoming `inline_translation_carrier_bound`, and it still makes no whole-function semantic claim. All 12 fixed cases are provider-ready on Windows and WSL in the P0-A10 input worktree that retains the three pinned checkouts; an ordinary fresh worktree without those ignored checkouts does not have that prerequisite. WSL can list and launch `zai/glm-5.1`. OpenCode writes provider errors to its bounded log and then continues internal retries, so the old harness reached its outer 30/180-second limit first and recorded an empty response as `provider_timeout`. On 2026-07-12, a new log-offset diagnostic covers nonzero exits, timeouts, and first-created logs, emits only fixed sentinels, and never stores raw logs or secrets. It reclassified one real call as `provider_insufficient_balance`. A passing DeepSeek auxiliary run cannot substitute for this evidence, so no real GLM candidate exists and this item remains open.

- [x] **P0-A7: project-level ContextPack and compile-context closure**

  Build minimal context from the real source root/span, effective includes, `compile_commands.json` or manual flags, macros, target ABI, dependency declarations, Clang AST/diagnostics, typed-IR/C2Rust baselines, and validation failures. Relative/absolute paths, generated headers, and build directories must resolve. Secrets, host absolute paths, and unrelated large files must not enter publishable artifacts.

  Completion evidence: ContextPack v3 is split by responsibility into source, compile database, compile arguments, response files, security, and deterministic-artifact modules. It supports an explicit external source root, a repository-relative source root, real span/source SHA bindings, compile-command selection, include/define/ABI summaries, a 128 KB total cap, and a 32 KB per-artifact cap. Path or symlink escape, hash drift, secrets, and host paths fail closed.

- [x] **P0-A8: validation-driven bounded AI repair loop**

  Feed only structured failure facts back to the model: rustc diagnostics, C/Rust schema diff, negative mutation, and unsafe/ABI/alias gates. Default to at most three rounds, with five as the competition hard cap; stop immediately when both input and failure hashes are unchanged. Preserve each candidate, patch, diagnostic, and hash. The model must not modify the oracle, expected output, validator, or gate configuration.

  Completion evidence: the bounded repair coordinator, one-candidate/one-file-patch contract, per-round SHA evidence, LF normalization, unchanged-input-plus-failure stop, and provider/validator fail-closed handling are implemented. A fresh C-oracle proof rechecks the harness, fixture, source span, flags, and ABI. The exact validator runs rustc, replay, schema diff, negative mutation, unsafe/ledger, alias, ABI, and final gates against the current candidate SHA and feeds ten bounded failure classes into the same repair state machine. `--ai-first-candidate` is mutually exclusive with `--accept-existing-evidence`, so old accepted reports cannot certify a new AI candidate. Independent `validate_ai_exact_evidence.py --require-semantic-pass` reopens the auto manifest, router, candidate evidence, gate index, and canonical-draft SHA bindings; the competition runner counts compile/semantic pass only when that strict result and router state both pass. Fresh demo/add-one evidence on 2026-07-12 checked 16 artifacts and returned `semantic_pass=true`.

- [x] **P0-A9: AI-primary multi-candidate router**

  The competition translation path generates a GLM-5.1 candidate first. typed IR and raw C2Rust are zero-token candidates, while C2Rust+repair is an AI-assisted alternative. The router may rank only by recomputable gate results, never by project/function/slice names or model self-assessment. Every candidate traverses the same compile/oracle/replay/diff/negative/unsafe/final gates.

  Completion evidence: the competition runner passes `--ai-first-candidate` for fresh translations, and competition-exact forbids OpenCode/GLM/agent/variant substitution. The pure router accepts at most four candidates, deduplicates by artifact SHA, schedules `opencode-ai -> typed-ir -> c2rust-repair -> c2rust-baseline`, and selects only a candidate that passes all eight candidate-bound gate groups. `auto_migrate` first validates AI, typed IR, and current-run raw C2Rust; any passing zero-token candidate suppresses repair. If all fail, it chooses exactly one repair base: repair raw C2Rust only when it passed strictly more gates than AI, otherwise repair AI, with one shared default-three/hard-five-round budget. The generic C2Rust repair state machine contains no project/function special case, permits one Rust candidate or patch, preserves independent report/round/prompt/response/candidate SHA evidence, and reruns fresh exact gates as `source=c2rust-repair`. Strict validation and competition metrics reopen unique, duplicate, and no-candidate audit states from disk. Repair rounds count toward total model invocations without inflating the initial-AI-candidate success rate. Baseline compile/status, repair reports, and historical accepted evidence never advance a candidate by themselves.

- [ ] **P0-A10: finite cross-project stability acceptance**

  Maintain at most 20 fixed cases covering at least three real C projects and ten distinct construct families. Run the finite set once per stage, with no 1,000/10,000-round or loop stress tests. Publish AI invocation, candidate generation, rustc compilation, semantic acceptance, refused/blocked, repair-round, and route-selection metrics. Repeated similar slices must not inflate success rates.

  Current progress: input and harness closure are complete. `validation/ai-finite-cross-project-suite.json` fixes 12 cases across three real projects and 12 unique construct families: eight real upstream function slices and four executable generic carriers bound to real upstream fragments, with zero synthetic or unbound provenance. `project_sources` pins repository, repository-relative checkout root, and commit. The validator verifies Git HEAD, origin, tracked-clean state, spec/fixture SHA, source file/span SHA, and line/byte alignment offline; it permits only LF/CRLF newline equivalence and rejects content drift or project-label substitution. In the P0-A10 input worktree that retains all three pinned checkouts, Windows and WSL both return `ready=12/blocked=0` under `--require-all-ready`.

  `run_ai_finite_cross_project_suite.py` now passes all 12 specs to one competition runner invocation, disables accepted-evidence shortcuts and outer retries, hash-binds the suite and summary, and refuses to copy metrics unless the lower-level summary validator passed. WSL local simulation may explicitly use an offline Cargo cache, but the summary records `bypassed_for_local_cache`, and `competition-exact` rejects that bypass. Insufficient balance, authentication failure, or provider launch failure opens the circuit on the first failure; timeout retains a two-consecutive-failure threshold, and changing kinds resets the consecutive count. Only actually invoked items enter slice, workflow, and AI-unit counts; remaining items are recorded as skipped rather than fabricated `not_invoked` units, while ContextPack-preflight refusals explicitly record zero provider calls.

  The empty-out-root WSL auxiliary run `ai-auxiliary-p0-a18c8-deepseek-wsl-20260712-02` completed all 12 fixed cases on 2026-07-12. All 12 initial candidates were generated, four evidence-driven repairs ran, and total provider calls were 16. Six cases passed exact gates and six failed; contract failures, execution failures, and provider blocks were all zero. The report SHA-256 is `dfff968ad274924b763a8cede749338f37dcca25e38b5148fbeaaef0d09c2927`. This is a DeepSeek `wsl-local-simulation` quality signal with both public numerators fixed at zero. P0-A10 remains open because the competition GLM run and real-host evidence are still missing.

- [x] **P0-A11: provider admission and zero-call evidence closure**

  Before process launch, the candidate producer reopens the ContextPack's real source/span/carrier bindings. Path escape, incomplete hashes or declarations, mid-read drift, invalid coordinates, budget overflow, encoding failure, or a fragment hidden by conditional compilation is rejected structurally with `provider_invocations=0`. The summary validator does not trust manifest self-reporting; it reopens the hash-bound ContextPack and recomputes readiness and call count. This gate decides only whether a model call is warranted and never raises semantic status.

- [x] **P0-A12: file-based prompt transport**

  Write candidate and repair prompts to repository-scoped, hash-bound files, then launch OpenCode with its file-input option plus a fixed short message so Windows/WSL argv limits cannot change behavior. The manifest must record transport, prompt path/hash, and effective command policy; logs and public artifacts must not copy the full prompt, credentials, or host paths. Completion requires large ContextPacks and repair candidates to leave argv while preserving parsing, timeout, diagnostics, and strict-gate behavior.

  Completion evidence: candidate generation and bounded repair share `opencode-file-attachment-v2`. argv fixes the short message before `--file=<prompt-path>`, correcting OpenCode 1.17.18 consuming the message as a second file; the complete prompt remains SHA-bound on disk. The schema-v7 manifest and schema-v3 repair report record file option/style, message position, fixed-message SHA, and `inline_prompt_in_argv=false`; both summary and exact-evidence validators independently reject transport drift.

- [x] **P0-A13: content-addressed AI candidate cache**

  Derive the cache key from the ContextPack payload hash, prompt schema/version, actual prompt hash, resolved model, agent name, repository-local agent-definition hash, variant, and parse contract. Cache only successfully parsed results whose candidate and raw response can both be reopened by SHA; never cache provider failures, timeouts, refusals, or unparsed responses. A hit still traverses all common gates, records `cache_hit` and zero new provider calls, and remains `semantic_gate=false`; any binding drift invalidates the hit and triggers a real model call.

  Completion evidence: caching is enabled only through explicit `--cache-root` or `--ai-candidate-cache-root`, and the competition runner accepts only repository-local roots. The schema-v4 manifest binds the eight-part content key and copies a hit's `entry.json` into current path/SHA evidence; the summary validator reopens the entry, raw response, and initial candidate, reparses the response, and recomputes the key. Same-key requests first use single-flight and recheck the cache, then publish under a cross-process lock through a unique staging directory and first-writer-wins rename; a corrupt entry is quarantined and rebuilt by one real provider call. Failures, timeouts, refusals, and parse failures are never cached. A hit records initial `provider_invocations=0` and metrics-v2 `cache_hits=1`, while later repair rounds still count as invocations; common gates and `semantic_gate=false` remain unchanged. All 124 contract tests pass on Windows; WSL passes 123 of the same 124 and platform-skips the Windows-lock-specific case.

- [x] **P0-A14: bounded compile response-file expansion**

  Expand only relative `@file` inputs inside the source root, with recursion-depth, file-count, per-file/total-byte, and cycle limits; bind every response file by logical path and SHA. Parse failure, path escape, or budget overflow must fail closed instead of silently dropping include, define, target-ABI, or other arguments. Validate this against different real C projects without project, function, or fixture special cases.

  Completion evidence: ContextPack v3 and its independent schema enable only `gnu-v1` for clang/gcc/cc families; MSVC and unknown dialects fail closed. Relative paths may normalize `..` while remaining inside the root; real escape, absolute/drive/UNC paths, cycles, links, invalid UTF-8/NUL/quoting, depth over 4, more than 16 expansions, files over 64 KiB, totals over 256 KiB, or more than 4096 arguments block before provider invocation. Successful expansion recovers defines, includes, and target ABI, records original/expanded argv SHAs, and binds every file's logical path/SHA/size/depth in both the selected entry and ContextPack inputs. Response changes invalidate the ContextPack, prompt, and AI cache key. Windows and WSL each run 149 tests and pass 148: Windows skips only unavailable symlink creation, while WSL skips only the Windows-lock-specific case.

- [x] **P0-A15: recomputable AI caller/callee context and prompt scope**

  ContextPack must give AI bounded facts for direct-callee signatures, definition status, source bindings, stub boundaries, and call-expression contracts instead of asking the model to infer cross-function semantics from caller source alone. Candidate-manifest `prompt_scope` must be computed from the actual ContextPack rather than claiming absent type-map, CFG, pointer-graph, root-cause, or caller/callee inputs.

  Completion evidence: ContextPack v3's 16 KiB C boundary now includes `external_direct_callees` and `call_expression_contract` under the common sensitive-field and host-path sanitizer, including quoted secret assignments inside ordinary strings, embedded JSON, and escaped JSON. The independent `context_scope.py` derives a stable scope only from nonempty loaded excerpts, real failure states, and named direct-callee facts; informational diagnostics under a successful status do not count as root cause. Normal generation and cache hits share this rule. Provider admission returns zero-call `required_callee_context_incomplete` when a required callee boundary is compacted or truncated by either its local budget or the 128 KiB total ContextPack budget. Current schema-v7 records scope, model identity, and the invocation receipt; the fresh-run summary validator requires v7, reopens the hash-bound ContextPack, byte-recomputes the prompt, and rejects scope, identity, receipt, or version drift. Older versions retain schema and accepted-evidence archive readability but cannot self-select downgrade for a fresh run. This adds no project, function, or fixture special case and does not increase the semantic numerator.

- [x] **P0-A16: transparent auxiliary-model validation and tool-free candidate boundary**

  When GLM balance is unavailable, an alternate model may be selected explicitly for local candidate-quality evaluation, but model identity must derive from the resolved model and propagate through generator, candidate, repair, router, and summary. Only `zai/glm-5.1` is `competition-primary`. Every alternate must record `competition_eligible=false` and `evaluation_scope=auxiliary-local-validation`; it cannot close P0-A6/A10/H9, enter competition metrics, or impersonate `opencode-glm51-1`.

  Completion evidence: a shared model identity and dynamic candidate id now feed manifests and routers. Competition summary validation independently derives identity from the resolved model and cross-checks generator/candidate fields. The exact router binds the selected id, only manifest candidate, and full model scope, while C2Rust repair generators must match the routed AI generator. Candidate generation and repair share the bounded response contract: `c2rust-candidate` is the default with `*=deny`, unknown repository-local agents are rejected before provider launch, worker/preflight retains `c2rust-migrator`, and logical probe model `GLM-5.1` is separate from candidate resolved id `zai/glm-5.1`; `opencode models` accepts both exact tokens and rejects near matches. Parsers recursively reject nested `tool` or `tool_use` events. Strict JSON and one complete JSON fence are accepted, while multiple or incomplete fences fail closed, raising the parse contract to v3. Provider responsibilities are split into 474-line orchestration, 218-line response parsing, 282-line runtime diagnostics, and 121-line receipt construction. Schema v7 binds each provider call to a minimal receipt and separate session-export identity projection. Both use dedicated `additionalProperties=false` schemas and retain only identity, prompt/response SHAs, and the SHA of the full in-memory export; the full session is never persisted. Competition-exact reopens the same live OpenCode session and checks the export SHA, actual identity, attached prompt path and ContextPack prefix, and assistant response, so an unrelated GLM session cannot replace a DeepSeek response. Under WSL OpenCode 1.17.18, omitting `--ai-agent` while using `opencode/deepseek-v4-flash-free` generated `opencode-deepseek-v4-flash-1`. The candidate passed rustc plus compile/oracle/replay/schema-diff/negative-diff/unsafe/alias-ABI/final-verification exact gates. The router preserves its full auxiliary identity and reports `semantic_pass=true`, while the overall auto manifest and candidate remain noncompetition `semantic_pass=false`. The same 203-test group passes on Windows and WSL, with one platform-specific skip on each.

- [x] **P0-A17: isolated auxiliary-model suite and strict input binding**

  When GLM balance is unavailable, run DeepSeek V4 Flash or another noncompetition model through a separate auxiliary runner over the fixed cross-project set. Auxiliary success rates must never enter competition or translator numerators. Every run requires an empty out-root, path-safe and unique project/slice identities, and slice-spec SHA checks before and after model execution. Concurrent OpenCode units receive isolated config/data/cache/state/tmp directories so SQLite, sessions, logs, and export identity cannot cross units.

  Completion evidence: `run_ai_auxiliary_cross_project_suite.py` defaults to `opencode/deepseek-v4-flash-free`, rejects competition-eligible models, accepted evidence, stale out-roots, path escape, duplicate units, and spec TOCTOU, and updates the provider circuit in batches of one to four workers. Reports hard-code `competition_success_numerator=0` and `translation_coverage_numerator=0`. Fresh-oracle full-file and function-span bindings accept only exact bytes or a provable LF/CRLF projection; byte coordinates cannot degrade into permissive line fallback. Candidate and repair prompts copy the exact C signature, parameter order, and Rust pointer/unsafe policy before the full ContextPack. A mixed-version run performed while source files were changing was discarded and contributes no published rate. Auxiliary results still cannot close P0-A6/A10/H9.

- [ ] **P0-A18: put the replay-compatible Rust API contract before the model**

  Before provider launch, generate and hash-bind the exact Rust function contract: name, visibility, parameter count and order, C-pointer to Rust reference/slice mapping, whether length parameters remain, return type, ABI, and unsafe boundary. The slice spec, Rust boundary, and replay generator must derive this contract through shared logic. Project names, function names, fixture names, and model guesses cannot drive it.

  Current root cause: the high-salience boundary summary moved DeepSeek's `fdb_is_str` output from an unrelated single-`u8` API to a semantically close safe-slice implementation, but the model still removed the `len` parameter required by replay, so the initial candidate failed with an arity mismatch. The first isolated repair round also failed to restore `len` and added unnecessary `unsafe`, so the exact gates correctly rejected it. Completion requires the provider and repair prompts to bind the exact generated-replay API and a material reduction in first-round rustc API mismatches on the fixed auxiliary suite. Only exact gates report pass rates.

  This item now has three independently verifiable stages so one improved fixture cannot be reported as generic completion:

  - [x] **P0-A18a: executable replay source contract**. `auto_migrate` generates the replay draft before provider launch. ContextPack v4 fully binds replay source, SHA, size, and real call count so validators can reopen and recompute it. The model projection no longer exposes replay source that may carry expected/actual values; it shows only the same-source ReplayCallPlan and `required_candidate_api`, once in each candidate or repair prompt. Provider readiness, manifest v8, cache, scope, and the fresh summary validator fail closed. Missing, sensitive, oversized, call-free, or drifted replay evidence results in zero provider calls or an invalid fresh run. `_auto_migrate_ai_exact.py` was also split into a 489-line compatibility facade plus routing, persistence, and validation modules.
  - [x] **P0-A18b: structured ReplayCallPlan**. Extract one structured plan from the replay generator with parameter names/order/Rust types, C mappings, retained length fields, return type, ABI, unsafe policy, and call args. The renderer and prompts consume the same plan. Dedicated legacy fallback dispatch and the readiness OR list are removed; function names are contract data only.
    - [x] **P0-A18b1: declarative plan core**. The bounded DSL, plan SHA, ContextPack nested contract v2, source marker, recomputed call arity, single prompt payload, and fresh binding are complete. `source_function_name` is distinct from Rust `api_name`; zlib/libuv now emit real calls, while `fdb_blob_make/kv_to_blob/set/del` select renderers by semantic contract. An AI draft can enter replay only when manifest v8, selected candidate, applied artifact, and draft SHA all close.
    - [x] **P0-A18b2: plan every legacy adapter**. Every inventoried scalar, record, external-sequence, record-pointer, and opaque-context adapter consumes ReplayCallPlan. Incomplete contracts fail closed instead of falling back to a dedicated renderer.
      - [x] **P0-A18b2a: plan byte-slice/CRC paths**. `readonly_byte_slice_bool_return` is normalized by contract kind into the same plan without function-name dispatch, while `fdb_calc_crc32` declares generic parameter/fixture mappings. The plan now supports canonical inline-fixture SHA binding and a `u8_array` codec; `fdb_is_str` and CRC32 both emit typed direct calls. The 141-test focused suite passes and fixed-suite preflight remains 12/12 ready with zero model calls.
      - [x] **P0-A18b2b: record/external plan v2**. All inventoried nine record-state and four scripted external/call-continue renderers are migrated.
        - [x] **P0-A18b2b1: record plan-v2 core**. Bounded `bindings`, `binding_value/borrow_mut`, recursive record initializers, `binding_path` observations, typed `u32/usize/bool` codecs, and mutable-root noalias closure are wired into ContextPack, schema, and fresh binding. Constant, field add, field-plus-scalar add, postfix increment, interior projection, stats sequence, and guarded stats have left legacy dispatch. The v2 implementation keeps a thin facade and splits bounded parts by responsibility.
        - [x] **P0-A18b2b2: remaining state/external paths**.
          - [x] **P0-A18b2b2a: record state 9/9**. Owner-interior u32-to-usize add reuses typed owner observation, while reset-add-while-continue reuses multiple `binding_path` observations. Record parameters with `direction=input` now emit `binding_borrow`/`&T`; only `inout` emits `binding_borrow_mut`/`&mut T`, while noalias still closes over every simultaneous borrow root.
          - [x] **P0-A18b2b2b: scripted external 4/4**. Generic `scripted_runtime` data declares scalar/sequence stimuli, reset channels, call count/args/order probes, scalar/tuple/array/vector/matrix codecs, and a fixture-only claim boundary. Output pointers use bounded array bindings. The plan carries only closed channel/operation values plus callee, fixture-field, and API data; the renderer maps channel/operation pairs to fixed repository helpers, preventing arbitrary Rust-helper injection. All seven real slice specs behind the four legacy renderers now enter plan v2, and the 102-test focused group passes. Of 159 `test_auto_migrate` tests, only two pre-existing CRC32 methods fail and one platform test skips; the same two methods also fail at clean baseline `26b6edce`, so they are not counted as scripted-runtime regressions.
      - [x] **P0-A18b2c: eliminate simple legacy renderers**. All 13 inventoried direct-call adapters for scalar returns, mutable out reports, record/pointer identity, and opaque contexts are migrated. Dedicated fallback dispatch and the readiness OR list are removed.
        - [x] **P0-A18b2c1: scalar-return 4/4**. Structural matching recognizes pure-u32, single-i32, signed-shift-i32, and ABI-bound u64 without reading function or project names, then emits one schema-v3 declarative contract. Fixture metadata such as `status/contract` becomes hash-bound `fixture_assertions` whose constants are checked while building the plan, and `u64` is now a first-class codec. All eight existing demo specs leave legacy rendering; 27 plan tests and three exact auto-migrate tests pass.
        - [x] **P0-A18b2c2: mutable-out/report 4/4**. Single i32 output, dynamic i32 slice output, input-buffer out0-plus-sum, and call metadata now produce generic plans from C signatures, parameter directions, length companions, the call-expression contract, and closed fixture observables. Fixed i32 arrays, bounded `Vec<i32>` initializers, `i32_slice/i32_vec` codecs capped at 4096 items, whole-binding observations, `usize/string_vec` metadata assertions, fixture relations, full symbol/field renaming, and drift refusal are covered. Call metadata explicitly declares `input_fixture_fields`, contexts, and source calls instead of guessing fields or parsing C text. The real store-add-one, copy-i32-ptr-arith, sum-i32-buffer, sum-i32-ptr-arith, and call-expression replays no longer enter their legacy renderers.
        - [x] **P0-A18b2c3: record-pointer/opaque 5/5**. Record pointer identity, record projection identity, record buffer/length identity, opaque key, and opaque key/value now use generic v2 producers. Opaque contexts use a safe Rust fixture model instead of forging a FlashDB ABI marker; old raw-pointer AI drafts fail closed at the replay compile gate. The focused gate passes 48/48 on both Windows and WSL; the 207-test WSL stage gate retains only the two CRC32 failures already present at clean baseline plus one platform skip. This is replay/harness capability only: it does not increase the translation coverage numerator or replace C oracle/Rust replay/diff semantic evidence.
    - [x] **P0-A18b3: split ReplayCallPlan by responsibility**. The former 946-line implementation is now a 48-line compatibility facade plus real dispatch, v1-builder, fixture-binding, schema, and literal-rendering modules, with no `exec` or source-fragment assembly. Commit `b28d5648` passes 87 compatibility tests with one Windows symlink condition skipped.
    - [ ] **P0-A18b4: move ReplayCallPlan v2 out of dynamic fragments**. `replay_call_plan_v2.py` still loads nine `.pyfrag` files totaling about 2,949 lines. Replace them with explicit builder/schema/fixture/scripted/identity/buffer/string modules while preserving plan JSON, plan SHA, and generated Rust byte-for-byte.
  - [ ] **P0-A18c: finite cross-project acceptance**. All fixed 12 cases must generate a real replay call, first-round rustc API mismatches must materially improve over A17, and quality remains counted only by exact gates.
    - [x] **P0-A18c1: bounded provider/repair reliability**. ContextPack replay contract v3 derives an independently recomputable `required_candidate_api` from ReplayCallPlan, including the exact signature, supporting structs, ABI, unsafe marker, and reference-return lifetime; candidate and repair prompts present one complete API copy. AI manifest v9 retries the identical prompt once only for a valid tool-free JSONL completion with no assistant text, terminal `step_finish`, and zero output/reasoning tokens. Both response/receipt/session identities are hash-bound; balance, authentication, timeout, nonzero exit, tool events, and ordinary malformed responses do not retry. Fixture-only Rust compile stubs are removed by restoring the canonical AI draft byte-for-byte after checking, preventing applied-artifact SHA drift. Repair accepts only a complete self-contained Rust candidate and is skipped with zero calls when the fresh oracle did not pass, the target contract is absent, or no structured candidate failure exists. The auxiliary aggregator reopens hash-bound repair reports and counts initial, repair, and total provider invocations separately; its default timeout is 300 seconds. The 161-test focused Windows gate passes. This closes the harness implementation slice only; P0-A18c still owns the fixed 12-item rerun result.
    - [x] **P0-A18c2: fail-closed safety-syntax rejection**. When the Rust safety contracts for call-continue, reset-add, or interior projection raise `ValueError`, `auto_migrate` no longer terminates with a traceback. The runner records `replay_safety.kind=generated_rust_safety_contract_failed`, `replay_execution.phase=safety`, failed mappings, and `semantic_gate=false`, skips rustc/replay execution, and allows later exact/repair processing to continue. The new path passes 4/4 on both Windows and WSL.
    - [x] **P0-A18c3: closed candidate source assembly**. `required_candidate_api` now carries a recomputable `candidate_source_contract`: the candidate is self-contained, nonempty `supporting_types_source` appears exactly once, the harness injects no missing types, and compiler-owned fixture types form a closed type environment. Candidate and repair prompts restate the same rule directly after the required API and forbid replacement extern/FFI/opaque types. The rule reads no project or function names. The API/prompt/provider focused gate passes 62 tests on Windows and 55 on WSL.
    - [x] **P0-A18c4: safe projection for unobserved null-pointer defaults**. In record-pointer identity contracts, fields that are absent from observables and exist only to complete a proxy record with `null/null_mut` defaults now use `Option<NonNull<c_void>>` plus a `None` initializer instead of a raw pointer. Record buffer/length and actual pointer-identity fields do not use this path. ReplayCallPlan validation, rendering, ContextPack schema, and required API all support `optional_non_null_none`. The rule reads no project or function names, and the 34-test focused gate passes.
    - [x] **P0-A18c5: separate safe `NonNull` types from raw-pointer operations**. The exact alias gate masks a `core/std::ptr::NonNull<T>` path only when the candidate contains no `unsafe` and the path appears in a generic type position. `NonNull::<T>` construction, other `ptr` APIs, `as_ptr/as_mut_ptr/from_raw/into_raw`, raw-pointer types, and every `unsafe` + `NonNull` combination still fail closed and require a current-candidate alias proof. The rule reads no project, function, or field names; the related 53-test gate passes on both Windows and WSL.
      - After commit `ecbb2ef3`, the zero-repair DeepSeek checks for `kv_to_blob` and `tsl_to_blob` both reached one-call AI exact pass. Their router SHA-256 values are `4f753f9cd00359d46ed13044f9845b9dff04702c84a4c9ede0aacfc85de31cac` and `93f7b5fe6d2f7f3d903d7042d2a78236b96b86afa491faeea5c971ed00d7a68b`. This closes the alias false positive for two record-identity candidates; it does not increase the competition numerator.
    - [x] **P0-A18c6: generic, source-backed external-callee behavior contract**. When the translated function calls outside the current slice, the model may recover behavior only from SHA-bound C definitions, macros, enums, and struct fields. Fixture expected/actual values and oracle-bearing replay source remain hidden from both candidate and repair. Project names, function names, paths, and fixed fixture values cannot select behavior.
      - [x] **P0-A18c6a: external-callee source context**. Extract bounded function definitions for declared direct callees from the source root, failing closed on path, file SHA, span, TOCTOU, count, or byte-budget violations without dispatching on target names.
      - [x] **P0-A18c6b: source behavior parsing and independent binding**. Parse negated guards, function-like macro field projections, and enum ordinals from SHA-bound source, producing an independent `behavior_sha256`. Fixture expected values are validator-only cross-checks and do not construct rules. The effective fixture SHA binds both source fixture and spec overlays.
      - [x] **P0-A18c6c: remove model oracle inputs and dedicated semantic stubs**. Candidate and repair prompts hide replay source, expected/actual/observed/mismatch, and gate values. `flashdb_kv_set_fixture_model_allowed`, `flashdb_kv_del_fixture_model_allowed`, `rust_check_harness_only_external_stub`, and the fixed `7i32` path are removed. The Windows focused contract gate passes 41 tests with one platform condition skipped.
      - [x] **P0-A18c6d: cross-platform stage gate**. Schema parsing, fresh binding, `git diff --check`, and directly affected auto-migrate paths are closed. The final focused contract group completes 96 tests on Windows (95 pass and one platform skip) and passes 96/96 on WSL; the seven auto-migrate tests for removing dedicated stubs pass 7/7 on each. Readiness recomputes rule/behavior hashes and checks every real callee plus source binding, with fail-closed negatives for commented pseudo-control-flow and `oracle_contract` feedback. The 252-test Windows stage group retains only two CRC32 failures already recorded at clean baseline plus two platform skips, so they are not stage regressions.
      - [x] **P0-A18c6e: auxiliary-model exact rerun**. Empty out-root `ai-auxiliary-p0-a18c6-kv-set-deepseek-wsl-20260712-02` used `opencode/deepseek-v4-flash-free`, one initial call, and zero repairs. The independent validator reopened 16 hash-bound artifacts and reported AI exact pass. Router SHA-256 is `3d71be46e6d8530842397547a9011c1e65b1dc500922ee0d7f69daa12f7514e2`; candidate SHA-256 is `9e9b78cac94f05e6cd23ee6aafd9e4598be415df325e8b4e77e803f75c292230`. The run retains auxiliary identity and both public numerators at zero; it cannot replace the fixed 12 cases or GLM competition verification.
      - [x] **P0-A18c6f: split implementation by responsibility**. External-callee source-block extraction/input binding and guard/macro/enum behavior parsing now live in separate 260-line and 307-line modules; generic provider readiness and callee hash/binding readiness are separate 309-line and 164-line modules. They use no `exec`, dynamic `.pyfrag` loading, or project-specific dispatch.

    - [x] **P0-A18c7: cross-project AI-input and evidence-contract closure**. Recompute eligibility after repair; admit the actual external-source/typed-IR prompt scopes and both context-boundary states in the manifest schema; do not require repository function source for fixture-only scripted callees; derive target-function guard/return rules by reopening a file-SHA-bound strict source span and resolving macro/enum dependencies; use the real translation-carrier fragment as the fresh-oracle span. Project TypeMap `mappings`, CFG `functions`, and a bounded clang typed-IR summary into prompt scope while independently truncating oversized diagnostics. Every rule is driven by structure, declarations, and source hashes rather than project, function, or fixture identifiers. The focused gate passes 125/125 on both Windows and WSL.
      - The fixed empty-directory auxiliary run generated 12/12 candidates with 12 initial and four repair calls, zero contract failures, zero execution failures, zero provider blocks, and an exact-pass increase from 3/12 to 6/12. New passes are `tsl_to_blob`, `kv_set`, and `kv_del`. Report SHA-256 is `dfff968ad274924b763a8cede749338f37dcca25e38b5148fbeaaef0d09c2927`; auxiliary identity and both public numerators remain zero.
      - The remaining failure boundary is now clean: fresh oracle passes for all four real fragments, leaving generated-replay behavior mismatches; zlib-ng and libuv still fail fresh oracle because generated include/header and whole-translation-unit link closure are incomplete. These move to A18c8 without reopening A18c7.

    - [ ] **P0-A18c8: generic semantic closure for the remaining exact failures**.
      - [x] **P0-A18c8a: carrier/scripted source behavior digest**. Derive source-backed branch, call-order, field-write, and return summaries from the real fragment, ReplayCallPlan scripted runtime, CFG/effect data, and typed IR. Fixture expected/actual values remain hidden from the model.
        - The ordered typed-IR projection preserves assignment target/RHS pairs, nested `If/While/DoWhile`, call order, `continue`, fixed returns, unsigned wrapping, and all-ones sentinels under an independent `projection_sha256`. Unknown nodes, more than 64 items, exhausted node/depth budgets, truncation, or hash drift are blocked before provider launch; partial projections are never described as exact semantics.
        - Candidate and repair share the same high-salience source-semantics contract. Scripted callees are harness-owned and already in lexical scope: the model must call but must not declare, define, mock, or inline them. Candidate-owned scripted callees now produce structured rust-check evidence instead of a traceback, and legal Rust signatures with trailing commas are no longer rejected.
        - Artifact bounding reserves the non-metadata budget for a complete clang excerpt, fixing the case where a 6.3 KiB ordered projection was rewritten by generic depth limiting and then failed its SHA binding. The focused gate passes 131/131 on both Windows and WSL, and `git diff --check` passes.
        - In a four-case DeepSeek zero-repair `wsl-local-simulation`, all cases were provider-ready and made one call each. Interior projection and stats sequence moved from replay failures to all eight gates passing; each independent validator reopened 16 artifacts and returned `semantic_pass=true`. Router SHA-256 values are `1d40a293cd63acee5a8eab1c72c155a879bf932ec5bbe68da9e15e965c394e31` and `7bfbc41e2803d72415c4035ab5068eb4b6bbe02eec5966b1e845556e941b2ae1`.
        - Zero-start and read-kv-body-call still fail exact gates after their initial candidates and one repair each: DeepSeek continues rewriting nested loop/return/sentinel behavior or required-API mutability. This evidence does not expose fixture values, add more rounds, or claim a new fixed-suite rate. The default pipeline remains protected by the generic typed-IR fallback and exact gates, so the parent item stays open.
      - [x] **P0-A18c8b: real-project fresh-C-oracle build closure**. Prefer a bound compile database, generated includes, and build-artifact/link contracts. Missing context fails closed; zlib/libuv names cannot select semantic stubs or test-only link flags.
        - A `linked_artifacts_v1` resolver and independent manifest schema now select behavior only from schema/mode/data fields, never target/project/function/slice/name identities. A closure binds the source tree, normalized compile DB, source/generated include trees, static libraries, ordered system link args, source/link symbols, CMake config, toolchain, and target ABI. Every path is repo-relative; parent traversal, absolute paths, symlinks, missing artifacts, and SHA drift are rejected.
        - Producer and validator rebuild the same ordered argv from the closure. Invalid closures produce `native_build_closure_invalid`, zero argv, and structured diagnostics. The slice spec/build profile and ContextPack carry the closure manifest path/SHA; fresh-oracle verification reopens the closure and includes compile-DB/include/library/link/symbol/config/toolchain/ABI identity in flags and the reuse key.
        - The minimal repo-owned closures live under `validation/native-build-closures/`. zlib-ng uses the `ZLIB_COMPAT=OFF` `libz-ng.a`, explicitly binding `adler32_z -> zng_adler32_z` plus generated headers. libuv uses `libuv.a` plus `-lpthread -ldl -lrt -lm`. Both compile databases are free of host absolute paths, and archive hashes match the captured `nm` symbols.
        - In WSL `runs-05`, both units reached `compile_succeeded_not_oracle` with return code 0 and `exited_zero_not_oracle`; oracle compile-contract recomputation passed, and the full libuv auto-translation schema validator passed. Fresh proof now fails only with `oracle_output_mismatch/oracle_output_unproven`, not header/link/build-profile/closure mismatch. Focused contract tests pass 61/61 on Windows and WSL, and the fixed 12-case offline preflight is 12/12 ready with zero model calls.
        - An empty zlib CFG is now represented honestly as `blocked`, and the full auto-translation schema validator passes. This proves complete refusal evidence only, not translation or semantics. The obsolete `adler32_step` AST fixture was removed and must not be restored as synthetic proof for real `adler32_z`.
      - [x] **P0-A18c8b1: hash-bound real-TU translator compile context**. Project a repo-relative source/include/define/compile-DB/ABI context from the verified closure. The compile DB must match its SHA and select exactly one target-source entry; compiler, input, output, dependency-generation, and captured include arguments are removed before closure-authoritative includes and defines are overlaid. Invalid, ambiguous, or ABI-conflicting context blocks without legacy-parser fallback.
        - Function selection binds file, lines, byte range, and normalized fragment SHA. Macro-renamed definitions resolve to the expanded `FunctionDecl` while output restores the logical name. LF-normalized offsets map to raw CRLF offsets for Windows/WSL versus Linux checkouts. Non-null pointer `BitCast` remains refused; only an already-proven null pointer may be retargeted.
        - WSL `runs-07` binds zlib-ng and libuv to commit `0a7831c1`, clang 18.1.3, and each compile-DB SHA. Both native oracle harnesses reach `compile_succeeded_not_oracle`/`exited_zero_not_oracle`, and both full schema validators pass. zlib refuses a runtime function-table member call; libuv refuses `sizeof(*addr)` without record-layout proof. Both remain `semantic_pass=false` and add no success numerator. Rust lib tests pass 253/253 and focused Python tests pass 50/50.
      - [x] **P0-A18c8c: generic executable C-oracle call plan**. Generate data-driven harness calls, input initialization, return/record-field encoding, and stdout comparison from C signatures, fixture codecs, observable outputs, and ABI. Project/function identities cannot select adapters. Fresh oracle may enter AI repair/exact only after the harness executes fixture target calls and every output gate matches.
        - [x] **P0-A18c8c1: direct-return call plan**. ReplayCallPlan v1 scalar, string, hex/byte-array, derived-length, and `null_default` inputs now render generically into C fixture storage, signature-ordered arguments, one real target invocation, and direct-return comparison. The renderer never reads project, slice, function, or fixture identity to select an adapter. Case count, ids, strings, buffers, and rendered source are bounded fail closed, and normalized case-id collisions still produce distinct variables.
        - The validator and fresh-oracle proof regenerate the call plan from the hash-bound spec and compare the plan SHA, declarations, and complete invocation/assertion statements. A marker-only or forged matched-text harness cannot pass. In WSL competition-like `runs-02`, both zlib-ng fixtures execute `adler32_z`; compile and run return codes are zero, the output gate matches, and the full schema validator passes.
        - This stage remains `semantic_pass=false` and does not increase the translation coverage numerator. libuv still refuses because its omitted C output parameter and C record-field projections are undeclared. Output initialization, record/byte-order projections, and C/Rust diff remain later A18c8c work.
        - [x] **P0-A18c8c2: strict structured C-oracle output protocol**. Each `generated` call-plan case/field emits exactly one canonical JSON record binding the ReplayCallPlan SHA, case ordinal/id, field, encoding, and canonical string actual value; total protocol output must fit the persisted stdout bound. The parser rejects missing, duplicate, extra, reordered, wrong-value/SHA, unknown-key, malformed JSON, duplicate JSON key, and non-canonical records while ignoring ordinary diagnostics.
        - Output gate v2, fresh-oracle, and the full evidence validator use the same parser to recompute the gate from persisted stdout instead of trusting producer-supplied matched/missing lists. Historical `not_used` harnesses retain the legacy gate, while a `generated` contract cannot downgrade. In WSL competition-like `runs-03`, both zlib-ng actual-value records match exactly, invalid/unexpected counts are zero, and compile, execution, and the full schema validator pass. This remains `semantic_pass=false` and adds no success numerator.
        - [x] **P0-A18c8c3: declarative output parameters and record projections**. A separate `c_oracle_contract` accepts only safe header basenames, zero-initialized single struct pointees, `address_of`, return, scalar mapping, field, BE16, and fixed object-byte enums. Raw C expressions and project/function adapters are rejected. Output bindings must exactly close omitted C parameters, observation order must exactly match ReplayCallPlan assertions and fixture observables, and the C call-plan SHA binds the replay plan, effective fixture, target ABI, and complete contract.
        - The renderer emits `_Static_assert` checks for pointer/`int`/`CHAR_BIT` and each observed field; each header must uniquely match `c_boundary.files(role=header)`. A complete target/slice/function/output-parameter rename still selects the same path. The implementation is split into a 70-line façade plus direct renderer, output renderer, output schema, and output protocol modules, all below 400 lines.
        - In WSL competition-like libuv `runs-02`, compile and execution return zero. All 12 strict records across the `loopback` and `invalid` cases match for return/status/family/host-port/port-bytes/address-bytes, invalid/unexpected counts are zero, and the full schema validator passes. This closes only the fresh C-oracle harness: the translator still fails closed at unproven `sizeof(*addr)` record layout, `semantic_pass=false`, and the translation success numerator does not increase.
      - [x] **P0-A18c8d: `sizeof(expression)` operand-type recovery**. Real clang JSON omits `argType` for expression-form traits. The frontend now recovers the type only when `inner` contains exactly one operand carrying `type.qualType`. The explicit `argType` path is unchanged; missing type or multiple operands remain `unsupported_sizeof_operand`. WSL libuv `runs-03` advances from the old operand blocker to `unsupported_sizeof_type: sizeof(struct sockaddr_in) requires explicit C layout/ABI provenance`, while the fresh C oracle still matches all 12 strict records. This stage emits no Rust and adds no success numerator.
      - [x] **P0-A18c8e: hash-bound record-layout provenance**. Only the hash-bound compile-database path runs a separate clang `-fdump-record-layouts-complete` invocation with the same sanitized compile arguments. Raw stdout, diagnostics, shared arguments, compile database, and complete target ABI are bound independently by SHA/value. `sizeof` admits only one exactly matching named `struct` layout; `_Alignof`, unions, missing/duplicate layouts, and type/ABI/hash conflicts remain fail-closed. The implementation does not select a size from project, function, or record identity.
        - WSL competition-like libuv `runs-06` captures `struct sockaddr_in` as `size=16, align=4`, derived from the clang dump rather than identity dispatch. The dump SHA is `a6c4e578f594579408d716a1cfbf48ed02169d05630167f2fbabb8c02e7c432b`; the compile-database SHA is `f81a760815fb8618d3856e9f376a89d0e9c76234d3af0dd9e7840d8319f40638`. The fresh C oracle still matches all 12 strict records and the full validator passes; the translator blocker advances to `memset destination BitCast operand struct sockaddr_in * is not mutable unsigned 8-bit pointer`.
        - This evidence proves within-run binding consistency and allows the persisted report to be reopened and checked. Without an externally supplied expected dump SHA, it does not claim to detect cross-run clang/environment drift. This stage remains candidate context with `semantic_pass=false` and adds no translation-success numerator.
      - [x] **P0-A18c8f: generic mutable record-pointer zero fill**. Admit only standard `memset` in statement position, a direct non-null mutable single-level record parameter, an exact zero byte, and complete `sizeof(record)` produced by the same hash-bound layout proof. The frontend lowers this to dedicated `IrStmt::RecordMemset`, retaining record type, size/alignment, four SHAs, and target ABI; an ordinary numerically equal `IrExpr::Call` cannot impersonate it. The typed emitter revalidates provenance and safely emits `*dest = Record { ...zero values... }` only for integers, fixed integer arrays, and recursively complete records. It never byte-slices a Rust record and emits no `unsafe`. Readonly, nullable, nonzero/partial writes, pointer fields, type/ABI/hash drift, `memcpy`, and custom calls remain refused. No project, record, or function identity selects the rule.
        - WSL `runs-09` uses clang 18.1.3 and the default Cargo cache as a local simulation. The old `memset destination BitCast operand struct sockaddr_in *` blocker is gone, while record layout remains bound to dump SHA `a6c4e578f594579408d716a1cfbf48ed02169d05630167f2fbabb8c02e7c432b` and compile-database SHA `f81a760815fb8618d3856e9f376a89d0e9c76234d3af0dd9e7840d8319f40638`. The fresh C oracle still compiles, executes, and matches all 12 strict records, and the full validator passes. The new blocker is `sa_family_t is outside the current type skeleton`; that name records the real observation but is not a future dispatch key.
        - The competition-environment repo-local Cargo mirror returned an empty response in this run, so `runs-09` is explicitly `wsl-local-simulation`, not `competition-exact`. No real libuv Rust candidate is emitted yet; `semantic_pass=false` and the success numerator is unchanged.
      - [x] **P0-A18c8g: generic typedef canonical/desugared type recovery**. Clang type-object candidates are still attempted in `qualType -> desugaredQualType -> canonicalQualType` order, but when all remain unsupported only because target ABI is not bound yet, the last and deepest canonical spelling is retained for the later ABI pass instead of preserving an unresolvable surface alias. Translation-unit `TypedefDecl` inventory now feeds both function-body AST type objects and named/anonymous record fields, including field nodes that omit desugaring when unique alias provenance exists. Alias versus explicit desugared/canonical drift, duplicate conflicts, unknown aliases, and missing target widths remain fail closed. No project, header, function, field, or typedef identity selects the rule.
        - A direct WSL translator rerun uses clang 18.1.3, the same hash-bound libuv compile database, and the `x86_64-unknown-linux-gnu` ABI. The old `sa_family_t is outside the current type skeleton` blocker is gone. The new blocker is `CallExpr: argument 2: ... mutable void * BitCast address target in_addr_t * is not a mutable record pointer`, meaning that an address of a complete mutable scalar lvalue passed as a `void *` write argument is not modeled yet. The two observed typedef names record real progress only and are not dispatch keys.
        - This probe is an unpublished WSL local-simulation diagnostic under `target/`; it contains no Rust candidate, C/Rust replay/diff, or semantic acceptance. `semantic_pass=false` and the translation-success numerator is unchanged.
      - [ ] **P0-A18c8h: generic mutable-scalar address to `void *` call argument**. Model a temporary mutable borrow only for a provably complete integer-scalar direct/record-field lvalue address, a direct external call, and an explicit mutable `void *` parameter. Alias, lifetime, alignment, width, signature, and post-call read paths must be verifiable. Const, nullable, bitfield, packed/volatile/atomic, pointer arithmetic, cross-call escape, and overlapping mutable borrows remain refused; no `in_addr_t`, libuv, or fixed-callee identity mapping is allowed.
        - [x] **P0-A18c8h1: safe-adapter candidate for a record-pointer field address**. The frontend preserves dedicated `MutableVoidPointerAddress` provenance and accepts only one direct mutable record-pointer root, one initial `->` followed only by `.`, a complete record-field path ending in a fixed-width integer, a same-width/signedness source integer pointer, a direct `FunctionDecl`, and a mutable `void *` parameter at that position. Typed IR revalidates complete field inventory, root ownership, non-nullability, overlapping borrows in the same call, and source/target types, then emits `&mut root.path` only for a compile-checked safe adapter. Ordinary `AddrOf` and numerically similar handwritten nodes cannot enter this path. A raw `*mut c_void` extern binding is not disguised as a safe interface and still fails at rustc/link gates without an adapter.
        - [x] **P0-A18c8h2: explicit noalias precondition and equivalent integer typedefs**. The legacy libuv slice spec now carries the generic `pointer_contract`: `ip` is a readonly input, `addr` is a mutable output, and `ip -> addr` is a complete noalias precondition, while `aliasing_proven=false` and the no-whole-program-alias-proof boundary remain explicit. The translator does not infer noalias from project/function identity or differing C types. The dedicated A18h check now accepts different typedef/base spellings only when constness, signedness, and width all agree; signed-32 and width-16 drift remain refused.
        - [x] **P0-A18c8h3: external direct-call compile context and pointer adaptation**. The libuv slice spec declares `__bswap_16` and `uv_inet_pton` with their real C signatures and binds normalized `inet.c` SHA; the system-header inline binds only its call site and header source instead of pretending to be a source implementation. Generic rustc-only stubs use an independent pointee type for each C pointer parameter, preserve const/mutable direction, and report `compile_only_pointer_pointee_erasure`. This applies only to `rustc_compile_only`; fixture/replay-specific models retain exact signatures.
        - The `target/a18c8h-probe-07` WSL run uses clang 18.1.3, the same hash-bound libuv compile database, and `x86_64-unknown-linux-gnu`. It emits a `GenericTypedIr` candidate with both callees marked `generated_compile_only`, `blocked_count=0`, a passing stub contract, and `rust_check.status=passed`. Replay explicitly skips with `compile_only_external_bindings_not_executable`, and this run also uses `--skip-c-oracle`; therefore `semantic_pass=false` and the success numerator remains 38.
        - [x] **P0-A18c8h4a: compile-only candidate/replay API compile preflight**. Whenever rust-check reports a `rustc_compile_only` binding, the harness combines the stub-injected candidate with generated replay and runs one `rustc --test`, persisting stdout/stderr, return code, and `candidate_replay_compile_preflight`. It keeps `execution_allowed=false` regardless of compile result, never runs the stub, and never creates replay pass. A renamed positive proves matching APIs compile but still skip execution; a renamed negative proves API mismatch saves diagnostics while semantics remain false.
        - [x] **P0-A18c8h4b: candidate-required external-callee filtering**. Rust-check first runs structured diagnostics with `--json=diagnostic-short` against the raw candidate without injected stubs, avoiding a compiler rendering crash from long rendered diagnostics when multiple calls are unresolved. It accepts only code exactly `E0425`, message exactly `cannot find function \`NAME\` in this scope`, and `NAME` in the intersection with slice-declared or blocked external callees, then builds compile-only context for only those names. A fully self-contained AI candidate is no longer unconditionally stubbed or prevented from replay merely because the original C call graph still contains external dependencies; a partially inlined candidate receives only its actually unresolved declared functions. Diagnostic-like strings, other error codes, `cannot find value`, and undeclared names cannot spoof selection. This stage does not loosen compile-only execution, does not treat rustc/replay alone as semantic acceptance, and leaves the success count at 38.
        - The `target/a18c8h-probe-10` WSL local simulation uses clang 18.1.3, Rust 1.95.0-dev, the same hash-bound libuv compile database, and the `x86_64-unknown-linux-gnu` ABI. Raw-candidate diagnostics select exactly the two declared functions `__bswap_16` and `uv_inet_pton`; rust-check passes after injecting both compile-only bindings. Candidate+replay preflight still returns 101 for the safe API shape, keeps `execution_allowed=false`, and replay skips with `compile_only_external_bindings_not_executable`. This run uses `--skip-c-oracle`, so `semantic_pass=false`, the success count is unchanged, and the evidence is not `competition-exact`.
        - [x] **P0-A18c8h4c: stable multi-diagnostic feedback for AI repair**. Generated replay and its non-executing preflight now use `--json=diagnostic-short`; this changes no compile, execution, or semantic gate and only removes long rendered snippets that can trigger a Rust diagnostic-renderer ICE. A neutral regression creates both a missing type and an argument-type mismatch in one replay and requires both structured diagnostic classes to persist. The `target/a18c8h-probe-11` WSL local simulation returns normal failure code 1, two `E0425`, two `E0061`, and `compiler unexpectedly panicked=false` for the same real candidate. These errors can feed the existing AI exact repair loop, but this run invokes no model, uses `--skip-c-oracle`, keeps `execution_allowed=false` and `semantic_pass=false`, and leaves the success count at 38.
        - [x] **P0-A18c8h4d: exact compiler diagnostics into AI repair facts**. A separate identity-neutral normalization layer extracts at most eight errors from exact rustc `errors` or replay compile JSONL, retains only bounded `code` and host/sensitive-metadata-redacted `message`, deduplicates them, and binds them to the candidate SHA. The compile gate forwards diagnostics only after candidate/target bindings match; the replay gate additionally requires matching replay/fixture bindings and an exact compile phase. SHA or contract drift never trusts runner diagnostics. Repair facts revalidate the allowlist and discard every extra field, so rendered source, spans, fixture values, and oracle values never enter the model prompt. The harness does not synthesize wrappers or rewrite candidates. OpenCode may return only a complete self-contained Rust candidate, which must rerun rustc, fresh oracle, replay, schema diff, negative mutation, unsafe, alias, ABI, and final verification. This improves repair input only and leaves the success count at 38.
        - [x] **P0-A18c8h4e: AI ContextPack typed-IR extension projection closure**. A separate projection-extension module recognizes `RecordMemset` and `MutableVoidPointerAddress` by structure and type contract rather than project, function, or field identity. Record zeroing requires complete hash-bound layout and ABI evidence, with write length equal to record size. Mutable addressing requires a same-width integer source pointer converted to mutable `void *`, while preserving the nested member path. Neutral renamed tests cover two root/field identities, and layout size, complete ABI, integer width, constness, or unknown-node drift remains fail-closed. Rebuilding the ContextPack from existing real libuv WSL artifacts advances typed IR from `invalid` to `ready` with no unsupported nodes and accurately exposes `external_callee_source_context_incomplete` as the next provider-readiness blocker. This stage invokes no model, accepts no semantic candidate, and leaves the success count at 38.
        - [x] **P0-A18c8h4f: cross-platform newline binding for external-callee source**. A separate hash-bound UTF-8 source reader lets external callee definitions, target behavior blocks, and macro/enum behavior dependencies reuse the primary-source LF/CRLF equivalence contract. Every source block and input binding retains the checkout's actual SHA, the slice-declared SHA, and the `exact/newline_equivalent` mode, and provider readiness recomputes each identity. Newline-style-only changes pass, while content, declared SHA, match mode, span, or binding drift remains fail-closed. Neutral tests use fully renamed CRLF checkout source against an LF-declared hash for both callee and behavior-dependency binding. Real libuv advances `uv_inet_pton` from `source_file_sha256_mismatch` to a newline-equivalent bound source. The system-header `__bswap_16` remains explicitly non-repository-backed without impersonating real source, and overall provider readiness advances from blocked to `ready`. This stage still invokes no model and leaves the success count at 38.
        - [x] **P0-A18c8h4g: real AI-first/repair stage acceptance and bottleneck reorder**. Fresh `target/a18c8h-ai-deepseek-02` runs in the WSL competition-like lane with clang 18.1.3, a fresh C oracle, `opencode/deepseek-v4-flash-free`, the restricted `c2rust-candidate` agent, `max` variant, and three repair rounds. Provider preflight is `ready`; one initial call and three repairs complete. Rounds two and three pass rustc, fresh oracle, unsafe scan/ledger, and alias. Replay advances from link-time compile failure in the initial/round-one candidates to runtime failure in rounds two/three, while ABI, diff, and final gates remain open. Independent `validate_ai_exact_evidence.py` reopens 29 hash-bound artifacts, passes structural validation, and reports `semantic_pass=false`; this auxiliary result contributes to neither competition nor translation-success numerators. The run also proves h4d is closed only in the direct-runner unit boundary: the real `exact_replay_runner` still drops `compile_stderr`, and runtime repair receives only generic `replay_failed`. The next stage therefore closes feedback information before expanding more translation syntax.
        - [x] **P0-A18c8h5a: real exact replay compiler/linker fact closure**. Replay result projection from `validate_auto_migrate_candidate` now lives in a separate roughly 50-line module and forwards `compile_stderr` only for phase=`compile`. The exact gate still verifies candidate, replay, fixture, and target SHAs before retaining compiler facts; drift in any binding discards them. Rustc JSON accepts only a complete `rust-lld: error: undefined symbol: <C identifier>` match inside a note child of a strict linker-failure error and emits bounded `{code: linker_undefined_symbol, message}`. Paths, rendered source, commands, secrets, arbitrary child text, and malformed symbols do not cross the boundary. A real WSL replay of this run's DeepSeek initial candidate plus generated replay produces the identity-neutral `undefined external symbol uv_inet_pton`; the non-target Windows linker remains a generic compile error. Real-wrapper, renamed-linker, spoof, sensitive-data, and four-binding-drift regressions pass, and the 62-test AI exact/repair/provider stage is green. The full 163-test auto-migrate suite retains exactly the three known CRC32 failures and one platform skip, with no new failure. This only improves repair information, never executes a compile-only stub or grants semantic acceptance, and leaves the success count at 38.
        - [x] **P0-A18c8h5b: oracle-free replay case/field failure localization**. ReplayCallPlan derives the complete assertion inventory, while v1/v2 renderers replace value-bearing assertions with opaque guards. The real runner accepts only the fixed `<generated-replay>` alias, one Windows/POSIX separator, an optional numeric thread id, the exact combined-source line, and one replay-owned marker. The auto-migrate wrapper rebuilds inventory from the spec/replay; the exact gate maps `{case_id, observable_field}` only after candidate/replay/fixture/target/plan/inventory bindings all match, and the repair contract rejects every extra field. Expected/actual values, fixture content, replay source, panic text, paths, line numbers, and raw stderr never cross the boundary. Unknown/duplicate/spoofed markers, three envelope drifts, and four execution-binding drifts fall back to a generic failure. Negative mutation now flips the opaque observable guard first instead of accidentally testing a remaining metadata assertion; legacy replay has a bounded fallback, while no recognized assertion is refused. Multi-field v2 scripted probes remain unlocalized and fail closed. The Windows stage passes 67 tests with one platform skip and WSL passes 67/67. The Windows 163-test run retains only the three known CRC32 failures plus one skip; WSL retains only the known `missing_clang_path` route failure plus six platform skips. Neither adds a regression. This improves repair information only, grants no semantic pass, and leaves the success count at 38.
        - [x] **P0-A18c8h5c: precise ABI gating for safe Rust candidates**. A separate 121-line classifier scans only candidate source after comments and strings are masked; it never reads project, function, path, or fixture identity. Safe Rust with no ABI dependency passes independently as `required=false/proof_class=not_required`, so a replay failure no longer duplicates `abi_target_binding_failed`. `usize/isize`, pointer-width casts, target cfg, native endian, and platform FFI types require rustc/oracle/replay execution bindings with matching candidate and target SHAs. `repr(C/transparent/packed/align/integer)`, raw pointers, extern/linkage, layout intrinsics, transmute, unions, and assembly report `abi_layout_proof_missing` without current-candidate layout/FFI proof. Repair receives only dependency kinds, never source, paths, or layout values. Real libuv repair-01 classifies as `repr_c/raw_pointer/extern_abi/ffi_platform_type`, while repair-02/03 classify only as `repr_c`, replacing the old undifferentiated ABI noise. The combined stage passes 76 tests with one Windows platform skip and 76/76 on WSL; the Windows 163-test full run retains only the three known CRC32 failures plus one skip. Other exact/final gates remain unchanged and the success count stays 38.
        - [x] **P0-A18c8h5d: compiler-header declaration-only source classification**. A separate 121-line identity-neutral classifier and a separate small readiness module create and recompute a hash-bound declaration only when `definition_status`, strict `compiler-header:<relative-header>#<symbol>`, a duplicate-free inventory containing the source header, a unique `signature_ref` whose role/function/status/source_ref all agree, and `stub_boundary=compile_only` match. ContextPack stores it under `declarations` with `allowed_use=compile_context_only`, `execution_allowed=false`, and `semantics_verified=false`; it creates neither a repository source block nor a file binding, and the prompt no longer describes declaration-only context as repository definitions. Provider readiness rejects namespace case, absolute/parent/backslash headers, symbol, inventory, signature, hash, status, injected source block, or stub-boundary drift. Two fully renamed positives and a real mixed libuv context prove that `__bswap_16` and `uv_inet_pton` enter declaration and source-block channels respectively without `repository_source_path_required`. The Windows combined stage passes 95/95 and the WSL functional stage passes 91/91; two WSL doc scans are unavailable only because of the Windows worktree `.git` path. The full 163-test runs retain exactly the known Windows three CRC32 failures plus one skip and the known WSL `missing_clang_path` failure plus six skips, with no new failure. This executes no compile-only stub, invokes no model, grants no semantic acceptance, and leaves the success count at 38.
        - The real `target/a18c8h-probe-08` WSL local simulation still has `rust_check.status=passed`, while preflight returns 101 and `candidate_replay_api_compile_failed`, explicitly reporting missing `Ip4AddrReport` and call-shape mismatch. Replay still skips with `compile_only_external_bindings_not_executable`. This stage improves repair diagnostics only and does not increase the success numerator.
        - The A18h parent remains open: required safe candidate API adaptation, executable external-callee semantics/no-escape, direct local/parameter scalar addresses, the raw extern boundary, and post-call field initialization remain separate work. Const, nullable, variadic, indirect-call, signedness/width drift, incomplete-record, and same-root sibling-read cases have fail-closed regressions.

      Prior failure evidence: same-profile `kv_set` router `4a6b7b99095e2c91fde2c40a9eac9ca141a9bb276d4c981dd886115350d6f6a0` passed rustc/unsafe/oracle, but the candidate implemented external-callee result `-1` while the oracle produced `7`. All three WSL worktree runs reported `repo_commit=UNKNOWN0`, so they remain local hash-bound AI-routing evidence only.

  Historical auxiliary runs retain only the decision index below. The corresponding reports and Git history remain the detailed artifact source. Every run is `wsl-local-simulation`, with both public numerators fixed at zero.

  | Stage / run id | Exact | Decision-relevant result | Bound evidence |
  | --- | ---: | --- | --- |
  | A18b2b2a / `ai-auxiliary-p0-a18b2b2a-wsl-20260712-160700` | 2/12 | 12 calls and 8 candidates; 2 contract failures, 1 execution failure, and 1 provider block prevent a generic-improvement claim | run id + unit artifacts |

  | A18b2c2 / `ai-auxiliary-p0-a18b2c2-deepseek-wsl-20260712-180000` | 1/12 | 12 calls and 8 candidates; regression from the prior run means replay planning did not yet improve success | report SHA `bc26d70321086cd5a6e27b9e5d13912aaa29c32445160536364fe9ad00f27458` |

  | A18b2c3 / `ai-auxiliary-p0-a18b2c3-deepseek-wsl-20260712-192612` | 2/12 | 12 initial + 6 repair calls; fixes old aggregate undercounting but only restores the two-pass baseline | report SHA `7e8bd77b9afd6c5883d007a47c5058d47a12d1a5d973b86e5ff042b908e2d5c0` |

  | A18c1 / `ai-auxiliary-p0-a18c1-deepseek-wsl-20260712-204206` | 2/12 | 12 initial + 4 repair calls and 11 candidates; contract failures fell to zero and first-round API mismatch fell 3 to 1, but exact success did not improve | report SHA `512641d1ece1d6dcf8b5bc20af3208da467a1c5284931d3c2a32bdb5cf8551c1` |

  | A18c3 / `kv_to_blob` targeted | 0/1 exact | rustc/replay pass, but `alias_proof_missing` keeps semantic false; a targeted sample is not fixed-suite success evidence | router SHA `0c5f8f7130f4b6b8c163f1007af130cc6016f48234a54e6dde4485a8a7b31316`; manifest SHA `16a7fa57a8038cb9c66a57c8f8a8eeb04cb171dba68a5cc9b6802aa55529f18b` |

- [ ] **P0-A19: unfamiliar-C-project whole-project AI-harness generalization**

  The goal is not another translator branch for a known function. When the competition platform gives OpenCode a previously unseen C repository, the harness must autonomously produce an auditable project-level translation plan, isolated parallel candidates, a compilable Rust project, and verification-driven repairs. Decisions may consume only source, AST, compile databases, build files, types, call graphs, ABI facts, diagnostics, and hash-bound evidence. Project names, function names, directory names, slice ids, fixture constants, and golden-case identities must not select translators, prompts, adapters, stubs, or repair policies.

  **Current boundary (2026-07-15):** A19 has completed one real WSL DeepSeek auxiliary worker plus bounded Ninja/CMake link and static-archive closure, actual rustup-toolchain content binding, non-degrading SandboxBackend requirements/VerificationPlans, a bubblewrap host capability probe, candidate/project Cargo evidence reopening, immutable coordinator-receipt epochs, a project-repair CAS/event/artifact ledger, minimal relevant IR contexts, an isolated provider-contract simulation, host IR rebuilding and re-coordination, deterministic single-step CompletionCoordinator scheduling, and a schema v7 host Cargo project-diagnostic intake. Validated compile intakes now enter a coordinator receipt v2 compatible with the schema v7 database and the shared project-repair queue. AI candidates can reach only `pending-reverification`; the same gate must produce a higher-epoch pass on a new managed generation before one atomic settlement closes the historical obligations. Cargo check/test now completes whole-cohort gate/order/owner/error classification before any repair is recorded, and unknown, mixed, toolchain/environment, or stale results block the entire batch. Cargo stdout/stderr, classification receipts, and bounded unresolved-link intake are content-addressed and reopenable. Initialization/feature/cfg/ABI-specific producers, A19e7 independent verifier capabilities/nonces and process-issued raw output, the global planner, positive whole-project semantic gates, and real held-out semantic acceptance remain incomplete. The parent stays unchecked and the translator numerator does not increase; existing provider-contract simulations and local WSL smoke are not `competition-exact`.

  **A19a arbitrary-repository intake and build-closure discovery**

  - [x] Select an explicit or unique compile database, detect Make/CMake/Meson build facts, expand bounded response files, retain C variants, and safely skip C++ translation units.
  - [x] Enforce repository confinement, link/junction, size/count, and SHA-256 drift checks for roots, sources, databases, outputs, includes, and response files. External paths and sensitive defines do not become model facts.
  - [x] Content-bind repository-confined CMake `link.txt`, generated includes, compile outputs, targets, response files, search roots, and ordered system-link arguments. Under `required`, a missing closure lets only the currently condition-eligible frontier produce bound `candidate-only` quarantine artifacts while the lowest promotion authority and downstream dependencies remain blocked; `bounded-source` can also produce only nonsemantic candidates.
  - [x] Parse repository-confined `build.ninja`, recursive `include/subninja`, variables, rules/build edges, compiler link edges, and response files with a bounded non-executing parser. Content-bind support files and inputs/outputs and recheck drift before execution.
  - [x] Parse `ar`/`ranlib` static-archive closure from multiline CMake `link.txt` and Ninja archive rules, binding operation, target, member order, and same-target `ranlib` without executing build commands.
  - [ ] **A19a6: close Meson and constrained-generation facts.**
    - [x] A19a6a: read existing repository-local `meson-info/intro-targets.json`, `intro-buildoptions.json`, and compiler/dependency metadata without execution; bind targets, sources, generated sources, and introspection-file SHAs. Compiler/link/dependency metadata currently remains a bounded digest and non-authoritative source. Fail closed on incomplete schemas, paths, or closure.
    - [ ] A19a6b: only when explicitly enabled, let a host-owned runner execute one configure/generate action under network, timeout, resource, and repository-boundary restrictions, write into an isolated temporary root, and then extract facts read-only. Missing or failed Meson/configure is an environment/build-discovery block, never guessed arguments or semantic repair input.
      - [ ] A19a6b1: derive compile-database sources that are declared but not yet materialized from canonical BuildIR and bound Ninja/CMake/Meson producer graphs. Run only the minimum producer target after A19e8 sandbox capabilities close, with fixed producer argv and individually constrained output paths, then rediscover and reopen every input. Arbitrary shell execution, guessed targets, and treating manually pregenerated files as a closed harness path are forbidden.

    A19a6a is covered by fixed `meson-info` paths, strict schemas, path/reparse-point restrictions, content binding, and drift revalidation. A real WSL plan over a pinned curl commit also found four unity C sources referenced by the compile database but not produced by the default build. Planning continued after explicitly running the four producer targets already declared by Ninja. The harness currently reports the repository-relative missing-source path in blocker evidence but does not execute the producer itself, so A19a6b/A19a6b1 remain unchecked.
  - [ ] **A19a7: canonical BuildIR and build-adapter convergence.**
    - [x] A19a7a: define a versioned, canonically serialized BuildIR that uniformly represents targets, translation-unit variants, source/generated inputs, compiler/toolchain identity, compile arguments, includes/defines, archive/link order, external dependencies, and ABI facts. Bind every field to its source path/SHA, extractor version, and provenance.
    - [x] A19a7b: require every build adapter to expose only BuildIR downstream. Adapter-specific raw facts may exist only as audit attachments referenced by BuildIR; planners, DAGs, ContextPacks, Rust reconstruction, and verifiers must not consume private adapter shapes. Facts that cannot be normalized losslessly become explicit blocked/refused outcomes, never guesses or silent omissions.
      - [x] A19a7b1: compile-database, Ninja/CMake, and read-only Meson facts enter the canonical BuildIR/attachment validation path.
      - [x] A19a7b2: connect Make dry-run reports to the same BuildIR adapter/validator and statically prove that no downstream module imports the private Make shape.
        - [x] A19a7b2a: explicitly selected canonical Make reports now enter the shared BuildIR/attachment reopener. Planning and worker admission reopen the raw report and reproject it; equivalent repository-root renames preserve semantics, while report or repository-input drift fails closed.
        - [x] A19a7b2b: planners, discovery, the CLI, and verifiers now use generic `BuildInputSelection`, BuildIR-stage, and reopener facades. A Git-tracked AST gate enforces exact source-to-facade-to-symbol and facade-to-private-adapter import graphs, rejecting direct, aliased, relative, and star imports, private-symbol escapes, and dynamic loader/`exec`/`compile`/`eval`/`__import__` references. It enumerates repository production `.py/.pyi` directly instead of depending on host-specific Windows-worktree Git paths; legacy `MakeReportSelection` conversion remains inside the adapter facade.
    - [x] A19a7c: use one BuildIR validator to reopen and recompute every provenance/hash, generated-input closure, target/TU uniqueness rule, and ordered link/archive closure. Source, build-metadata, toolchain, or canonical-projection drift must fail closed before worker launch and final verification.
      - [x] A19a7c1: reopen raw attachments and recompute the compile database, source/generated bindings, Meson facts, TU/target uniqueness, ordered target DAG, and canonical projection. CMake/Ninja link inputs/search roots and archive members now preserve argv order and repeated entries instead of silently sorting and deduplicating them; BuildIR ordinals and semantic-hash regressions lock this behavior. Any repository-fact drift fails closed before planning and worker admission.
      - [x] A19a7c2: under the competition profile, bind and reopen the actual C compiler/archiver/linker binaries, versions, target/sysroot, and environment fingerprint, then let CompletionCoordinator rerun the same BuildIR verifier before `project-final`. A compile-database driver token alone cannot close toolchain drift.
        - [x] A19a7c2a: derive compiler driver/wrapper, linker driver/linker, and archiver/ranlib roles uniformly from compile-database, CMake/Ninja/Meson, and Make raw facts. Every TU and compile/link/archive target must reference an existing canonical toolchain id, and the competition profile must reject token-only or orphan tool records.
        - [x] A19a7c2b: use a host-owned fixed resolver/prober to bind the PATH snapshot, selected/resolved executable, stable binary SHA/size, version, effective target/sysroot/resource roots, host/WSL identity, and environment-allowlist fingerprint. Shells, project argv, and caller-supplied probe argv are forbidden; timeout, flooding, child-process residue, wrapper/path aliases, or missing capabilities fail closed.
        - [x] A19a7c2c: make canonical C-toolchain evidence a BuildIR raw attachment. The same BuildIR verifier must reopen binaries and raw-probe CAS, rerun fixed probes, rederive role mappings, and reproduce the projection byte for byte; PATH, symlink/reparse, binary, version, target, sysroot, environment, or profile drift blocks.
        - [x] A19a7c2d: let `CompletionCoordinator` obtain the sole BuildIR reference from the immutable migration manifest. The competition `complete` repository root is only a reopen locator; run the same verifier before any candidate/AI/Cargo execution and again before host project-final publication, then bind both content-addressed receipts into the completion receipt.
        - [x] A19a7c2e: add identity-free Windows contract tests, real WSL/Linux GCC/Clang/binutils probes, cross-host/profile rejection, and assertions that project-final drift launches no later work. Until A19e7/A19e8 are complete, keep `semantic_gate=false` and do not describe local probes as an independent verifier or `competition-exact`.
        - [x] A19a7c2f: exclude the per-session `WSL_INTEROP` socket value from the reopenable environment fingerprint while retaining its presence for WSL host identity, and continue to bind PATH, compiler binary/version/target/sysroot, and fixed probes strictly. Two independent WSL login processes now reproduce the same evidence SHA; PATH, binary, or probe changes still fail closed under the existing contract.
    - [x] A19a7d: use identity-free equivalent-build fixtures to prove that adapters produce the same canonical projection for the same build semantics, and static boundary tests to reject downstream imports of private adapter fields. A finite held-out project must pass BuildIR validation before entering the migration DAG.
      - [x] A19a7d1: a Git-tracked same-source fixture proves that compile-database+CMake and compile-database+Ninja produce identical canonical BuildIR and `semantic_sha256`. The Make lane is reopened by the same validator and equals them only on a strict, test-only, nonsemantic common contract. Compile target ids now use canonical `kind=object`, define drift changes the common contract, and differences in status, boundaries, raw refs, materialization, toolchain, direct argv/link arguments, and provenance remain retained and explicitly unequal.
      - [x] A19a7d2: extend the same-source fixture to Meson, multiple TUs, archive/ranlib, multi-input and external-dependency/toolchain semantics, then require a finite held-out project to pass the BuildIR validator before DAG admission.
        - [x] A19a7d2a: a Git-tracked extended fixture uses the same two TUs, objects, static archive, ranlib pass, final multi-input link, and `-pthread` facts to prove byte-for-byte identical canonical projections and `semantic_sha256` values across CMake, Ninja, and Meson. Repository renames preserve semantics, while switching authoritative compile/link facts from Clang to GCC changes toolchain semantics in all three lanes.
        - [x] A19a7d2b: BuildIR schema/extractor is now v2. Meson target types and source summaries remain only in provenance excluded from the semantic projection. Canonical target kinds, ordered inputs, archive operation/ranlib count, declared external dependencies, and generated sources use adapter-independent fields. A static boundary test forbids all downstream production modules from reading Meson-private fields, and self-reported Meson compiler-metadata drift cannot override compile/toolchain authority.
        - [x] A19a7d2c: two finite randomly identified held-out projects reopen the same BuildIR reference twice before CIndex, migration graph, ContextPack, portfolio, or ledger work. Initial validation failure or source/hash drift at worker admission blocks immediately with zero later DAG, ledger, or model calls.
        - [x] A19a7d2d: close the host-external native-library input boundary. Sanitize strictly recognized host-absolute library arguments into ordered position, portable basename/format, and source-argument SHA, then project them as `unresolved-native-library` plus `native_link_config_unresolved`; host-directory drift does not change the BuildIR `semantic_sha256`. External objects, unknown paths, and path-smuggling `-l`/`DEFAULTLIB` options remain fail-closed. GeneratedClosure reparses complete bound inputs, and BuildIR recomputes dependency kinds, ids, boundaries, counts, and resolved claims. This boundary may enter AI planning, but CompletionCoordinator blocks project semantic pass until resolution evidence exists.
        - [ ] A19a7d2e: feed `unresolved-native-library` into an identity-free AI/Cargo reconstruction flow that proposes recomputable Cargo dependencies, `build.rs`/link directives, or FFI boundaries. A host verifier independently decides from target ABI, toolchain, portable library identity, actual resolution, and raw Cargo evidence; a model claim cannot set resolved to true. Close only after at least two held-out projects with different native-dependency shapes complete real Cargo build/test and C-oracle comparison, with no project- or library-name dispatch.
          - [x] A19a7d2e1: planning now derives a content-bound `native-link-model-context` from verified BuildIR, groups duplicate dependencies by portable name/format, and exposes only requirement counts and set hashes rather than host-absolute paths. Repositories without native dependencies create no model work. A model response must cover every requirement exactly once and may select only `rustc-link-lib`, an FFI boundary, or defer; it always remains candidate-only with `resolved=false`, `cargo_executed=false`, and `semantic_gate=false`. The BuildIR candidate-execution gate admits only the `native_link_config_unresolved` blocker, but canonical RustProjectIR still retains seven non-native unresolved interface classes, so CompletionCoordinator does not cross that boundary into Cargo and the final gate remains strict.
          - [x] A19a7d2e2: RustProjectIR is now v2 and recomputes `native_link_requirements` from bound BuildIR; an AI candidate can enter only as `native_link_plans`. Missing plans, unsupported strategies, requirement-set drift, or host-path injection fail before Cargo generation. A validated direct-link candidate deterministically emits a search-path-free `build.rs`; the last-good manifest binds candidate/build-script SHAs and keeps `resolution_gate=false`. A separate host `native-link-resolution-receipt` schema/recompute contract binds per-requirement Cargo raw refs, toolchain, ABI, linker trace, and portable actual-artifact metadata; a model cannot issue this receipt or promote itself to resolved.
          - [ ] A19a7d2e3: close fixed native resolution/linker traces, independent verification, final-gate wiring, and held-out semantic evidence.
            - [x] A19a7d2e3a: Cargo sandbox plans fix the complete environment, `--jobs 1`, trusted `cc`/`ld` content binding, and linker-message tracing. Stdout/stderr persist as raw-byte CAS; only the trace JSON layer applies strict UTF-8, duplicate-key, and non-standard-constant checks. The host reopens actual objects through fixed guest-root mappings with TOCTOU rejection, and pure-Python ELF/ar parsing verifies format, machine, pointer width, endianness, and archive members. Content-addressed link-order, target-ABI, bounded FFI-import/actual-export AI-symbol context/evidence, and project settlement now feed `verify_project_cargo`. Missing symbol candidates, raw/trace evidence, object drift, or order/ABI/symbol mismatch stay explicitly blocked; every artifact keeps `semantic_gate=false` and numerator zero.
            - [ ] A19a7d2e3b: close native AI candidates, host interface promotion, candidate-project verification, and the final CompletionCoordinator checkpoint. Model output, Cargo success, or a string issuer must never promote interface completeness, resolution, or semantic state by itself.
              - [x] A19a7d2e3b1: a missing `native_link_plans` set now creates an identity-neutral deterministic project-level repair diagnostic and reuses the existing isolated OpenCode preflight/attempt/receipt worker. The schema-v3 prompt exposes only model context grouped by portable identity; the complete BuildIR context is bound separately as a content-addressed reference hidden from the model. The model may return only one plan-set bind operation covering every requirement. Ingest reopens the immutable migration contract, complete native context, and BuildIR on the host and recomputes the candidate before binding RustProjectIR. Missing or duplicate coverage, absolute paths, reference escape, context drift, or native-section injection through an ordinary repair fail closed. This adds no direct model entry point and keeps `resolved=false`, `semantic_gate=false`, and numerator zero.
              - [ ] A19a7d2e3b2: implement an unforgeable host interface transition and the minimal `IR_PARTIAL -> CANDIDATE_PROJECT_VERIFIED -> IR_PROMOTED -> PROJECT_FINAL_VERIFIED -> COMPLETED` state machine. Per-section closure receipts must deeply reopen the DAG, every BuildIR, candidate/source CAS, fixed validator versions, and all seven interface fact families. Candidate Cargo check/test/trace may run only in a quarantine generation and must not update final `CURRENT` or final project gates. Canonical RustProjectIR v3 may be promoted atomically only by the host from complete closure evidence. Promotion must create a new `verification_context_sha256` and `promotion_epoch`, regenerate the project, and rerun final integration/Cargo so old Cargo evidence, ABA epochs, and post-Cargo settlement self-reference cannot be reused.
                - [x] A19a7d2e3b2a: add the lower-level project-final candidate-domain and isolated whole-project verification API. Before Cargo, the host deeply reopens RustProjectIR, the complete DAG, and every BuildIR, then recomputes the run contract, full project-final candidate set, and domain context. A single whole-project `cargo check/test` runs only in a detached immutable quarantine generation; native dependencies force raw-output and linker-trace capture. Cargo observations, raw CAS, native settlement, and `verification_context_sha256` enter a separately reopenable candidate-only receipt. The path never updates final `CURRENT`, writes no `project_gate_records`, and grants no semantic gate or coverage numerator. A missing trusted native linker blocks before any Cargo launch. This API grants no promotion and remains disconnected from CompletionCoordinator until b2b/b2c are complete.
                - [ ] A19a7d2e3b2b: implement seven real host section validators. Each validator must independently recompute its facts from deeply reopened C/BuildIR/DAG inputs, candidate sources, and compiler evidence. Arbitrary JSON, model claims, a fixed issuer string, or hash consistency alone cannot issue closure. The current simple Rust-symbol regex, empty sections, and `unresolved-before-compiler` cannot close public signatures, type layouts, global ownership, or initialization/destruction.
                  - [x] A19a7d2e3b2b1: land strict but non-authoritative interface fact producers and deepen candidate-generation reopening. The host can recompute compiler-artifact sets from Cargo JSONL; derive workspace, feature, and target topology from `cargo metadata --no-deps` while retaining package/target semantic identities and removing host paths; extract source-bound top-level function, global, record, and initialization candidate facts from Clang AST JSON; and extract field offset, size, and alignment facts from raw Clang record-layout output. Duplicate keys, non-finite numbers, unknown events/targets/crate types, implicit or invalid declarations, anonymous/union/bitfield/flexible layouts, input floods, and toolchain/target drift fail closed. Candidate receipt reopening also verifies the link/junction-free generation tree, last-good/quarantine manifests, generator, and IR/interface/candidate-set bindings; a self-consistent rewrite of inner and outer hashes cannot change the candidate set. Every artifact fixes `semantic_gate=false` and numerator zero and cannot issue section closure.
                  - [ ] A19a7d2e3b2b2: connect the b2b1 parsers to fixed host runners, raw-output CAS, and the complete deeply reopened domain; add Rust post-cfg public-signature/layout witnesses, the cfg/feature target matrix, global owner/access facts, initialization/destruction order graphs, and C/Rust comparators; then let seven independent validators issue recomputable receipts. Parser unit tests, Cargo build-finished, correct hashes, or empty facts cannot close this item.
                    - [x] A19a7d2e3b2b2a: complete the candidate Cargo fact subchain. The host runs exact `cargo metadata --no-deps --format-version 1` only inside the detached quarantine generation and reuses fixed `cargo check --message-format=json`; raw metadata/check/test stdout and stderr enter separate gate CAS roots. Candidate receipt v2 binds one generation, sandbox contract, toolchain, probe, execution plan, metadata topology, and compiler-artifact set. Deep reopening rereads raw bytes, reruns both strict parsers, and compares every derived field; legacy v1, argv drift, cross-generation reuse, stderr drift, or derived-CAS drift fails closed. This item still fixes `interface_closure=false`, `semantic_gate=false`, and numerator zero.
                    - [x] A19a7d2e3b2b2b: add fixed isolated runners and raw-output CAS for Clang AST and record-layout, then build the common D0 validation domain that reopens the run contract, parent/child DAGs, every BuildIR and original-C repository binding, candidate/source CAS, base v2 IR, quarantine generation, b2a receipt, and fixed toolchain/target context.
                    - [ ] A19a7d2e3b2b2c: implement Rust post-cfg typed/layout witnesses, the cfg/feature target matrix, module/target ownership, global access, and initialization/destruction graphs, then let seven independent C/Rust comparators and validators issue recomputable section receipts for non-empty covered domains. Plain empty arrays cannot close a section.
                      - [ ] A19a7d2e3b2b2c1: complete the compiler-backed Rust witness-producer subchain. Every witness must bind D0, the b2a quarantine generation, raw compiler output, Cargo topology, actual cfg/features/target/toolchain, and remain candidate-only.
                        - [x] A19a7d2e3b2b2c1a: deeply reopen the metadata/check raw CAS from the D0-fixed b2a receipt, rerun the Cargo metadata and compiler-artifact parsers, and generate a recomputable package-to-target ownership, actual target/profile, and active-feature topology witness. This witness covers only the existing `--all-targets --all-features` lane. Featureful projects, missing/extra targets, ambiguous package identity, or source/CAS drift block it, and it cannot issue section closure.
                        - [x] A19a7d2e3b2b2c1b: use a content-bound `syn` or equivalent mature Rust parser to extract pre-cfg module/item/signature/type/global/init candidates plus every cfg/cfg_attr. Macro expansion, parse ambiguity, or unsupported attributes must block explicitly; regex must not act as Rust syntax authority.
                        - [ ] A19a7d2e3b2b2c1c: run post-cfg compiler witnesses for the bounded feature/target matrix under the fixed sandbox/toolchain and capture raw/derived CAS for public signatures, typed layout/offsets, module ownership, global access, and initialization/destruction graphs. Missing default/all/no-default or other required lanes cannot close the matrix.
                      - [ ] A19a7d2e3b2b2c2: implement seven independent, version-fixed C/Rust comparators/validators that recompute from D0 and raw witnesses. Each receipt may close only its own non-empty covered domain, and every missing, empty, stale, or swapped fact must fail closed.
                        - [ ] A19a7d2e3b2b2c2a: `public-signature` comparator/validator.
                        - [ ] A19a7d2e3b2b2c2b: `shared-type-layout` comparator/validator.
                        - [ ] A19a7d2e3b2b2c2c: `global-ownership` and access comparator/validator.
                        - [ ] A19a7d2e3b2b2c2d: `initialization-destruction` ordering-graph comparator/validator.
                        - [ ] A19a7d2e3b2b2c2e: `cfg-feature-extraction` comparator/validator.
                        - [ ] A19a7d2e3b2b2c2f: `nested-module-multi-target` comparator/validator.
                        - [ ] A19a7d2e3b2b2c2g: `target-matrix` comparator/validator.
                - [ ] A19a7d2e3b2c: implement the append-only transition ledger, monotonic `promotion_epoch`, non-reusable in-process transition permit, and canonical RustProjectIR v3 atomic promotion. Every promotion must bind the b2a receipt, all seven b2b receipts, base IR/domain bindings, and a new `verification_context_sha256`, rejecting old evidence, cross-run/candidate-set reuse, and ABA epochs.
                - [ ] A19a7d2e3b2d: make CompletionCoordinator publish final `CURRENT` only for promoted v3. The promoted IR must be rematerialized and rerun through final integration, Cargo, semantic gates, and the last BuildIR checkpoint; candidate Cargo evidence is pre-promotion evidence only and cannot be reused as final pass.
                  - [x] A19a7d2e3b2d1: the final generation publisher now reopens the IR and rejects `interface_completeness != complete` or any remaining unresolved section before stage/CURRENT writes. Public `integrate-verified`, CompletionCoordinator, and other callers can no longer put partial v2 into final `CURRENT`. Verifier tests that depended on the old behavior now use explicit test-only generations; production gates were not relaxed.
                  - [ ] A19a7d2e3b2d2: complete promoted-v3 detached final generation, epoch/context-bound final gates, the last BuildIR/Cargo/semantic rerun, and post-pass `CURRENT` CAS publication.
              - [ ] A19a7d2e3b3: bind the actually selected static-archive member/symbol, ABI, toolchain, link order/trace, actual artifact, independent verifier process/capability, and complete CAS reopening to the current promoted IR and final BuildIR checkpoint. Missing, swapped, stale, or mock-only evidence must block completion.
            - [ ] A19a7d2e3c: at least two identity-neutral held-out projects with different native-dependency shapes must complete real Cargo build/test, corresponding C build/oracle/replay-diff, and every project semantic gate. Missing or partial success, model self-reporting, or local contract tests alone cannot aggregate to resolved.

        Finite A19a7d2e1/e2 stage evidence (2026-07-15): all 678 `test_project_migration*.py` tests passed on Windows with five platform-conditional skips; the same 678 passed under WSL with three platform-conditional skips. New tests cover same-library path grouping, zero model work without dependencies, missing/duplicate/path-injected/forged-resolved responses, BuildIR-to-RustProjectIR drift, candidate-only `build.rs`, rejection of unmaterialized strategies, and tampering of Cargo/toolchain/ABI/linker-trace/actual-artifact receipt evidence. This stage invoked no provider/model and ran no real held-out Cargo, linker trace, or C oracle. It keeps `semantic_gate=false` and the translation numerator at zero, so A19a7d2e remains open.

        Finite A19a7d2e3a stage evidence (2026-07-15): the same `test_project_migration*.py` set ran 790 tests with zero failures on Windows and WSL, with five platform-conditional skips on Windows and three on WSL. One hundred focused native regressions cover raw-byte CAS, trace parsing, actual-object reopening, ELF/ar boundaries, ABI, link order, symbol context/candidates/evidence, settlement, and tamper closure. CompletionCoordinator and native trace code were split by responsibility, and every added or modified production Python file remains at or below 300 lines. This stage invoked no provider/model, completed no real held-out Cargo/C oracle, and used no independent verifier process. An unreachable `interface_candidate_ready` branch rejected by canonical RustProjectIR was found and removed, so A19a7d2e3 and A19a7d2e remain unchecked.

        Finite A19a7d2e3b1 stage evidence (2026-07-15): the same `test_project_migration*.py` set ran 796 tests with zero failures on Windows and WSL, with five platform-conditional skips on Windows and three on WSL. Six focused tests cover the model-safe prompt, complete plan-set binding, BuildIR reopening, incomplete-set rejection, absolute-path/reference-escape rejection, and one fake-provider round trip through the isolated OpenCode runtime, preflight, attempt ledger, provider receipt, and host ingest. All 50 project-repair regressions pass, and every added or modified production Python file remains at or below 300 lines. This evidence made no real provider/model call, ran no held-out Cargo/C oracle, and did not implement host interface promotion or an independent verifier. Therefore only A19a7d2e3b1 is checked; A19a7d2e3b, A19a7d2e3, and A19a7d2e remain unchecked, with `semantic_gate=false` and numerator zero.

        Finite A19a7d2e3b2a/b2d1 stage evidence (2026-07-15): all 813 `test_project_migration*.py` tests passed on Windows with five platform-conditional skips, and the same 813 passed under WSL with three platform-conditional skips. The WSL Rust translator `--all-features` run passed 267 + 688 + 4 tests, explicitly ignored 133, and had zero failures. New tests cover the candidate domain, isolated Cargo execution, candidate-only receipt and tamper closure, and prove that a missing trusted native linker launches no Cargo process. The production final publisher rejects partial v2 before stage/CURRENT writes, while old verifier fixtures now use an explicit test-only generation. Every added or modified production Python file remains at or below 300 lines. This stage made no real provider/model call and completed no held-out Cargo/C oracle, seven-section interface validation, RustProjectIR v3 promotion, or independent verifier. Therefore only b2a/b2d1 are checked; b2, b2b, b2c, b2d/b2d2, b3, e3, and e remain unchecked, with `semantic_gate=false` and numerator zero.

        Finite A19a7d2e3b2b1 stage evidence (2026-07-15): all 852 `test_project_migration*.py` tests passed on Windows with six platform-conditional skips, and the same 852 passed under WSL with three platform-conditional skips, with zero failures or errors. New and main-thread-reviewed tests cover strict JSON/UTF-8, bounded flooding, semantic identity retention, host-path removal, implicit/invalid declarations, conservative layout rejection, externally supplied toolchain/target bindings, self-consistent manifest forgery, and link escape across Cargo compiler-artifact, Cargo metadata, Clang interface AST, Clang record-layout, and candidate-generation reopening. Every added or modified production Python file remains at or below 300 lines. This stage invoked no provider/model, did not connect the parsers to fixed compiler runners or seven closure receipts, and completed no Rust post-cfg signature/layout witness, real held-out Cargo/C oracle, v3 promotion, or independent verifier. Therefore only b2b1 is checked; b2b and every higher completion item remain unchecked, with `semantic_gate=false` and numerator zero.

        Finite A19a7d2e3b2b2a stage evidence (2026-07-15): Windows ran 867 `test_project_migration*.py` tests, with 861 passing and six platform-conditional skips caused by Linux-only live gates or Windows symbolic-link privileges. WSL then ran the same suite with `C2R_RUN_LIVE_TOOLCHAIN_TESTS=1` and passed 867/867 with zero skips, failures, or errors. The candidate whole-project path now includes the fixed Cargo metadata runner, metadata/check/test raw CAS, metadata/compiler-artifact derived CAS, candidate receipt v2, same-generation/sandbox bindings, legacy-v1 rejection, and raw/derived/stderr deep reopening. Outputs from every executed command reach CAS before a compile failure blocks fact parsing; CAS write/hash-binding failures recompute `raw_output_bound` as false; native-trace blockers preserve their diagnostics and return before ready facts; metadata/compiler derivation failures after raw CAS binding preserve those references and block precisely; the receipt recomputes execution status, `cargo_executed`, and `semantic_gate`; and the generic candidate compile verifier retains its original two-gate behavior without claiming topology facts. Every added or modified Python file stays at or below 300 lines. This stage invoked no provider/model and completed no Clang runner, full D0 domain, Rust typed/layout witness, seven section receipts, real held-out Cargo/C oracle, v3 promotion, or final CURRENT publication. Therefore only b2b2a is checked; b2b2, b2b, and every higher completion item remain unchecked, with `interface_closure=false`, `semantic_gate=false`, and numerator zero.

        Finite A19a7d2e3b2b2b stage evidence (2026-07-15): an independent competition-profile host receipt binds the Clang binary, version, target, and resource directory, and each BuildIR translation unit receives separate AST and record-layout plans. The Linux runner executes only through shell-free bubblewrap. Once a command starts, stdout/stderr first enter immutable raw CAS separated by plan/unit/gate/stream, followed by independent derived CAS. Record-layout can consume only a reopened AST receipt for the same unit and selects only named complete records bound to original C sources. D0 uses one read-only SQLite snapshot to reopen the run contract, exact candidate set, parent/child DAGs, every BuildIR, the original C repository, candidate/source CAS, base v2 IR, quarantine generation, b2a receipt, all Clang plans, and the live toolchain target. A real WSL lane installed and exercised the bubblewrap 0.9.0 capability probe, then completed real Clang AST-to-`Packet` layout under read-only repository/toolchain mounts and reopened both raw and derived evidence. The live lane also found and fixed two general defects: empty environment values were rejected by argv validation, and a `/workspace`-absolute source argument prevented portable AST source-location evidence. The focused set passed 48 tests on Windows with one conditional live skip and 48/48 in the WSL live lane. The stage-wide set passed 914 tests on Windows with seven platform-conditional skips and 914/914 in the WSL live lane with no skips, failures, or errors. Every added or modified production Python file remains at or below 300 lines. This stage invoked no provider/model and did not implement Rust post-cfg witnesses, seven comparators/validators, v3 promotion, an independent verifier, or a real held-out Cargo/C oracle. Therefore only b2b2b is checked; b2b2, b2b, and every higher item remain unchecked, with `section_closure=false`, `semantic_gate=false`, and numerator zero.

        Finite A19a7d2e3b2b2c1a stage evidence (2026-07-15): a new candidate-only Rust Cargo topology witness accepts only a D0 reference and deeply reopens D0, letting D0 fix the run, candidate set, base v2 IR, and b2a receipt. It then rereads metadata/check raw CAS from b2a, reruns the Cargo metadata and compiler-artifact parsers, and records package-to-target ownership, actual target/profile and active feature sets, the b2a verification context, quarantine generation, and Cargo fact binding in a recomputable CAS receipt. Output contains no workspace or host paths. Missing, extra, or duplicate targets, ambiguous package identity, mismatched all-features observations, self-consistent tampering, or current D0/b2a/CAS drift all fail closed. Only featureless packages can make the existing `--all-targets --all-features` topology lane ready. Any declared feature remains blocked with `rust_cargo_feature_matrix_lanes_missing`; one lane is never treated as a complete cfg matrix. All 25 focused tests passed. The stage-wide set passed 923 tests on Windows with seven platform-conditional skips and 923/923 in the WSL live lane with no skips, failures, or errors. Every added or modified production Python file remains at or below 300 lines. This stage invoked no provider/model and did not implement the pre-cfg `syn` producer, post-cfg typed/layout witnesses, seven validators, v3 promotion, or a held-out oracle. Therefore only c1a is checked; c1, c2, b2b2c, and every higher item remain unchecked, with `section_closure=false`, `semantic_gate=false`, and numerator zero.

        Finite A19a7d2e3b2b2c1b stage evidence (2026-07-15): `c2r-translator` now contains an independent stdin JSONL parser pinned to `syn=2.0.118` and `quote=1.0.46`. Normal source parsing is bounded to 4 MiB, streamed input to 64 MiB, one syntax fact to 64 KiB, aggregate facts to 20,000, and output to 16 MiB. The parser reads no candidate path, computes the source SHA-256 directly from raw bytes, and extracts root/inline/external modules; ordinary, trait, impl, and foreign function signatures; structs, enums, unions, aliases, and traits; const/static/foreign-static facts; initializer and Drop candidates; and attributes plus macro invocations at every parsed syntax position. Plain `cfg` is retained verbatim. `cfg_attr`, derive and function-like macros, unknown attributes, unresolved external modules, verbatim or parse ambiguity, invalid UTF-8, empty source, and every limit breach block explicitly. The host materializer deeply reopens D0, the b2a quarantine generation, base-v2-IR candidate CAS, and the c1a Cargo topology receipt. A repo-relative path, size, and SHA-256 bind the parser binary. Per-candidate stdout/stderr enter immutable raw CAS before strictly recomputed witnesses enter derived CAS. Receipt reopening rereads the original candidate bytes, parent evidence, and CAS, reruns the same current binary, then compares raw bytes and every derived field. Rewriting internally consistent JSON and hashes, replacing the binary, swapping candidates, removing blockers, or changing counts cannot survive this domain. The binary currently runs as a bounded separate process with a minimal environment and the receipt explicitly records `sandboxed=false`; c1c still owns fixed Linux isolation plus post-expansion, post-cfg typed/layout and target-matrix facts, so c1b has none of that authority. Five focused Rust tests and 12 focused host parser/process/evidence and source-layout tests pass. Rust `--all-features` passed 267 library tests, 688 bounded tests with the existing 133 explicit ignores, four integer-conversion tests, and five new witness tests. The first 934-test Windows project-wide run had one unrelated pre-existing concurrent-path transient that passed immediately in isolation; the final stage-wide run passed 934 tests on Windows with seven platform-conditional skips, while the WSL live lane passed 934/934 with no skips, failures, or errors. Every added or modified production Rust/Python file remains at or below 300 lines. This stage invoked no provider/model and implemented no c1c witness, seven comparator/validators, v3 promotion, or held-out oracle. Therefore only c1b is checked; c1, c1c, c2, b2b2c, and every higher item remain unchecked, with `post_cfg=false`, `section_closure=false`, `semantic_gate=false`, and numerator zero.

        Finite A19a7d2d held-out evidence (2026-07-15): pinned libevent commit `e1f0335d2f3af1235421c939c62943bb99b3d652` in WSL competition-profile local simulation run `hard-libevent-native-06` produced a ready GeneratedClosure, `ready_with_boundaries` BuildIR, 5,906 units, 23,600 assignments, and four initial-ready workers. It sanitized 168 `libcrypto.so`/`libssl.so`/`libz.so` dependencies into unresolved native boundaries. An independent project-final reopen reported only `native_link_config_unresolved`, with no C-toolchain evidence drift. The run took 778.33 seconds and peaked at 1,549,408 KiB RSS; ContextPages, portfolio, and portfolio DAG were 185,683,579, 245,224,312, and 37,335,598 bytes. It invoked no model, Cargo, or oracle and keeps `semantic_gate=false` with numerator zero. A19b4c2 remains responsible for monolithic-artifact and per-catalog NTFS publication costs; planning success is not translation success.

        Final finite gates for the same stage: all 660 `test_project_migration*.py` tests passed on Windows with five platform-conditional skips, and all 660 passed under WSL with three platform-conditional skips. The WSL Rust translator `--all-features` run passed 267 + 688 + 4 tests, explicitly ignored 133, and had zero failures. The bilingual document mirror plus source-layout gate passed 5/5. Ignored Rust cases, the held-out plan, and unexecuted model/Cargo/oracle work do not count as successful translation.

        This completion proves only canonical adapter convergence for bound build facts, drift rejection, and DAG-admission ordering. It does not prove command execution, build success, program semantics, independent verifier identity, real held-out translation success, or `competition-exact`. A19a7c2 remains authoritative for real host-toolchain identity.
  - [ ] **A19a8: explicitly enabled, strongly isolated Make dry-run fact collection.**
    - [ ] A19a8a: permit an explicit `collect-make-facts` choice only when a compile database, verified generated facts, and read-only metadata cannot close BuildIR; never auto-fallback. Before Make starts, prove network isolation, read-only source, an independent output root, child-process containment, an environment allowlist, timeout, CPU/memory/file/process/output limits, and post-exit cleanup. Missing capability fails closed before execution.
      - [x] A19a8a1: the current entrypoint accepts only a precollected Make report with explicit path/SHA/size binding. It never falls back from compile-database or ordinary discovery, and dual input is blocked by discovery and CLI preflight.
      - [ ] A19a8a2: implement host-owned `collect-make-facts` that executes only after earlier adapters cannot close the build and every A19e8 capability probe passes. The current precollected-report entrypoint is not a production collector.
    - [ ] A19a8b: fix the invocation to `make -B -n -j1 --no-print-directory -f <makefile> -- <targets>` and reject arbitrary commands, flags, environments, shells, configure steps, or generators.
      - [x] A19a8b1: the fixed argv/target contract, bounded stdout parser, and nonsemantic report schema exist. A report can be built only from a successful typed runner outcome with verified cleanup and binds its execution plan plus raw stdout/stderr. Recursive Make, shell control, command chains, libtool/configure, ambiguous compile-plus-link commands, and path escape are rejected; `semantic_gate=false` and numerator zero are fixed.
      - [ ] A19a8b2: execute the fixed command inside an A19e8-proven sandbox and persist/reopen raw stdout/stderr, toolchain, sandbox, and input references. Cover `$(shell ...)`, included-Makefile rebuilding, timeout, output flooding, and exit cleanup. The current parser/report contract does not imply that a runner is connected.
    - [x] A19a8c: normalize only losslessly parsed direct compiler, archiver, ranlib, and linker invocations into BuildIR. The report binds Makefile, source, leaf input, toolchain, preflight, plan, and raw output; absent outputs remain `materialized=false`. BuildIR records explicit boundaries for headers, generated-output materialization, and external dependency resolution, claiming only a complete direct command graph. When the `required` complete closure is missing, only the current frontier may enter the non-promotable `candidate-only` quarantine path; explicit `bounded-source` remains only a nonsemantic investigation path. Both report and BuildIR remain nonsemantic evidence with numerator zero.
    - [ ] A19a8d: use identity-free equivalent fixtures to prove that Make dry-run and compile-database/Ninja/CMake adapters produce the same canonical projection, covering shell/configure/recursive-Make rejection, timeout/output flood, path/link escape, input drift, and sandbox-receipt drift.
      - [x] A19a8d1: identity-free Make fixtures cover repository-root rename equivalence, fixed argv, shell/configure/recursive-Make rejection, timeout/output flood/cleanup, report and every bound input/preflight drift, and worker-admission evidence reopening.
      - [ ] A19a8d2: add cross-adapter canonical-projection equivalence for the same Make, compile-database, Ninja, and CMake build semantics, plus included-Makefile and `$(shell ...)` adversarial cases inside the production sandbox.
        - [x] A19a8d2a: same-source CMake/Ninja lanes achieve full canonical BuildIR equivalence; the Make lane achieves bounded common-build-contract equivalence while retaining every adapter/evidence difference and a define-drift negative case, fixed at `semantic_gate=false` and numerator zero.
        - [ ] A19a8d2b: under the A19e8 production sandbox, add included-Makefile rebuilding, `$(shell ...)`, real timeout/output-flood/cleanup, and Meson, archive/ranlib, multi-input link, external-dependency, and toolchain equivalence/difference matrices. Until then, bounded common-contract equality cannot be reported as full canonical equivalence.
    - [ ] A19a8e: extend real recursive Make and build-time generators into reopenable, verifiable typed build events. Continue to fail closed whenever working directory, input, output, execution order, or sandbox identity cannot be bound losslessly; never infer a child directory from text position or execute an arbitrary program directly.
      - [ ] A19a8e1: parse and hash-bind recursive-Make enter/leave-directory events, retaining a repository-relative working directory, parent/child invocation edge, and target scope for every child command. An unbalanced directory stack, linked path, cross-repository path, or concurrent output whose ownership is ambiguous blocks collection.
      - [ ] A19a8e2: model “compile and link host tool -> execute tool to generate source/header -> consume generated output” as a generated-node closure. Only the A19e8 sandbox may execute a content-bound host tool, with argv, environment, inputs, outputs, exit status, and raw streams bound. Until modeled, compile-plus-link generation steps and subsequent arbitrary commands remain `make_collection_output_unrepresentable`.

  **A19b project dependency graph and bounded decomposition**

  - [x] Build content-bound function/TU nodes, direct-call resolution, SCC/wave DAGs, shared-global context groups, and parser-boundary routing.
  - [x] Put repository include closure, compile defines, top-level declarations, global initializers, and TU coverage records into deduplicated ContextPacks with separate page, group-total, and page-count limits.
  - [ ] Replace lexical-only gaps with clang AST/type/layout/preprocessor facts for macro expansion, function pointers, complex declarations, and ABI layout. Every unproven span must enter an explicit boundary or refusal.
  - [ ] **A19b3: canonical CIndex, content-addressed evidence, and exact provenance.**
    - [x] A19b3a: distinguish raw source/header blob SHA from normalized semantic-fact SHA. Header/include records retain reopenable content references only, so storage for a header shared by many TUs grows near-linearly with unique content rather than reference count.
    - [x] A19b3b: bind every parser/preprocessor/include blocker to source path, source SHA, directive SHA when available, offset/range, and a reopenable evidence-set reference. Summaries may compress presentation but must not collapse same-offset findings from different files or allow dangling/tampered evidence.
    - [ ] A19b3c: replace equal-length whitespace projection of top-level context with AST/preprocessor-derived declaration spans, layouts, macro definitions/expansions, and initialization facts. An uncertain boundary blocks explicitly instead of presenting invented complete context to AI.
    - [ ] A19b3d: add tests for same-offset/different-header provenance, dangling or tampered evidence, near-linear shared-header growth, source drift, and canonical-hash recomputation. Every downstream consumer uses only a CIndex projection reopened by the shared validator.
  - [ ] **A19b4: deterministic staged retrieval, selection receipts, and frontier-lazy ContextPacks.**
    - [ ] A19b4a: define versioned retrieval catalog/query/selection receipts binding policy, query seed, current unit/SCC, required/selected/omitted fact refs, materialized-page refs, budget, and SHA. Ordering is deterministic; missing required facts, reference drift, insufficient budget, or unresolved symbols block assignment.
      - [x] A19b4a1: host catalogs, deterministic symbol selection, and selection/materialization receipts bind policy, query seed, SCC, required/selected/omitted set SHAs, byte budget, and materialized page-set SHA. Reference, budget, or receipt drift fails closed.
      - [ ] A19b4a2: add unresolved-symbol, AST type/layout/macro, and verifier-failure facts to one query/required-fact contract. Missing facts become a recoverable retrieval block instead of relying only on a lexical identifier seed.
        - [x] A19b4a2a: every `unresolved_external` call/global now derives a versioned `host-required-symbol-facts-v1` query. Its receipt binds the SCC, origin facts, query SHA, match set, and unresolved set. Only a structured matching declaration or bounded lexical declaration in the current required/selected facts can close a request; ordinary call text cannot impersonate a declaration. A missing declaration, a declaration omitted by budget, receipt drift, or a zero-deferred-fact bypass blocks assignment with no model launch.
        - [ ] A19b4a2b: connect A19b3c AST declaration/type/layout/macro producers and verifier-owned failure facts, then promote missing facts from the current static `blocked` state into A19b4c2's recoverable frontier `pending_retrieval -> ready` transition. A19b4a2 stays unchecked until then.
    - [ ] A19b4b: make the required layer include current source, compile context, direct calls/globals, and blocker summary. Select declarations, macros, layouts, and direct-include segments by exact symbol/type matching; expand only through include adjacency, dependency-SCC interfaces, and verified failure facts. Never put the full repository or unrelated headers back into a prompt.
      - [x] A19b4b1: current source/direct-call/global/blocker facts remain visible, while deferred header/top-level chunks are selected by deterministic exact identifiers, direct include bindings, and adjacent chunks. Omitted facts appear to the model only through a host-withheld summary.
      - [ ] A19b4b2: consume A19b3c AST/type/layout/macro facts and expand only through include adjacency, dependency-SCC interfaces, and verified failure facts, including fail-closed unresolved-symbol rules.
    - [ ] A19b4c: schedule only the current runnable frontier and retrieve/materialize the minimum pages for its assignments. A group in `pending_retrieval`, with incomplete required facts, or with an unverified receipt cannot be leased or launched. Recompute the next frontier from new DAG/verification facts instead of expanding all project ContextPacks before launch.
      - [x] A19b4c1: generate a size-bounded, content-bound host catalog per SCC. Planning writes neither prompt pages nor worker requests. Dispatch reopens the catalog, receipt, and page set only for the latest `schedule.ready`, materializes pages incrementally before leases, and binds an immutable frontier receipt into the request and attempt. The same frontier produces the same content-addressed receipt on first execution and resume; immediately before model launch, the host reopens that receipt and checks every worker, assignment, catalog, retrieval, and page binding. Existing-page, group-index, catalog, receipt, or partial-write drift fails closed, and the main path does not copy the omitted-fact store.
      - [ ] A19b4c2: make selection itself a frontier-scoped `pending_retrieval -> ready` transition and recompute the next frontier from the latest DAG, failure evidence, and expansion queries after each wave. Remove plan-time whole-project selection, monolithic CIndex JSON, and repeated portfolio contexts while retaining near-linear sharded CAS growth.
        - [x] A19b4c2b: host-owned single-SCC refresh closes `pending_retrieval -> ready` through a private permit, CAS reopening, a runtime overlay, and dispatch/request/prelaunch revalidation; drift creates zero attempts and zero provider calls.
        - [x] A19b4c2c: wave inputs, bounded selection directives, atomic whole-wave invalidation, and resumable per-SCC refresh are connected to the production CLI. Failure evidence, expansion queries, query epochs, cross-SCC scope, and caller authority fail closed. The parent remains unchecked because removal of plan-time monolithic CIndex/repeated contexts and near-linear scale evidence are still open.
        - [x] A19b4c2d: persist a compact, hash-bound portfolio summary in the root `project-migration-plan.json` instead of duplicating all assignments, ledger units, and context. Dispatch streams path/size/SHA verification on one file handle, requires canonical UTF-8 JSON, and checks run/status/DAG/portfolio-plan identities before immutable-ledger verification. The full portfolio remains monolithic, so this item does not close the A19b4c2 parent.
        - [x] A19b4c2e: rebuild ContextPages one SCC at a time during planning and release page/catalog payloads immediately after binding, passing only a host-issued group closure into the portfolio. The closure binds group, context, catalog, canonical profiles, page order, logical/artifact SHA, and sizes. All catalogs first enter same-volume temporary staging and become write-once visible only after every group validates, so a later-group failure leaves no visible partial output or sticky retry state. Main JSON writes, content hashes, and ledger portfolio bindings now share streaming canonical metadata, and CIndex, ContextPages, and graph objects are released after their final use. Full CIndex/ContextPages/portfolio artifacts and whole-project selection remain monolithic, so the parent stays unchecked.
    - [ ] A19b4d: define distinct minimal-context contracts for planner, translator, reviewer, and repairer. Translators receive complete required facts; planners receive project summaries and interface risk. A model needing more facts submits a structured retrieval query; the host selects, accounts for, and returns a new receipt. Models cannot read files directly or assert facts themselves.
    - [ ] A19b4e: handle functions larger than one-call budget hierarchically: extract signatures, control-flow regions, local type/macro dependencies, and state summaries, produce candidates for verifiable regions, then recompose at function level. If cross-region semantics or ABI cannot be proved, defer/refuse the whole function instead of truncating it and claiming completeness.
    - [ ] A19b4f: add exact macro/type hit, deterministic order, required-budget block, receipt-tamper, selected-only materialization, frontier-lazy loading, role isolation, explicit expansion, and huge-function refusal tests. Finite held-out projects must show lower token/page use without degraded build/oracle conclusions.

  Header deduplication, blocker summaries, recoverable function spans, callee signatures, unresolved-external required-fact receipts, deterministic selection receipts, retrieval-segment/seed-page separation, dispatch-time physical frontier materialization, pre-model receipt reopening, and verifier-failure/include-adjacency/dependency-interface frontier selection with resumable wave refresh now exist. The root plan no longer duplicates the full portfolio, and planning no longer retains every ContextPages payload plus a second catalog copy. However, selection is still computed for the whole project at planning time, CIndex/ContextPages/portfolio artifacts remain monolithic, and role contracts plus huge-function decomposition remain open, so A19b3/A19b4 and the A19b4c2 parent stay unchecked.

  Finite A19b4a2a stage evidence (2026-07-14): all 525 `test_project_migration_*.py` tests passed on Windows with five platform-conditional skips; the same 525 tests passed under WSL with three platform-conditional skips. This stage invoked no provider/model and ran no project semantic gate. It proves only required-symbol query recomputation, receipt binding, and assignment fail-closed behavior; the translator numerator remains zero.

  Finite A19b4c1 stage evidence (2026-07-13): 254 `test_project_migration*.py` tests passed on Windows with two platform-conditional skips, and all 254 passed under WSL. A WSL local simulation over pinned mbedTLS commit `9e9eb069d6aa3db84bef07b6d83a78bdee9b1da6` completed planning with the `required` build-closure policy: 2,410 ledger units, 9,252 assignments, 97 structurally blocked groups, and four ready workers. Planning wrote only 2,410 sharded catalogs; prompt pages, groups, assignment requests, attempts, and model invocations all remained zero. The run took 8 minutes 47 seconds with about 1.15 GB peak RSS, while CIndex, ContextPages, and portfolio artifacts remained about 50.6 MB, 181 MB, and 96.1 MB. This is neither competition-equivalent semantic acceptance nor evidence for closing A19b4c2 or increasing the translation-success numerator.

  Finite A19b4c2b/c stage evidence (2026-07-14): 619 `test_project_migration*.py` tests passed on Windows with five platform-conditional skips; the same 619 passed under WSL with three platform-conditional skips. The frontier/CLI/security focus set passed 114/114, the Rust translator all-feature tests passed, and the judge entrypoint dry-run planned 4/4 entries. WSL competition smoke remains blocked by local image version differences, a pre-existing FlashDB oracle call-plan evidence gap, and the OpenCode agent length gate, so this is recorded only as `wsl-local-simulation`, not `competition-exact`, semantic acceptance, or a translator-numerator increase.

  Finite A19a6b1/A19b4c2d held-out evidence (2026-07-15): pinned curl commit `6546ffeda4f1e81f68db6af721398148b5c74317` completed a 494-TU WSL plan after explicitly producing four Ninja-declared generated sources, yielding 4,476 ledger units and 17,700 assignments. Its root plan was 71,191 bytes versus a 194,295,931-byte full portfolio. Pinned mbedTLS commit `9e9eb069d6aa3db84bef07b6d83a78bdee9b1da6` reached a BuildIR with 580 targets, 383 TUs, zero boundaries, and complete required build closure. Planning produced 11,637 ledger units, 44,808 assignments, and four initial-ready workers in 47 minutes 24 seconds with about 5.30 GiB peak RSS. The root plan was only 5,087 bytes, but CIndex, ContextPages, and portfolio artifacts were 175,890,287, 819,814,295, and 512,488,719 bytes. Final canonical streaming dispatch took 2 minutes 24 seconds with about 3.02 GiB peak RSS, materialized 26 current-frontier pages, and produced four launch descriptors without a model/provider call. This proves only local-WSL BuildIR/plan/dispatch and compact-root reopening, not Rust-project reconstruction, a semantic gate, or `competition-exact`; the translator numerator remains zero, and the monolith/memory results keep the A19b4c2 parent open.

  Finite gates for the same stage: all 631 `test_project_migration*.py` tests passed on Windows with five platform-conditional skips; the same 631 passed under WSL with three platform-conditional skips; the bilingual document mirror passed 4/4; and the WSL Rust translator `--all-features` command completed with zero failures. A 16 MiB canonical-artifact microbenchmark reduced loader peak memory from 50,337,122 to 33,688,912 bytes, while the real dispatch peak remained dominated by the portfolio object and frontier materialization. Ignored Rust cases and an unexecuted semantic project gate are not counted as successful translations.

  Finite A19b4c2e memory/equivalence evidence (2026-07-15): a synthetic plan with 32 groups of 256 KiB reduced `tracemalloc` peak from 18,139,218 bytes on the retained-payload path to 2,087,339 bytes on the streaming-closure path, about 88.49%. A stage-only rerun for the pinned mbedTLS commit reused the existing 819,814,295-byte ContextPages and 33,160,149-byte graph, regenerated 11,637 ledger units and 44,808 assignments in 17 minutes 21 seconds, and peaked at 4,584,668 KiB RSS. The old repository-to-plan run peaked at 5,555,644 KiB, a difference of 970,976 KiB (about 17.48%), but the measurement boundaries differ and this is not a strict end-to-end comparison. The DAG was byte-identical to the old baseline. All 44,814 changed portfolio lines were explained by this run's explicit `max_attempts=5` versus the old baseline's `1` and the derived plan SHA; after normalization, SHA and size exactly matched the 512,488,719-byte baseline with zero other differences. This stage invoked no model/provider, ran no semantic gate, keeps `translation_coverage_numerator=0`, and cannot close the A19b4c2 parent.

  Final finite A19b4c2e gate (2026-07-15): the focused document-mirror, streaming-write, catalog-security, closure-forgery, staging-recovery, and ledger-streaming set passed 21/21. All 648 `test_project_migration*.py` tests passed on Windows with five platform-conditional skips, and the same 648 passed under WSL with three platform-conditional skips. The WSL Rust translator `--all-features` command completed with zero failures. Existing ignored Rust cases, the stage-only memory rerun, and the unexecuted project semantic gate are not counted as successful translations.

  **A19c OpenCode multi-worker candidate portfolio**

  - [x] Materialize planner/translator/reviewer/repairer portfolios, isolated config/data/state/cache/tmp/out roots, DAG dependencies, and SQLite assignments/leases/attempts/artifacts.
  - [x] Close plan/preflight/request/attempt drift, lease re-entry, heartbeat, non-atomic ingest, tool events, secret input, parent-environment leakage, agent permission override, pre-spawn command-start accounting, and missing provider-evidence persistence. The current finite regression runs 351 tests on Windows with 349 passing and two conditional skips, plus 351/351 on WSL.
  - [x] Run one real WSL `opencode/deepseek-v4-flash-free` worker through the fixed project preflight: one provider invocation, preflight-report SHA `f0bc76b6b2489597e782cd1f0e532b7483f03658680b4352401ee12c814913c0`, verified session export, execution-report SHA `9e04b437135aa9f1e2171a180f2e77272374fc8bba3c1257d314e8a963c10b5d`, and candidate SHA `359cb2ef234f85d9aa1cbdf2d77e07a296f55cb8bd7d4ad9dd37afe4d9ea72ef`. It remains only `candidate-ready` / `auxiliary-local-validation`; only the exact real competition-host GLM-5.1 contract can close P0-H9.
  - [ ] **A19c3: progressive AI admission and immutable upgrade while build closure is incomplete.**
    - [x] A19c3a: when BuildIR is verified but generated closure is incomplete, preserve only the scheduler's already condition-eligible current frontier under both `required` and `bounded-source`. Bind exact `candidate-only`/quarantine/promotion requirements into the assignment, worker request, output contract, and translator/repairer artifact. AI may generate, review, and repair candidates inside the isolated unit, but the lowest SQLite promotion authority rejects `last-good`, so the result cannot unlock cross-unit dependencies, integration, semantic gates, or the numerator. Missing, contradictory, or drifting contract metadata fails closed.
    - [ ] A19c3b: after closure completion, add an immutable upgrade/replan transaction that reopens BuildIR, generated closure, prior candidate source, and verification evidence, then chooses regeneration or revalidation under a new assignment/run identity. It must not mutate the old request/artifact's hash-bound `candidate-only` identity or use an old quarantine candidate to unlock later waves directly.

    Finite A19c3a stage evidence (2026-07-15): every candidate must now carry an `admission_binding_sha256` recomputed from the integration manifest's BuildIR/generated-closure references. Complete closure yields `semantic-eligible`; incomplete closure yields `candidate-only` under both `required` and `bounded-source`, and missing fields no longer default to admission. Only the host copies that identity into candidate metadata from the worker result contract. Semantic context, candidate final, the lowest `last-good` promotion authority, CompletionCoordinator's project-final BuildIR checkpoint, and read-only held-out acceptance independently reopen or recompute admission/closure, so quarantine candidates can perform candidate compile/quarantine work only. All 1001 `test_project_migration*.py` tests passed on Windows with 11 platform-conditional skips; the same 1001 tests passed under WSL with six platform-conditional skips. This stage invoked no provider/model, did not complete A19c3b, a project semantic gate, or real held-out translation, and keeps `semantic_gate=false` with numerator zero.
  - [ ] **A19c4: maximize AI project reasoning, candidate diversity, and information gain.**
    - [ ] A19c4a: add a global AI planner over the complete BuildIR, Migration DAG, RustProjectIR, and prior failure facts. It produces a recomputable migration strategy from dependencies, interface risk, available verification, and project blockers, then replans locally as evidence arrives without routing by project name or fixed test identity.
    - [ ] A19c4b: allow explicitly strategy-labelled diverse candidates and interface proposals for a high-risk unit under one shared budget, deduplicate them by source SHA, and compare them through identical host gates. Reviewer/critic workers may emit structured risks and repair hypotheses only; model voting, self-score, or majority opinion cannot grant a pass.
      - [x] A19c4b1: select fixed, content-bound generic strategy variants for initial generation and compile/oracle/negative/unsafe/ABI/final failure families, rotating them with prior translator/repairer attempts. Bind the strategy into the request, prompt, and candidate metadata, while explicitly forbidding model self-score and semantic acceptance.
      - [ ] A19c4b2: retain multiple parallel or sequential candidates for one high-risk unit under a shared token/time budget, deduplicate by source SHA, and run identical host gates on the same cohort. Selection must be decided by gate evidence and remain resumable and replayable.
        - [x] A19c4b2a: sequentially generate at most two unique candidates for a planner-bound high-risk unit within the translator's shared attempt cap. Candidate files are isolated by attempt and deduplicated by source SHA, so repeated content does not fill the unique pool. The host verifies the latest candidate without a failed gate first, falls back to an unverified candidate after a gate failure, and, when all candidates fail, selects one deterministic repair base only from latest host-gate pass count, failure count, and source SHA. The fact layer, scheduler, candidate set, and lowest verification entry point share one content-bound selector; reviewer self-score cannot change the result.
        - [ ] A19c4b2b: create an immutable candidate-evaluation cohort for every unique candidate of one unit, run the complete identical compile/semantic/final host-gate suite without ending comparison at the first failure, persist a selection receipt, and deterministically choose among multiple fully passing candidates using host risk facts. The sequential failure fallback in b2a improves convergence but does not replace this all-candidate same-cohort comparison, so b2 and b2b remain open.

        Finite A19c4b2a stage evidence (2026-07-16): a generic content-bound candidate-pool selector now uses only the presence of a planner assignment as the structural risk fact that raises the unique-candidate target to two; it reads no project, function, path, or test identity. Two independent translator attempts bind rotating strategies. Repeated source retains independent attempt provenance but counts as one unique candidate by its full source SHA. The latest candidate without a failed gate enters the existing identical reviewer/compile/semantic/final chain first; failure moves the unit to a resumable state and selects an unverified fallback. When all candidates fail, latest host verifier records alone rank the repair base by pass count, failure count, and stable SHA. All 1170 Windows project-harness tests passed with 24 platform-conditional skips. Under WSL after loading the repository's `huawei-competition-ubuntu-24.04` profile, all 29 candidate-pool, scheduling, candidate-set, gate, recovery, and source-layout tests passed. An exact production-harness scan found no known project identity or historical key fragment, and every added or modified Python file remains at most 300 lines. This stage invoked no provider/model, did not run the competition platform, and did not create immutable per-candidate evaluation cohorts or compare multiple fully passing candidates. Therefore b2b, b2, and their parents remain open with `semantic_gate=false` and numerator zero.
    - [ ] A19c4c: schedule the next planner/translator/reviewer/repairer call from failure fingerprints, unresolved uncertainty, dependency critical path, expected information gain, token/time cost, and observed convergence. Stop unchanged-input failures, low-information repetition, and exhausted budgets instead of mechanically launching every role or a fixed number of rounds.
      - [x] A19c4c1: add deterministic information-gain ordering that accepts only `external-verifier` evidence and recomputes critical path from the bound DAG. New failures, input/strategy changes, uncertainty, token estimates, and convergence attempts affect priority; repeated repair with unchanged failure, input, and strategy stops, while model self-scores or caller-reported critical path cannot change fallback order.
        - [x] A19c4c1a: execute every worker from one `run-to-completion` dispatch as a real concurrent batch and drain the batch before deciding its terminal result, preventing later pre-created leases from expiring behind serial workers. A terminal worker no longer leaves an unexecuted sibling; only host-confirmed, unstarted, unconsumed `prelaunch-blocked` attempts may retry, and two consecutive no-progress batches produce a stable blocker.
        - [x] A19c4c1b: derive answer-free channel signatures from project-oracle failures for `stdout`, `stderr`, exit, signal, timeout, oversize, and process class. Repair receives only channel relationships and a stable fingerprint, never expected/actual values, prefixes, hash contents, or raw output.
      - [ ] A19c4c2: derive those facts automatically from the ledger, BuildIR/DAG, and verifier receipts; add shared token/time budgets, strategy coverage, and convergence windows across workers, and record exhausted budgets as resumable blocked/deferred evidence.
    - [ ] A19c4d: maintain bounded content-addressed project knowledge containing only host-extracted or verified API/type/ABI/ownership/build facts, failure classes, and candidate decisions. Retrieve the smallest context relevant to the current unit, interface, or repair; never feed oracle answers, fixture expected/actual values, credentials, or the unrelated full repository back to the model.
      - [x] A19c4d1: implement strict allowlists, content SHA, evidence SHA, subject relevance, entry/byte bounds, and artifact/request/prompt binding. Oracle/expected/actual values, credentials, raw source bodies, and complete-repository fields fail closed before model input.
        - [x] A19c4d1a: project a ready `ProjectTestInventory` through each BuildIR target scope into a separate content-addressed, model-safe test contract and bind it through portfolio, scheduler, request, and translator/repairer prompt. The model sees only target/test IDs, argument shapes, environment-key names, input paths, working directory, and timeout class; literal arguments, environment values, oracle values, and runtime output stay withheld, with at most 128 relevant tests per unit.
      - [ ] A19c4d2: let BuildIR, RustProjectIR, TransitionAuthority, and dedicated verifiers write facts automatically, query by the current unit/interface/repair, and persist reuse-hit metrics. Callers must not construct knowledge passes or bypass provenance.
    - [ ] A19c4e: coordinate the global planner with the project-level repair queue for cross-unit API, shared type, initialization, linkage, and feature conflicts. AI may propose grouped candidate patches and migration order, but every application remains bounded by file/budget limits, quarantine, rollback, and project-final full revalidation.

  **A19d Rust-project reconstruction and incremental integration**

  - [x] Generate a stable content-derived Cargo module tree and Cargo.lock with symbol dependencies and unsafe budgets. Store last-good as immutable generations behind an atomic `CURRENT` pointer and recover interrupted projections at startup.
  - [x] Bind integration reports to the ledger-authoritative candidate set. A host adapter reopens the immutable generation, checks accepted group/source SHAs, and records the fixed-authority integration gate.
  - [ ] **A19d3: canonical RustProjectIR and cross-unit coordination.**
    - [ ] A19d3a: define a content-bound, versioned, recomputable RustProjectIR that uniformly represents the crate/module tree, public APIs, shared types, global ownership, initialization order, FFI/link boundaries, `cfg`, features, targets, and unsafe obligations. Every declaration must reference BuildIR, Migration DAG, and candidate-source evidence.
      - [x] A19d3a1: RustProjectIR v1 now binds and reopens canonical BuildIR, the complete Migration DAG or a content-addressed dependency-closed cohort, candidate sources, and per-declaration evidence. The host recomputes public/required/unsafe/FFI facts from current source and blocks any IR/source/metadata drift. Wave IR also references the immutable full DAG; project-final IR must cover every unit. A fixed completeness boundary marks unextracted interface layers as partial and prevents project-final gates and a completed receipt, while candidate generations remain publishable for isolated checks.
      - [ ] A19d3a2: feed complete Rust signatures, shared-type layouts, global ownership, initialization/destruction, link/target, and conditional-configuration facts from host extractors or verified AI interface proposals. Automatically derived public signatures remain explicitly unresolved and cannot impersonate complete interface proof.
    - [ ] A19d3b: add one project interface coordinator that reconciles cross-unit APIs, shared-type layouts, symbol visibility, global-state ownership, initialization/destruction order, FFI boundaries, and Cargo features before unit candidates merge. Workers must not independently emit conflicting public glue or Cargo features.
      - [x] A19d3b1: the fixed host coordinator detects virtual/candidate crate roots, unknown parents, parent/init cycles, orphan or unknown module references, and API/type/global/FFI/feature/cfg/init duplicates or conflicts. Its canonical receipt SHA is recomputed before every Cargo generation; conflicts produce no generation.
      - [ ] A19d3b2: make the global planner and every translator/interface proposal consume the same coordinator receipt before merge, then return resolved interface decisions as immutable project facts for later ContextPacks. Model opinion alone still grants no pass.
        - [x] A19d3b2a: the existing unit planner, translator, and repairer now load the same latest immutable coordinator-receipt epoch while materializing facts and receive only diagnostics relevant to their group. Contexts, requests, and prompts fail closed on epoch, receipt SHA, group, or content-SHA drift. The true global planner and resolved-interface fact feedback remain in the parent item.
    - [ ] A19d3c: route type, link, initialization, and feature diagnostics that cannot be attributed to one unit into a separate project-level repair queue that proposes bounded repairs from the complete RustProjectIR. Never assign them randomly, duplicate shared types, or bypass conflicts with fixture-specific shims.
      - [x] A19d3c1: the coordinator emits a bounded, content-hashed project-only repair queue. Each item binds the IR SHA, interface SHA, diagnostic SHA, resolvable affected modules/units, unresolved module IDs, and attempt cap, while forbidding random unit attribution, generated glue, and fixture-specific shims.
      - [ ] A19d3c2: persist queue/events/attempts in the TransitionAuthority ledger, let the project repairer consume only the smallest relevant IR/diagnostic context and emit a new IR candidate, and make recovery, budget exhaustion, rollback, and re-coordination replayable events.
        - [x] A19d3c2a: project receipts, queues, items, attempts, CAS events, and artifacts have been ledger-backed since schema v5. The dedicated repair worker reads only affected module/interface records, the model can submit only allowlisted patches, and the host reopens full bindings, rebuilds RustProjectIR, re-coordinates, and selects resolve/rollback/retry/exhausted. Launch intent is committed before process creation; unstarted recovery is owner/lease constrained, while a started command with an unknown result becomes `failed/manual-reconcile`. Terminal responses, IR candidates, and rollback evidence bind to immutable artifacts from the same attempt, and ordinary workers recheck the latest coordinator epoch at launch. Finite tests cover request/preflight/fence binding, isolated roots, failure recovery, command replay, immutable evidence, IR unit-domain binding, and receipt recomputation.
        - [ ] A19d3c2b: make the sole CompletionCoordinator automatically schedule, execute, or resume the latest unfinished project queue and route real verifier diagnostics that cannot be localized to a unit into it. This layer emits only a new RustProjectIR/receipt and hands control to A19e6; it has no promotion or completion authority, and neither low-level APIs nor model output may complete the project directly.
          - [x] A19d3c2b1: connect a host-owned latest-receipt scheduler before Cargo generation. Each step selects only the first actionable item in the canonical queue, while authoritative RustProjectIR values use a generic content-addressed path that can be reopened after restart. Under two concurrent coordinators only the first CAS applier receives model-launch authority; expired unstarted attempts are deterministically recovered and redispatched, started attempts with unknown outcomes require manual reconciliation, and budget exhaustion or orphan candidates fail closed. This kernel only materializes requests and never launches a model or completes a project. Completion is rejected while the latest queue is unfinished, and a duplicate runtime cannot launch the same attempt twice. Finite tests cover concurrency, latest-only selection, leases, budgets, and low-level bypasses.
          - [x] A19d3c2b2: schema v6 adds stable diagnostic lineage/observations, inherited attempt counts across receipts, and hard per-run caps of 64 provider calls and 65 receipt epochs. CompletionCoordinator performs zero-attempt observation and isolated preflight before issuing a host permit bound to the exact receipt, queue, status, version, model, agent, and runtime inputs; each resume launches at most one provider call. Complete provider results enter the ledger before ingest, so an ingest crash resumes by reopening evidence with zero model calls; terminal replay never relaunches, while incomplete or unknown results require manual reconciliation. Public integration and low-level APIs have no dispatch, promotion, or completion authority. Finite tests cover dual executors, stale permits, cross-epoch budgets, preflight blocking, provider-result crash recovery, and terminal replay.
          - [ ] A19d3c2b3: normalize compile/link/init/feature/ABI diagnostics that a dedicated host verifier cannot reliably map to a unit into project diagnostics, bind them to the current cohort, generation, raw observation, and verifier receipt, and admit them to the same queue. Environment or sandbox failures must not impersonate repair diagnostics.
            - [x] A19d3c2b3a: schema v7 adds an immutable content-addressed project-diagnostic intake committed atomically with its host project gate. The Cargo path freezes the current candidate set, reopens the managed generation and canonical RustProjectIR, and partitions only structured rustc `error` diagnostics. A unique unit path enters unit repair; a project `E####` compile error with a repository-relative location binds the cohort, IR/interface, project input, raw observation, and verifier receipt. Warnings, generic failures, more than 64 compiler errors, environment or sandbox blockers, and stale or ambiguous unit paths produce zero intake and zero AI calls. Artifact, ledger, or source-evidence drift is rejected on reopen. Finite gates cover 371 Windows cases with two platform-conditional skips and 371/371 under WSL; no real provider call was made, and this is not `competition-exact`.
            - [x] A19d3c2b3b: validated intakes enter a coordinator receipt v2 compatible with the schema v7 database and share the project-repair queue with static interface diagnostics. The receipt explicitly binds the canonical intake-reference set, set SHA, and verifier-diagnostic SHAs, and verifier-origin diagnostics dispatch first. Request materialization and provider launch reopen the intake, current candidate set, original managed generation, RustProjectIR/interface, and verifier receipt; generation drift recovers with zero provider calls. An AI candidate is publicly only `pending-reverification`; internal `candidate-ready` is not resolved, and neither an ordinary static receipt nor a low-level resolve API can close a verifier-origin item. Only a ledger-authoritative higher-epoch pass from the same gate on a new managed generation can close it, after a content-addressed revalidation receipt reopens raw observation/evidence, the current cohort, candidate IR/interface, and current generation. One transaction then registers the successor, resolves the target, and cancels superseded queue siblings. A failed recheck rolls back the old candidate, registers the successor, and inherits the budget. CompletionCoordinator rejects every historical project-repair obligation outside `resolved/cancelled`. Finite gates run 386 Windows cases with two platform-conditional skips and 386/386 under WSL; only mock providers were used, no real model was called, and this is not `competition-exact`.
            - [ ] A19d3c2b3c: uniquely attribute a complete batch of whole-cohort verifier diagnostics before admitting link/init/feature/ABI repairs from independent evidence producers. Any unclassified, mixed, or environment failure blocks the entire batch. Do not check b3 until b3b and b3c close.
              - [x] A19d3c2b3c1: Cargo check/test now uses two-phase whole-cohort admission. It freezes and analyzes gate order, execution status, complete candidate identity/content ownership, and every error before allowing per-gate recording. Unit ownership is cross-built from canonical RustProjectIR `modules.rust_path/unit_id/candidate_sha256` and the candidate set rather than treating a fixed filename as the sole fact; an unresolved symbol that clearly names another module's entity becomes a project diagnostic. A normalized event SHA prevents same-code/same-file diagnostic identity collisions. Toolchain-sensitive rustc errors, unknown/generic or mixed failures, duplicate owners, stale paths, and sandbox/environment blockers yield zero project/unit repair admission for the entire batch. Project-derived candidate failures recheck the source candidate set in both the verifier-record and retry-state transactions, so cohort drift cannot mutate the new state. Finite gates run 400 Windows cases with two platform-conditional skips and 400/400 under WSL; no provider was called, and this is not `competition-exact`.
              - [x] A19d3c2b3c2: close the in-process evidence chain from raw Cargo output to a whole-cohort classification receipt, and normalize an unresolved project symbol as a link diagnostic only when it binds uniquely to the current RustProjectIR. Missing linker/toolchain/native library, offline dependency, permission, resource, and other environment failures remain blockers with zero intake and zero provider calls.
                - [x] A19d3c2b3c2a: bounded Cargo check/test stdout and stderr are persisted as private content-addressed artifacts that bind gate, stream, SHA, size, and a restricted repository-relative path. Reopening rejects symlinks, escape, size drift, or content drift. Observation v3 binds the classification receipt while v1/v2 remain read-only compatible. The classifier reparses complete diagnostics from the raw references and cross-binds the run, candidate set, project input, RustProjectIR/interface, unit partition, whole-cohort admission, and intake diagnostic SHAs. Raw text never enters model context and grants no semantic pass. This receipt is still issued by the current host process and does not replace the independent A19e7/A19e8b2 verifier-process/capability boundary.
                - [x] A19d3c2b3c2b: the shared linker parser accepts only bounded rustc parents and GNU/lld/MSVC unresolved-symbol forms. Every symbol must map uniquely to one module through the current RustProjectIR public API, global owner, initialization function, or exported FFI link name. Unknown or ambiguous symbols, FFI imports, mixed batches, environment failures, and diagnostic/symbol overflow all yield zero admission for the complete batch. Stage gates pass 425 Windows cases with two platform-conditional skips and 425/425 under WSL. No provider or model was called, and this is not `competition-exact`.
              - [ ] A19d3c2b3c3: add separate host-owned verifiers and independently process-issued raw output/receipts for initialization, feature/cfg, and ABI checks, including paired stale-cohort, raw-output/receipt-tamper, revalidation, and zero-provider-call tests. Do not infer these semantic diagnostics from Cargo text before the A19e7/A19e8b2 raw-evidence boundary closes.
          - [ ] A19d3c2b4: allow `candidate-ready` only to hand off to A19e6's fresh `project-final` cohort/generation. Promotion/completion requires all whole-project gates and invariant audit; no repair API, model response, or stale receipt has that authority.
    - [ ] A19d3d: generate Cargo generations deterministically only from validated RustProjectIR, then verify coverage of every migration unit, dependency edge, public interface, and configuration. IR drift, orphan modules, duplicate symbols, temporary handwritten glue, or a failing real multi-file build blocks promotion.
      - [x] A19d3d1: the production generation API, `integrate-verified`, and candidate quarantine have removed manifest-plus-descriptor write entrypoints and consume only a reopened, conflict-free RustProjectIR; the direct `integrate` CLI is gone. Each generation embeds canonical IR and binds domain/coordinator/interface SHAs; the integration verifier rebuilds from the original artifact root and compares the complete generation byte for byte. Descriptor-only generators remain only in test support.
      - [ ] A19d3d2: extend the current flat library-module projection to verified nested module trees, multiple crates/binaries/examples, complete target/feature/cfg, initialization/destruction, and native-link configuration, then close A19d3 only after real multi-file Cargo build/test and every project-final gate pass.
        - [ ] **A19d3d2a: migrate tests under the target C repository's `tests/` directory as first-class test targets instead of merely translating compile-database entries as ordinary library modules.** The host must discover and content-bind each test target, C test source, runner, fixture/data input, working directory, environment, arguments, expected exit/signal, and timeout from BuildIR plus CMake/CTest/Meson/Make test metadata. RustProjectIR must then project them deterministically into `#[test]`, `tests/*.rs`, or explicit Rust test binaries while preserving target dependencies and execution semantics. Every discovered C test target must map one-to-one to a Rust test target or produce an evidence-bound fail-closed refusal. Acceptance must run both the original C-suite oracle and the Rust suite, compare test inventories and per-test outcomes, and reject zero-test `cargo test`, missing tests, silently removed assertions, or compile-only success. At least two held-out C projects must prove there is no hardcoding of the `tests/` path, test names, or project identity.
          - [x] A19d3d2a1: add an identity-neutral `c2rust-baseline` whole-project execution entrypoint that strictly binds a repository-owned compilation database, runs C2Rust, applies generic generated-code repairs, discovers every translated public `main`, deterministically creates Rust wrappers, preserves original compilation working directories, runs Cargo all-target checks for every package, and executes every wrapper. The same production path passed four pinned projects in WSL local simulation: FlashDB with 7 TUs, 2 wrappers, and 24 original tests, report SHA-256 `48c94540ef05b88763ffeb2477dd2c17623de68a7b1d5e211da3157fc78aa8f2`; cJSON with 27 TUs, 23 wrappers, and CTest 22/22, report SHA-256 `f5ca5c8920104c3f1061f6a6e453450c8184e538a1dfc3004a994c5b15e65885`; libyaml with 22 TUs, 14 wrappers, and CTest 3/3, report SHA-256 `35c265abc83b1a6d6931945b7db2951628a42e5ff67b32c584808eb0cf8f3b0e`; and Lua with 34 TUs and 1 wrapper, report SHA-256 `d5874a59c505c6015a9e797c88ca04e0ce8d8a254c8e503c05a587e30274a446`. Lua drove the identity-neutral structured Rust 1.95 `VaList` API repair. cJSON, libyaml, and Lua all participated in generic rule development and do not count as held-out. An initial Expat `R_2_8_2` held-out probe then exposed and drove the generic working-directory repair for C2Rust's common-ancestor trimming of a repository-internal source root, so the post-repair rerun counts only as a development regression. The default Linux non-fuzzer closure passed Cargo all-targets for 29 TUs, translated checks 4824/4824, and five valid-input CLI/example scenarios; all six Rust binaries avoided linking the original C library. Its formal report remains `blocked` because the low-level runner invokes non-test wrappers without arguments, so the rerun counts as neither held-out nor a formal pass. The complete current TinyCC `mob` source at commit `d9d02c56401e43be43760b63f7d82f771a7ed1f6` passed its full native cross build and official suite, but formal baseline report `2d4a94431f18d245532be61c9103874fdcfc641439daee06280eb51e1512fe15` stopped before Cargo on a same-source multi-configuration blocker and a fatal C2Rust diagnostic. Its AI-first plan verified BuildIR and created 411 migration units plus 1440 assignments, but incomplete link facts and generated-header closure deferred every assignment; neither model nor Cargo launched. TinyCC drove generic hardening, so every rerun is development regression evidence, not held-out evidence or a translated pass. This entrypoint fixes `semantic_gate=false` and numerator zero. It does not yet prove one-to-one C/Rust test targets from generic test metadata or reject every zero-test/assertion-deletion case, so A19d3d2a and A19d3d2 remain open.
          - [ ] A19d3d2a2: project content-bound compilation variants, link commands, dynamic exports/native libraries, test runners, and resource limits from BuildIR into C2Rust/Cargo acceptance. When an unknown project hits a tool-unsupported feature such as computed goto, the system may select only a portable build variant discovered from project-owned metadata and first accepted by the native oracle. Rust must inherit equivalent link semantics and explicit resource settings, while the formal report binds original-C and translated test commands, per-test outcomes, and differences. Acceptance must distinguish "all translated mains compile under Cargo" from "only original build-system-declared tests and validation scenarios are runtime gates", binding each test target's argv, stdin, fixture, working directory, resource limits, and expected exit/output contract from BuildIR. Ordinary CLIs that require arguments must neither fail spuriously under an empty invocation nor claim semantic success without a valid scenario. The Lua stress run proved that the official suite can pass after manually selecting `LUA_USE_JUMPTABLE=0`, adding `--export-dynamic`, and restoring the default stack, while the Expat diagnostic proved that manually supplied XML and benchmark arguments pass. These manual steps do not close this item, and production code must not inspect Lua, Expat, or any project identity.
            - [ ] A19d3d2a2a: when one C source has multiple semantic compilation configurations, generate separate Rust modules/targets/symbol namespaces under content-bound variant keys and connect each link target only to its matching variant. Explicitly refuse when configuration equivalence or symbol-collision freedom cannot be proved; C2Rust must not silently discard duplicate commands.
            - [x] A19d3d2a2b: before Cargo, the C2Rust baseline now checks same-source semantic configurations and reopens hash/size/path-bound transpiler stderr. Even with exit code zero, an ANSI-stripped line-leading `error:` diagnostic blocks as `c2rust_transpile_diagnostics_failed`. Missing, drifting, or escaping stderr blocks as `c2rust_transpile_diagnostics_unavailable`; the gate neither pushes the error into downstream Cargo nor records a partial generated tree as success.
            - [x] A19d3d2a2c: `c2rust-baseline` now accepts an identity-neutral, content-bound declarative execution-scenario contract. The contract must cover every generated wrapper exactly; each wrapper must be marked explicitly as `compile_only` or declare one or more globally unique scenarios that strictly bind argv, UTF-8 stdin, restricted environment variables, a repository-owned working directory, expected exit, and timeout. Both the raw and normalized contracts enter immutable CAS and contribute to run identity. Report schema v2 binds scenario inputs, stdin artifacts, execution plans, actual exit codes, and per-scenario status. Reopening cross-checks every item against the normalized contract and fails closed on contract, input, plan, or CAS drift. Omitting a contract preserves the legacy compatibility route, but explicit-contract mode executes only declared scenarios: an ordinary CLI no longer fails spuriously from an empty invocation, and `compile_only` cannot impersonate semantic success. Focused gates pass 39/39 on both Windows and WSL. The complete `project_migration` regression passes 1019 cases with 11 platform-conditional skips on Windows and 1019 cases with six platform-conditional skips under WSL, with zero failures on either platform. Production code reads no project name, test name, or fixture identity. Expat's original compilation database (36 compile entries and 29 unique C sources) still blocks correctly before Cargo on seven same-source multi-configuration groups, report SHA-256 `09f21f2b6d16ce60367cc8dcb1ec65785eab2f8be2659fb70e3b0b5de1abc588`. A test-only single-variant projection used solely to isolate this sub-capability completed 29 TUs, six wrappers, and six declared scenarios with actual exit code zero throughout, report SHA-256 `dd7f2194702ee1a45dc4601beb2e51753adff2907635a0bbf8cfebea63bbb2f4`. That projection is not a production variant-selection rule, held-out evidence, or an Expat whole-project pass. Both reports fix `semantic_gate=false` and numerator zero, and neither proves one-to-one build-system test inventory, so A19d3d2a2a, A19d3d2a2, A19d3d2a, and their parents remain open.
            - [x] A19d3d2a2d: close identity-neutral, recomputable same-source variant/target projection from BuildIR into the AI ContextPack as a bounded stage capability. Every `unit_id` must bind exactly one object target, and variant index/count must cover the complete same-source set. Target ownership follows the transitive `object -> archive/link -> downstream link` dependency closure; missing dependencies, duplicate object owners, output mismatches, orphan targets, cardinality drift, and reachable cycles all fail closed. CIndex strictly revalidates this context. ContextPack deduplicates shared target facts and binds a separate target-membership fact for each unit, preventing arbitrary selection among configurations of one source. Link closure also generically accepts existing, content-bound versioned `.so.N` and nontraditional-suffix inputs. Directory evidence permits only relative, non-dangling, acyclic links that resolve inside the bound root, while file bindings, absolute links, and escapes remain rejected. The original Expat `R_2_8_2` development regression now reaches ready discovery/closure/link and verified closure under WSL with 36 BuildIR units, 43 object/link targets, seven same-source two-variant groups, and zero blockers. Focused regression passes 62 tests on Windows with nine link-permission conditional skips and 62/62 under WSL. The stage-wide suite passes 1,036 tests on Windows with 20 platform-conditional skips and 1,036 tests under WSL with six platform-conditional skips. Every added or modified Python file stays at or below 300 lines, and production code reads no project, target, or test identity. This stage proves only discovery, closure, BuildIR, and model-input ownership: it creates no target-scoped Rust namespace and runs no C2Rust, Cargo, or test oracle. Therefore A19d3d2a2a and every parent remain open, with `semantic_gate=false` and numerator zero.
            - [x] A19d3d2a2e: make external function/global visibility in the migration graph BuildIR target-domain aware while preserving conservative behavior when no link evidence exists. Symbols are isolated by intersecting `object -> consumer` domains only when real archive/link consumers are present; object-only compilation facts do not invent final-program boundaries and retain cross-TU resolution. Equal definitions in disjoint targets no longer create false ambiguity, while definitions that converge on one target still fail closed. CIndex also detects arbitrary function-like macro-generated definitions from content-bound source and deduplicated header snapshots, keeps their source in context, and emits an explicit parser boundary instead of a fake ordinary-function node. The original Expat `R_2_8_2` competition-profile development regression now produces a complete plan with 36 BuildIR units, 43 targets, seven same-source two-variant groups, 489 function nodes, and 407 SCCs; `ambiguous_external_definition` falls from 130 SCCs in the previous stage to zero, while all 155 remaining parser blockers retain classified evidence. Neither the model nor Cargo ran, so `semantic_gate=false` and numerator remain zero. Focused gates pass 35/35 on Windows and WSL. The stage-wide suite passes 1,039 tests with 20 platform-conditional skips on Windows and 1,039 tests with six skips under WSL. Every touched production/test Python file stays at or below 300 lines. This item closes only target-scoped C-graph/model-input disambiguation, not A19d3d2a2a or any whole-project translation parent.
            - [x] A19d3d2a2f: add a BuildIR-bound C2Rust schema-v3 path. Reopen the complete BuildIR/compile-database binding before launching any process, then give every `unit_id` an isolated one-entry database, output root, and immutable generated snapshot so same-source variants can never overwrite one another through `--overwrite-existing`. Build a terminal-target plan recursively from original BuildIR `ordered_inputs`, preserving archive layers, member order, duplicate occurrences, link arguments, native/system boundaries, and unit fan-out. Equal exports may coexist across targets; duplicate global definitions inside one target closure require an evidence-bound refusal and must never select a winner by path order. Even when this slice passes, the whole baseline remains blocked as `c2rust_target_cargo_assembly_pending`; per-unit transpile success cannot impersonate Cargo or semantic acceptance.
              - [x] A19d3d2a2f1: complete schema-v3 reopening of the full BuildIR/compile-database binding, recomputation of entry indexes, hashes, and parsed facts, safe unit keys, one-entry per-unit databases, isolated fresh outputs, immutable generated snapshots, and the terminal-target plan. The plan recursively preserves original target occurrences, archive semantics, duplicate members, link arguments, object ownership, unit fan-out, and target-owned external/native boundaries; missing owners, unknown dependencies, cycles, output conflicts, and unrepresentable order fail closed. The focused C2Rust baseline suite passes 55/55 on both Windows and WSL, the source-layout gate passes, and every added or modified Python file stays within 300 lines. The real Expat `R_2_8_2` development regression executed 36/36 real C2Rust processes and produced 36/36 isolated snapshots under WSL from all 36 original compile records, covering 29 unique C sources, six terminal targets, and 99 unit occurrences. Current reopenable report SHA-256 `515576d6e134e0cb333a2af72b87988b2a29223b6e69b2ce539d8017c2848457` has only `c2rust_target_cargo_assembly_pending`. This evidence ran no AI, Cargo, or project tests; `semantic_gate=false` and numerator zero remain fixed, so it is not an Expat whole-project pass.
              - [x] A19d3d2a2f2: complete the target-scoped export inventory and reopen gate. Runtime parses external functions/globals from each unit's content-bound generated snapshot, aggregates them by terminal-target occurrence closure, and writes immutable CAS. Report reopen rereads the target plan, snapshot manifests, and every generated-file CAS, recomputes each unit, and compares the inventory byte for byte. Equal exports may coexist in different terminal targets; duplicate definitions inside one closure, whether across units or across files in one unit, emit stable `c2rust_target_export_conflict` evidence and refuse before Cargo without selecting a winner by source path or module order. The focused suite passes 55/55 on Windows and WSL. The real Expat reopen yields 380 unit-level exports, 764 target-occurrence exports, zero conflicts, and export-inventory CAS SHA-256 `3f04656e6dbaf6edb116a182f589b201cb0196c7fccd2aa69cbfc7891ceb4c2f`. This gate proves only export-conflict classification; it runs no Cargo and does not raise the semantic numerator.
            - [ ] A19d3d2a2g: carry the SCC-to-BuildIR identity bridge through the migration DAG and integration manifest, binding each group to `source_unit_ids` and object/owner/reachable target scope and recomputing that scope from BuildIR on reopen. Add a backward-compatible RustProjectIR v3 with first-class `workspace -> packages -> targets -> modules` topology. Derive module identity from both target namespace and candidate identity so equal source content may belong to distinct variants. Reconstruct a multi-package Cargo workspace from archive/link product kinds, then require `cargo metadata --locked --offline`, actual compiled targets, and linker evidence to exactly match the package/target set. Missing product type, input-order equivalence, native consumer, unique `main`, or namespace-collision proof must block instead of falling back to the flat single crate.
              - [x] A19d3d2a2g1: complete the host-derived `scc-build-target-scope-v1`. The migration graph derives deduplicated `source_unit_ids` from SCC node membership, then binds every unit's variant, unique object owner, complete reachable archive/link set, terminal target set, and shared target domain. The portfolio DAG includes the scope in group content SHA; the integration manifest binds the final ledger group SHA, original migration-graph artifact, and each scope SHA without copying full scopes. The run contract cross-checks ledger groups, while the RustProjectIR reopener reopens the canonical migration graph and every BuildIR and recomputes every SCC byte for byte. A cohort DAG may only slice scope bindings exactly from its parent manifest. Unknown/duplicate nodes or units, owner conflicts, target cycles, disjoint domains, or group/graph/order/scope/BuildIR drift fail closed. Legacy manifests without scope remain v2-compatible only and gain no v3 authority. Finite stage evidence: all 1,065 `test_project_migration*.py` tests pass on Windows with 20 platform-conditional skips; the same 1,065 pass under WSL with six skips; documentation mirror and 300-line source-layout gates pass 5/5. The original Expat `R_2_8_2` competition-profile development regression regenerates 36 BuildIR units, 407 SCC scopes (404 `target-bound` and three `shared-target-domain`), and six terminal targets. Scope-set SHA-256 is `d0aa78cf98106490f1c1eccb35b825cea8d3919238391126d33d6b18435d78e8`; integration-manifest SHA-256 is `64e3b5c35dd4d0981fe7924570cef0c002f46e0bcd5d35ba996763bdb0e008fa`. Hash-only scope binding reduces that manifest from 749,984 bytes to 138,749 bytes. This run proves planning and identity reopening only: no model, Cargo, or test oracle ran, so `semantic_gate=false` and numerator zero remain fixed. g2-g4, g, and every whole-project parent remain open.
              - [x] A19d3d2a2g2: complete a backward-compatible, fail-closed RustProjectIR v3. Generic validation and reopening dispatch only on `schema_version`, leaving the v2 single-crate contract unchanged. V3 makes `workspace -> packages -> targets -> modules` authoritative and carries no duplicate `crate` authority. The host classifies static libraries, executables, and shared libraries only from BuildIR `archive`, ordinary linker-driver `link`, `-shared/--shared`, or `/DLL` facts; it never reads project names, output extensions, or test identities. `-r/--relocatable`, object-only closure, unknown products, missing or disjoint scopes, mixed same-source variants, and namespace collisions become structured blockers. Module identity hashes the target namespace, SCC group, candidate SHA, and complete source-unit variant scope, so equal candidate content receives distinct identities in different target namespaces when closure evidence permits it. Targets preserve repeated ordered BuildIR input occurrences and link arguments, while package dependencies derive only from proven non-object target edges. The v3 reopener reopens the DAG, explicit migration graph, every BuildIR, candidate source, and compilation-facts binding; it recomputes scopes and the complete workspace/package/target/module projection from BuildIR, rejecting artifact-SHA, product-set, module-scope, input-order, or topology drift field for field. Candidate descriptor SHA is also recomputed from actual source bytes before IR creation. Every added or split Python file remains at or below 300 lines. Stage evidence: all 1,094 `test_project_migration*.py` tests pass on Windows with 20 platform-conditional skips; the same 1,094 pass under WSL with six skips; disk-backed v3 ready/blocked binding tests pass 6/6. Replaying the real Expat `R_2_8_2` 36-unit/407-SCC BuildIR and scopes, with candidate references only and no translation semantics, yields seven packages/targets (one shared library and six executables), 407 target-scoped modules, and zero blockers. Topology SHA-256 is `4452878618cf2538afbd7e3d56851d895c366c24dbdebf26a3e0f7c4a2358012`. This stage launched no model, Cargo, or test oracle. The production Cargo path remains on v2 until g3 closes; `semantic_gate=false` and numerator zero remain fixed, while g3-g4, g, and every whole-project parent stay open.
               - [x] A19d3d2a2g3: deterministically render a multi-package Cargo workspace from v3 while preserving archive/link product kinds, dependency direction, ordered input occurrences, entrypoints, crate types, and target-owned export namespaces. Unique `main`, collision-free crate/module names, and representable native/system link arguments fail closed when unproved; the generator never falls back to the flat single crate. The production generation domain selects v3 for complete target-scoped competition inputs while development retains v2 compatibility only. Staticlib, cdylib, bin, and two-package workspaces have deterministic reopen regressions, and the two-package artifact passes real locked/offline Cargo metadata and all-target check. The independent production verifier and linker witness remain in g4, so this closes generation only, not structural or semantic final acceptance.
              - [ ] A19d3d2a2g4: add an independent reopen gate that runs `cargo metadata --locked --offline`, all-target compilation, and linker-trace/ELF/archive reopening. Metadata packages/targets, actual rustc targets, ordered link inputs, and RustProjectIR v3 must match exactly. Passing this structural gate still cannot raise the semantic numerator before ProjectTestInventory, C/Rust oracle diff, and project-final pass.
             - [ ] A19d3d2a2h: add `ProjectTestInventory v1` plus `project-test-mapping` and `project-test-oracle-diff`. CTest may consume only content-bound `ctest --show-only=json-v1`, and Meson consumes `intro-tests.json`. Complete Make support accepts only Automake `TESTS/check_PROGRAMS`, a project-owned explicit manifest, or a host-verified explicit dry-run target; target selection must bind build metadata or a host-verified AI proposal rather than guess from a name alone. Kconfig/selftests and other systems need independent adapter evidence. Never infer the test set from a `tests/` path, a `test/check` name, or project identity. Every source test identity must map uniquely to a Rust target/selector and run both C and Rust under the same normalized argv/stdin/env/cwd/fixture/resource contract. Missing, extra, merged, zero-test, compile-only, silent-skip, or exit/signal/stdout/stderr differences block Q4-Q8.
               - [x] A19d3d2a2h1: complete the first production CMake/CTest adapter. It runs only sandboxed `ctest --show-only=json-v1` and binds command[0] exactly to one materialized BuildIR link target. Completion recollects CTest, reopens BuildIR and the inventory, and rederives every record; added, removed, property, or executable-content drift fails closed. Every inventory-declared test executable must map uniquely to a RustProjectIR target. Ordinary application and CLI executables are compiled by the all-target Cargo gate but are no longer misclassified as tests or blocked merely because they are absent from the test inventory. Unsupported verdict-changing CTest properties are never guessed.
               - [x] A19d3d2a2h2: complete the direct-executable dual-sandbox oracle. argv and environment use structured literal, repo-path, or path-template values. The host creates one minimal read-only input snapshot whose directories, files, permissions, exclusion policy, and explicit inputs are hashed. C and Rust use independent bubblewrap runtimes; Rust cannot see the C executable, source, state, or output, and the trusted host compares results. The C baseline must exit zero, and exit, signal, stdout, stderr, timeout, or oversized-output differences cannot pass.
               - [x] A19d3d2a2h3: attribute project-logic mismatches through test, source target, Rust target/module, and unit identities and atomically return them to AI repair. `run-to-completion` dispatches repairer and reviewer, reruns candidate compile/oracle/negative/unsafe-alias/ABI/final/promotion, then returns to the project oracle. The full oracle is content-addressed; the host gate and completion receipt bind its SHA, and finalization reopens inventory, mapping, oracle, raw observation, gate summary, and SQLite record. Drift cannot publish completed.
               - [x] A19d3d2a2h4a: complete a bounded, identity-neutral compatibility inventory for an explicit Make `test` recipe. The host runs only fixed `make -n --no-builtin-rules --no-builtin-variables test` inside a read-only, networkless, RLIMIT-constrained Linux bubblewrap sandbox; no recipe executes. Output must be direct argv without pipes, redirections, substitutions, or shell control. Only fixed `echo`/`printf` presentation commands may be ignored; Python, custom runners, compile commands, and every other unmapped command fail closed. A test executable must bind exactly to a materialized BuildIR link output. The inventory adapter, model-safe test contract, completion-time recollection, and field-for-field rederivation share the same `make-dry-run-v1` identity. Focused Windows tests pass 28/28, while all 1,184 Windows tests pass with 25 platform-conditional skips. WSL competition-profile/live tests pass 22/22, the CTest-environment repair set passes 16/16, and sandbox-argv tests pass 4/4. After a native build of a temporary copy of pinned FlashDB, real bubblewrap collection yields two tests, two target bindings, and zero blockers. One full WSL run under the original profile remains blocked in two Cargo setup classes because the Huawei sparse registry returned HTTP 502. The same seven tests pass with the existing global offline cache, but that does not count as a competition-profile pass. Production code reads no project name, test-binary name, or fixed path. This compatibility layer itself still selects a fixed `test` convention. h4b adds Automake, but arbitrary AI/host-selected targets, recursive Make, and custom runners remain open, so h4, Q4-Q8, and whole-project semantic acceptance remain unchecked.
               - [x] A19d3d2a2h4b: complete content-bound Automake target selection. A bounded scan binds every `Makefile.am` path/SHA/size and actual `TESTS/check_PROGRAMS` declaration. A root manifest plus any test declaration selects fixed `make -n --no-builtin-rules --no-builtin-variables check`; otherwise h4a's fixed `test` remains. Completion rederives the target binding and manifest drift fails closed. Mixed CMake/Make repositories select a unique adapter only from BuildIR raw provenance, output paths, and a compile database whose SHA/size matches BuildIR metadata; missing or dual evidence blocks. The Windows related group passes 48 tests with one Linux-only skip, WSL passes 20/20, and a real bubblewrap `make check` inventory passes. Production reads no project or test identity. Arbitrary targets, recursive Make, and custom runners remain open.
               - [x] A19d3d2a2h4c: complete a bounded, identity-neutral Meson project-test inventory. The host runs only fixed `meson introspect --tests` inside a read-only, networkless, resource-bounded Linux bubblewrap sandbox and binds the compile database, Meson build-file snapshot, `intro-tests.json`, tool identity, and sandbox identity. It accepts only direct-executable tests that map uniquely to materialized BuildIR link targets. Zero tests, stale `meson-info` metadata, and unsupported protocols, runners, fixtures, or resource contracts fail closed. Completion recollects and reopens every dependency; production reads no project name, test name, or fixed path. The focused Windows Meson group passes 18 tests with one POSIX-conditional skip. This slice does not prove arbitrary Meson runners or whole-project semantics.
               - [x] A19d3d2a2h4d: complete a bounded single-file stdin contract for direct Make test commands. It accepts exactly one whitespace-delimited trailing `< repo-file`; the input must be a non-link regular file inside the repository and no larger than 1 MiB, and it enters the minimal read-only input snapshot. The model-safe contract advances to v2 and exposes only the `repo-path` shape and path, never contents; legacy v1 contracts without stdin still reopen. The C baseline and Rust replay receive identical raw bytes in independent bubblewrap sandboxes. Runtime invocations bind stdin SHA-256/size and snapshot SHA, while persisted oracle-evidence v3 stores recomputable invocation commitments that hash argument, environment, and cwd values instead of exposing them. Reopening cross-checks both commitments, snapshot file SHA/size, and stdin for every case; legacy v2 evidence remains compatible only when the inventory has no stdin. Equal stdout with different stdin or invocation commitments cannot pass. Unquoted `~`, pipes, output redirection, substitution, repeated or attached `<`, and here-docs remain fail closed; `/workspace` conversion is guest-root anchored and the 128-argument limit matches the executor. Final focused Windows tests pass 29/29; the project-test group passes 80 tests with six platform-conditional skips; all 1,261 Windows project-migration tests pass with 26 platform-conditional skips. The WSL full suite passes 1,249 tests with 12 explicit environment-conditional skips, and the real binary-stdin C/Rust dual-sandbox oracle passes 1/1. Every gate has zero failures. Two independent read-only reviews confirmed that the initial six risks and the final invocation-reopen reproduction are closed. Production reads no project or test identity. This slice does not prove runner input, custom CTest/Meson stdin, or complete fixture/resource support.
               - [ ] A19d3d2a2h4: add arbitrary explicit targets, recursive Make, Kconfig, and custom-runner adapters; remaining stdin sources; declared and implicit fixtures; full resource contracts; multi-path templates; zero-test, omitted-test, and extra-Rust-test proof; and an adversarial process policy. Then obtain a one-to-one complete inventory pass through the competition entrypoint on a real multi-target C project that did not participate in rule development. Current support covers CMake/CTest direct executables, h4a's bounded Make `test`, h4b's content-bound Automake `check`, h4c's bounded Meson direct-executable inventory, and h4d's bounded single-file Make stdin; h, Q4-Q8, and every whole-project parent remain unchecked.

  **A19e project-level verification and precise repair**

  - [x] Candidate Cargo commands may run only inside a networkless Linux bubblewrap sandbox. Actual `cargo/rustc/rustdoc` binaries are resolved through `rustup which`; the bounded toolchain tree is fully content-hashed, rehashed before execution, and mounted read-only. The earlier WSL smoke returned `bubblewrap_unavailable` and executed no candidate; `--offline` was not presented as a sandbox. The later `bwrap` installation has so far closed only the Clang fact live lane and does not prove that the Cargo candidate smoke was rerun.
  - [x] Complete fixed host authorities, content-addressed evidence, latest gate epochs, immutable candidate sets, Schema DDL fingerprints, and current last-good recomputation. The CLI removes caller-controlled pass/verifier/candidate-set authority parameters.
  - [x] Add host-owned integration and Cargo check/test adapters and route bounded Cargo JSON module diagnostics back to the matching candidate compile gate for repair. The earlier WSL smoke lacked `bwrap`, so it executed no candidate and granted no pass; the later Clang fact live lane does not change that historical Cargo evidence.
  - [ ] **A19e4: close positive candidate verification, quarantine, and completion.**
    - [x] **A19e4a: make the run contract and DAG cohort nonreplaceable.**
      - [x] A19e4a1: generate the integration manifest before ledger creation and bind its path/SHA/size, complete dependency edges, and DAG SHA into the run metadata/schema. CLI and workers no longer accept a replaceable manifest.
      - [x] A19e4a2: compute transitive closure only from current active roots, their latest completed Rust candidates, and valid last-good dependencies, then bind it as an immutable verification candidate set. Missing members, non-latest candidates, incomplete dependencies, unrelated members, or cohort drift fail closed.
      - [x] A19e4a3: distinguish `wave-provisional` from `project-final` cohorts. Before completion, run a final revalidation barrier over the complete last-good set so early-wave gates cannot impersonate final whole-project gates.
    - [x] A19e4b: generate an isolated immutable quarantine Cargo generation from that set. It must not modify `CURRENT`/last-good and may run `cargo check/test` only in a proven networkless OS sandbox.
    - [x] **A19e4c: make the host compile decision nonforgeable.**
      - [x] A19e4c1: derive candidate compile pass/fail from real execution in a fixed host adapter. CLI/API callers cannot supply pass, verifier ids, candidate-set ids, manifests, or arbitrary commands.
      - [x] A19e4c2: bind compile evidence to candidate source, integration manifest, quarantine manifest/generation state, verification candidate set, fixed Cargo command, toolchain, and sandbox contract; final verification and promotion reopen every binding and reject any drift.
    - [x] A19e4d: route module-local Rust diagnostics to the matching repairer. Sandbox, toolchain, timeout, and resource failures remain blocked/deferred and never become semantic candidate failures. Unattributed project errors enter project-level repair rather than arbitrary unit attribution.
    - [ ] A19e4e: add dedicated host-owned runners and raw-observation schemas for C-oracle/Rust-replay/diff, negative, unsafe/alias, ABI, and candidate final gates. The low-level generic ledger API cannot insert caller-constructed passes. Re-derive the same verification context before promotion and reject stale gates, source, generation, or cohort state.
      - [x] A19e4e1: four candidate semantic-runner classes have fixed plans, strict raw schemas, caller-authority rejection, and stale-context reopening. Unsupported signatures, missing original-compiler facts, or unavailable backends fail closed and grant no pass.
      - [ ] A19e4e2: connect executable oracle/replay/diff, negative, unsafe/alias, and ABI backends, backed by A19e7/A19e8 receipts for promotion and `project-final` revalidation.
        - [x] A19e4e2a: the dependency-closed scalar-function lane executes the C oracle, Rust replay, diff, `forbid(unsafe_code)`, and ABI checks inside Linux bubblewrap. After the candidate is fixed, the host generates 32 held-out behavior cases; the negative gate compiles and runs a first-result `wrapping_add(1)` mutant and rejects the old six-sample lookup candidate. The C-oracle driver must also match the original BuildIR compiler basename, binary SHA, and size carried by CIndex. Four live WSL tests passed in 298.420 seconds. This slice does not cover dependent functions, pointer/record behavior, project tests, or A19e7 independent-process receipts, so A19e4e2 remains open.
          - [x] A19e4e2a1: upgrade the former mostly `[-4095,8191]` random scalar stimuli with source-bound boundaries. The host performs a bounded extraction of function integer constants outside comments, strings, characters, and floats; it prioritizes each threshold and its `-1/+1` neighbors, then type-safe powers and nonce-filled cases. Signed type extrema are enabled only when compile facts explicitly contain `-fwrapv`, avoiding false Rust failures from C signed-overflow UB; unsigned still covers wrapping boundaries. Unit tests cover `x == 100000` and lexical filtering. A real WSL four-gate run first exposed the extrema-UB false failure; after correction, the same live test passed in 258.737 seconds. This subitem itself does not parse macro constants or prove every input UB-free.
          - [x] A19e4e2a2: add evidence-bound object-like integer macros from source, CIndex, headers, and compile options to the same scalar-stimulus boundary set. The parser accepts only bounded integer expressions and resolved macro dependencies without `eval`; it supports `-DNAME`, `-D NAME=value`, and `-U`, while cycles, conflicting redefinitions, conditional definitions, function-like macros, and complex expressions fail closed. Macro thresholds and their `-1/+1` neighbors still obey type ranges and the `-fwrapv` rule. The focused Windows group passes 36 tests with two platform-conditional skips. This bounded producer is not a complete preprocessor, cfg/type/layout solver, or proof that every input is UB-free.
    - [ ] A19e4f: let the CLI completion command orchestrate only fixed adapters, then run integration, Cargo, project oracle/negative/unsafe-ABI, and project final after every unit is last-good. Model JSON, test-fabricated ledger rows, or compile-only success cannot complete a project.
  - [ ] **A19e5: single state-transition authority, declarative FSM, and invariant audit.**
    - [ ] A19e5a: declare unit/run states, legal commands, preconditions, events, and post-states in one FSM. Only `TransitionAuthority` holds the database capability to update `migration_units` and other semantic projections; lease, worker, verifier, repair, promotion, project-gate, and CLI modules may submit typed commands only and must not issue direct `UPDATE`s or bypass the transition table.
      - [x] A19e5a1: centralize `migration_units`/`project_runs` projection in `TransitionAuthority`, add typed unit/run commands with idempotent command-id/evidence binding, and statically reject direct updates to those projections from other production modules.
      - [ ] A19e5a2: add command-kind preconditions, monotonic state versions, and database-capability isolation. Prevent arbitrary legal edges from impersonating completion commands, forbid transition history deletion/mutation, and place candidate/project pass semantic tables under the same sole authority.
        - [x] A19e5a2a: ledger schema v4 adds immutable initial state and monotonic `state_version` to runs and units. Every production transition uses a registered command-kind factory whose kind fixes edge, reason, attempt, and last-good permissions, so the wrong kind cannot complete a unit or project through an otherwise legal state edge.
        - [ ] A19e5a2b: isolate database write capabilities and place candidate/project pass semantic tables under the same sole authority, so lower-level modules cannot hold connections capable of directly rewriting semantic projections.
    - [ ] A19e5b: atomically append an immutable event before projecting each legal transition with expected state/version, idempotent command id, and bound evidence. Concurrency, stale epochs, duplicate replay, partial transactions, and crash recovery must not create two active attempts, state regression, or ghost passes.
      - [x] A19e5b1: unit/run transitions append an explicit immutable event and CAS the projection by expected state/version in one transaction. Triggers reject event `UPDATE`/`DELETE`; every transition and completion path replays initial state through events and checks current state/version/last-good; prelaunch cancellation restores only from the immutable start event and preserves a cancelled attempt/event without consuming retry budget.
      - [ ] A19e5b2: bring assignment, attempt, lease, candidate, and project-gate semantic projections into the same replayable event system, with systematic fault injection for concurrency, partial transactions, and crash points.
    - [ ] A19e5c: run the same host-owned invariant audit before candidate promotion, `project-final`, and completed. At minimum, recompute the DAG/BuildIR/RustProjectIR, active-attempt uniqueness, latest gate epoch, candidate/generation/cohort bindings, all-unit last-good state, project-final freshness, and project-gate completeness. Any unknown or inconsistent fact fails closed.
    - [ ] A19e5d: use static scans, database-capability tests, illegal-transition-table tests, and fault-injection/replay tests to prove that production modules cannot mutate semantic state directly and that every interruption resumes to a deterministically recomputable event sequence and projection.
  - [ ] **A19e6: sole CompletionCoordinator and mandatory project-final completion chain.**
    - [ ] A19e6a: retain one `migrate/resume` entrypoint that can change the whole-project semantic lifecycle. Its CompletionCoordinator must execute discover/BuildIR, plan/DAG, dispatch/repair, promotion, RustProjectIR/Cargo generation, `project-final` revalidation, project gates, and complete in a fixed order; no other entrypoint may skip steps or select gates.
      - [x] A19e6a1: add root `AGENTS.md` and the canonical outer `project_migration_harness.py migrate` entrypoint. It uses competition/required defaults, creates a hash-bound plan, and then enters the existing `run-to-completion`; every non-completed result exits nonzero. Internal `generate-candidate` and `execute-command` worker modes take precedence to prevent recursive outer orchestration. Lower-level write authority is not fully removed, so A19e6a remains open.
    - [ ] A19e6b: after every unit reaches last-good, require the Coordinator to create a fresh complete `project-final` cohort and quarantine generation, then rerun compile, oracle/replay/diff, negative, unsafe/alias, ABI, integration, Cargo, and project final on that same cohort. Passes from `wave-provisional` or an old generation can never become completion evidence.
    - [ ] A19e6c: low-level debug/admin CLIs may only read evidence, explain plans, or diagnose against temporary copies disconnected from the authoritative ledger. They must not write semantic tables, insert passes, promote candidates, move `CURRENT`, or set completed; CLI/API authority tests must prove this boundary.
    - [ ] A19e6d: write idempotent checkpoints and a hash-bound completion receipt at every Coordinator stage. Resume must derive its next action from the FSM/events, and the final receipt must bind the state sequence, project-final candidate set/generation, every verifier receipt, project gates, and invariant-audit result.
      - [x] A19e6d1: candidate promotion and the final `project-final` completion barrier now share a provider-execution reopener. A model-started attempt requires exactly one execution artifact, and the host revalidates the content-bound request, preflight, model, session, invocation receipt, raw response, and parsed response. Missing, ambiguous, or drifted evidence after promotion cannot publish completed. This does not replace two-phase completion-receipt publication or crash recovery.
      - [x] A19e6d2: each OpenCode provider process uses a separate process group. Timeout or a ledger-start callback failure terminates the whole descendant tree and waits within a bound. stdout/stderr first go to temporary files and the host reads only each limit plus one byte, so an oversized response is not fully materialized in memory. The Windows focused group passes 14 tests with one POSIX-only skip, and the related WSL competition-environment group passes 17/17. This is not aggregate cgroup resource proof.
      - [x] A19e6d3: make completion receipt publication crash consistent. The host atomically writes canonical receipt bytes, fsyncs the file and supported parent directories, and reopens them before one SQLite transaction binds every unit/run completion transition evidence SHA to that receipt and commits completed. Persistence failure never calls completion, transaction interruption rolls back every completion projection, and a receipt-first orphan is idempotently reusable after restart. Reads of a completed run and coordinator recovery reopen the receipt, BuildIR references, project-final gate bundle, project-test evidence, and transition set; missing, forged, or drifted evidence can only block. The final Windows project-migration suite passes 1,208 tests with 25 platform skips, and the selected cross-platform WSL competition group passes 75/75. The closeout also removes the mixed adapter's private Make-module dependency and routes legacy provider behavior tests through the new controlled process boundary. A `finalizing` epoch and two-phase `CURRENT` publication remain under A19e6e.
    - [ ] A19e6e: complete monotonic `completion_epoch`, `active -> finalizing -> completed`, and recoverable two-phase `CURRENT` publication. Reject new leases and attempts while finalizing, and publish the verified receipt and `CURRENT` only after every gate and invariant audit passes on one cohort/generation. Failure or crash must never move last-good early.
      - [x] A19e6e1: complete the database-side finalizing epoch and completion-receipt binding. Schema v9 adds an independent run `completion_status`, a monotonic epoch, and cohort, generation, gate-bundle, invariant, and receipt SHA bindings. Upgraded v8 ledgers retain historical `active -> completed` replay only; every new completion command must use `active -> finalizing -> completed`. One transaction entering finalizing reopens the project-final cohort, managed generation, all project gates, and the quiescent invariant. Application checks and database triggers then reject assignments, leases, attempts, context refresh, candidate promotion, project repair, project gates, and provider starts. Crash recovery reuses the same epoch, receipt v2 binds the frozen context, and one final transaction binds every unit/run transition to the receipt SHA; drift or persistence failure can only remain finalizing/blocked. The merged Windows focused group passes 88/88 and every production module remains at most 300 lines. Existing `CURRENT` is not yet bound to epoch/cohort/generation/receipt and has no crash-safe prepare/commit pointer protocol, so A19e6e remains unchecked.
  - [ ] **A19e7: process-level capability boundary for host verifiers.**
    - [ ] A19e7a: run semantic verifiers in separate least-privilege host processes that receive only read-only inputs, dedicated sandbox-execution capability, and authority to submit verifier receipts to TransitionAuthority. AI workers, ordinary CLIs, test fixtures, and the orchestration process must not hold any capability that produces passes or writes semantic projections.
    - [ ] A19e7b: derive pass only after the verifier process actually executes a fixed VerificationPlan, binding the process-launch contract, input snapshot, command/toolchain, sandbox receipt, raw output, and exit state. A valid schema, caller observation, model JSON, zero exit code, or forgeable verifier id is never sufficient by itself.
      - [x] A19e7b1: add a strict non-authoritative envelope/raw-receipt foundation for separate initialization, feature/cfg, and ABI gates. It binds run capability, issuer/nonce, run/cohort/generation, RustProjectIR/interface, VerificationPlan, toolchain, sandbox, fixed argv, raw stdout/stderr, and exit state. Raw evidence is content-addressed, bounded, and reopenable; the current contract records only `failed/blocked` with `semantic_gate=false` and numerator zero.
      - [ ] A19e7b2: place this contract in a truly separate verifier process, issue results over a one-use capability channel, and consume each nonce atomically so that only real execution can derive pass. The current schema bindings are not authentication and cannot submit to TransitionAuthority or promote semantic state.
    - [ ] A19e7c: allow TransitionAuthority to accept only a recomputable, unused verifier receipt returned over the current run's capability channel, and make it reopen every content hash and current cohort/context. Missing, replayed, cross-run, crashed, channel-forged, source/plan-drifted, or nonexecuted observations become blocked/failure records without semantic promotion.
    - [ ] A19e7d: add hostile-caller, forged-schema/pass, raw-observation replacement, receipt replay, verifier kill/timeout, and concurrent-race tests proving that only the current real host-runner result can pass through the state-transition authority.
  - [ ] **A19e8: non-degrading SandboxBackend capability interface.**
    - [ ] A19e8a: define an implementation-independent SandboxBackend capability contract requiring at least network isolation, read-only repository/candidate inputs, a separate writable output, process/child-process containment, an environment allowlist, timeouts, CPU/memory/file/process-count limits, a read-only bound toolchain, and post-exit cleanup. Each VerificationPlan explicitly declares every required capability.
      - [x] A19e8a1: add an implementation-independent strict capability set, environment allowlist, resource bounds, and canonical `VerificationPlan`. The plan fixes purpose/argv/input/timeout/requirements SHA, and rejects shell/control syntax, oversized input, or any missing capability before execution.
      - [ ] A19e8a2: add cgroup v2 or competition-environment-equivalent aggregate limits for memory, CPU, and the process tree with real-host proof. Current rlimits prove inherited per-process bounds only and must not be presented as aggregate process-tree quotas.
    - [ ] A19e8b: require every bubblewrap or equivalent backend to run a host-owned capability probe/conformance suite before candidate execution and record backend identity/version, the actual isolation mechanism, mount/process/resource policy, and probe hash in a sandbox receipt. Command-line flags, self-reported capabilities, and `--offline` do not prove isolation.
      - [x] A19e8b1: make bubblewrap discovery run a fixed host probe before returning a backend. It checks user/pid/mount/network namespaces, zero capabilities, read-only project/toolchain mounts, isolated HOME/tmp/runtime, the environment allowlist, rlimits, and exit cleanup, then records backend version, contract/requirements/probe/raw-observation hashes, and per-capability results in the receipt.
      - [ ] A19e8b2: persist probe raw files, launcher/toolchain manifests, and execution stdout/stderr as content-addressed references issued by an independent verifier process. The current in-process receipt proves canonical bindings only and does not replace the unforgeable A19e7 capability channel.
    - [ ] A19e8c: require all equivalent backends to satisfy the same minimum guarantees and adversarial tests. Platform, privilege, missing-tool, or backend failures must never automatically reduce capabilities, use a normal subprocess, enable network/host writes, or loosen resource limits. Any missing required capability fails closed as an environment block before candidate launch.
      - [x] A19e8c1: make non-Linux hosts and missing/failed `bwrap`, toolchain, or probes return fixed environment blockers with zero candidate execution and no subprocess fallback. Windows passes 288 tests with two conditional skips, WSL passes 288/288, and a real WSL smoke without `bwrap` returns `bubblewrap_unavailable` with `cargo_executed=false`.
      - [ ] A19e8c2: add at least one non-bubblewrap equivalent backend and require every implementation to pass the same real-host conformance/adversarial suite without backend-identity dispatch. Cross-platform equivalent isolation remains unproved until then.
    - [ ] A19e8d: require compile, semantic runners, final/promotion, and held-out revalidation to reopen the same sandbox receipt and recompute backend/plan/toolchain/input bindings. Backend drift, stale probes, cleanup failures, or nonequivalent guarantees reject pass and completion.
      - [x] A19e8d1: make candidate compile and project Cargo raw observations share one strict reopener that recomputes contract/probe/plan/command-start/input/cleanup bindings. Project final/completion reopen the raw observation and require check/test inputs to equal the latest integration managed-generation manifest SHA. The legacy four-field Cargo observation and probe/plan/input/cleanup drift cannot grant pass.
      - [ ] A19e8d2: make oracle/negative/unsafe/ABI/final, promotion, and held-out consume the same one-time A19e7 process-issued receipt and reopen raw outputs plus issuer/nonce. Keep the A19e8 parent open until this closes.

  **A19f anti-special-case and finite held-out acceptance**

  - [x] The production orchestration package and related agent configuration pass static scans for known project/function/path identities, including raw text, constant concatenation, bytes, SHA prefixes/full values, and hex/base64 encoding. Graph and Cargo outputs have rename-equivalence tests, and a project-neutral two-TU planning case is included.
  - [x] Add a fixed finite held-out contract tool: 5–20 nonduplicate projects, at least 12 construct families, and at least two projects excluded from rule development, with explicit repo/commit/tree/compile-DB bindings plus identity-dispatch and near-duplicate rejection. Real mode forbids case-reported translation evidence, derives the ledger from the plan, opens SQLite read-only, and revalidates the original repository tree, compile database, source/generated closure, AI provider evidence, every candidate gate, the immutable candidate set, and the project final bundle under resource bounds. Offline contract success cannot impersonate translation success.
  - [x] Establish a generic raw-C2Rust whole-project execution regression and run pinned FlashDB, cJSON, and libyaml through the same production path: 56 translation units and 39 translated executables in total, with native tests and migrated wrappers passing for all three projects. Production code does not inspect project/test/function identity; working directories and path normalization derive from the compilation database and C2Rust rules, while reports and raw streams are content-addressed and the public summary removes host absolute paths. All three projects participated in this stage's rule development, so the result is not held-out, AI-primary, or final semantic acceptance and cannot close the next item or the P0-A19 parent.
  - [ ] **A19f1: competition whole-project qualification matrix (machine-acceptance contract).** Keep the fixed matrix at no more than 20 `case_id` values, with the first set explicitly covering TinyCC, FlashDB, cJSON, libyaml, Lua 5.5, Expat, PostgreSQL, and the Linux kernel. A case is addressed jointly by project, upstream pin, declared whole-project build scope, and formal variant set. Run each pinned case exactly once as a formal qualification. If a failed case drives rule changes, permanently reclassify it as a development regression; any later run needs a new pin/`case_id` and still counts toward the 20-case cap. Never run 1,000/10,000 repetitions, add near-duplicate projects, or select the best result from repeated attempts.

    Before launch, every row must produce a hash-bound `qualification-case.json`, and an independent verifier must recompute Q1-Q11 below. A missing, drifting, unknown, or non-passing field can yield only `blocked`/`not-run`, never project pass, `semantic_gate=true`, or translator numerator. Every row must declare `required_gates=[Q1,...,Q11]`; no project may remove gates.

    | Gate | Required fields and machine decision |
    | --- | --- |
    | Q1 `source_pin` | Fix `upstream_repository`, a 40-hex `source_commit`, `source_tag_or_ref`, Git tree id, and recomputed `repository_tree_sha256`; the actual checkout must match HEAD, be tracked-clean, and reproduce the same content tree. Do not launch while any pin is absent from the formal case manifest. |
    | Q2 `competition_proof` | Bind the competition environment, toolchain, sandbox, and `proof_class=competition-exact`, then reopen the real OpenCode model probe, session, worker, and raw streams. Formal P0-A19 AI evidence must use `opencode + GLM-5.1 + c2rust-candidate + max`. DeepSeek may be labeled only `auxiliary`/`local-validation`; it cannot replace GLM-5.1, competition-exact proof, or final model evidence. |
    | Q3 `native_c_baseline` | Bind the original C configure/build argv, environment, exit states, raw output, and the upstream official or repository-declared test-suite version, commands, nonzero discovery count, and per-test results. A failed native build or declared suite cannot proceed as a translation success. |
    | Q4 `build_test_inventory` | Discover and hash-bind from project metadata every TU, same-source compile variant, generated source/header, compile/link command, library/export, target, feature/config, test runner, scenario, argv/stdin, fixture/data item, working directory, and timeout/resource setting. Unknown, omitted, or unprojectable entries fail closed. |
    | Q5 `candidate_repair_provenance` | Bind every typed-IR/C2Rust/AI candidate and its accepted/rejected state, provider/model, prompt/context, input/output, patch, repair/rollback, selection reason, and artifact SHA. The host must recompute `candidate_set_complete`; publishing only the winning candidate is insufficient. |
    | Q6 `rust_reconstruction` | RustProjectIR/Cargo must reconstruct the complete crate/module, bin/lib/example, variant, link/export, test-target, and scenario set declared by Q4, then build/check/test every target. Compiling only some TUs, one binary, wrappers, or slices is not whole-project reconstruction. |
    | Q7 `execution_completeness` | Original C and Rust target/test inventories map one-to-one, with `silent_skip_count=0`, `zero_test_target_count=0`, and `required_test_omission_count=0`. Every skip needs a source, reason, and receipt; explicitly skipping a required test still blocks pass. |
    | Q8 `per_test_oracle_diff` | Every stable test/scenario id binds the C and Rust argv, stdin, fixtures, environment, working directory, resources, exit/signal, stdout/stderr, and schema-aware oracle diff. Finalization requires every mandatory per-test diff to pass. |
    | Q9 `no_original_c_residual` | Reopen actual linker traces and ELF/archive/import results to prove zero linked project-owned original C sources, objects, static libraries, and shared libraries. A required, explicitly declared native/FFI boundary may publish only `conversion_scope=partial`; it must not be named or marked `full conversion` and cannot close this matrix's whole-project pass. |
    | Q10 `resource_recovery` | Fix CPU, memory, wall-time, process-tree, disk, network, provider-call, and token limits; bind over-limit classification, checkpoints, crash/timeout recovery, deduplicated calls, and final measured use. Work exceeding competition resources is `blocked`; it cannot shrink scope and be rewritten as pass. |
    | Q11 `project_final` | The sole CompletionCoordinator must issue a `project-final` receipt reopened by an independent verifier and binding Q1-Q10, the complete cohort/generation, every project gate, empty blockers, `semantic_gate=true`, and the final invariant audit. Plan, index, compile, or a single Cargo gate has no completion authority. |

    | case_id | Project and fixed upstream | Held-out eligibility | Whole-project qualification boundary and current state |
    | --- | --- | --- | --- |
    | `wpq-tinycc` | TinyCC; `https://repo.or.cz/tinycc.git`; known development pin `mob@d9d02c56401e43be43760b63f7d82f771a7ed1f6`, with the formal tag/ref/tree receipt still to be fixed by Q1 | `development-regression`; already used for multi-variant/closure hardening and never held-out | `blocked`: the current report stops before Cargo/AI on same-source multi-configuration and fatal C2Rust diagnostics. It must requalify under Q1-Q11; a passing native suite or planner inventory is not translation success. |
    | `wpq-flashdb` | FlashDB; `https://gitcode.com/xwxf/FlashDB.git`; known competition pin `competition@f9d0421315c564fb890a1b14eee77b290e0d7bbe`, with the formal tree receipt still to be fixed by Q1 | `development-regression`; already used in rule development and never held-out | `blocked`: existing WSL/local raw baselines and function-level evidence are not a competition-exact whole-project `project-final`. Complete targets/tests, real GLM-5.1 provider evidence, per-test oracles, and Q1-Q11 remain required. |
    | `wpq-cjson` | cJSON; `https://github.com/DaveGamble/cJSON.git`; formal commit/tag/tree not yet written to the case manifest | `development-regression`; already used in rule development and never held-out | `not-run`: the existing WSL C2Rust regression is local regression evidence only and cannot replace formal AI, complete test projection, no-C-residual proof, or `project-final`. Do not launch before Q1 is fixed. |
    | `wpq-libyaml` | libyaml; `https://github.com/yaml/libyaml.git`; formal commit/tag/tree not yet written to the case manifest | `development-regression`; already used in rule development and never held-out | `not-run`: the existing WSL C2Rust regression is not whole-project qualification; all Q1-Q11 formal evidence remains open. |
    | `wpq-lua-5-5` | Lua 5.5; `https://github.com/lua/lua.git`; formal 5.5 commit/tag/tree not yet written to the case manifest | `development-regression`; already drove `VaList`/variant/link repairs and never held-out | `blocked`: manually choosing `LUA_USE_JUMPTABLE=0`, adding link flags, or restoring stack settings is not production proof. Project metadata must project those facts and Q1-Q11 must pass. |
    | `wpq-expat` | Expat; `https://github.com/libexpat/libexpat.git`; development ref `R_2_8_2` is known, but formal commit/tree are not yet written to the case manifest | `development-regression`; the initial probe already drove working-directory, variant/link-closure, and target-domain disambiguation repairs and is never held-out | `blocked`: all 36 compile entries, 29 unique C sources, seven same-source two-variant groups, and seven link targets now enter verified BuildIR/AI ContextPack. The competition-profile plan contains 489 function nodes and 407 SCCs with zero cross-target false ambiguity and 155 classified parser boundaries. The schema-v3 development regression executed 36/36 real C2Rust processes and produced 36/36 isolated snapshots; the export reopen gate recomputed 380 unit-level and 764 target-occurrence exports with zero conflicts. The latest plan also binds all 407 SCCs to target scopes recomputable from 36 BuildIR units across six terminal targets; scope-set SHA-256 is `d0aa78cf98106490f1c1eccb35b825cea8d3919238391126d33d6b18435d78e8`. RustProjectIR v3, multi-target Cargo, AI repair, one-to-one test inventory, and Q1-Q11 remain incomplete, so this is not a whole-project pass. Neither the test-only single-variant run nor any of these development regressions may be relabeled held-out or formal qualification. |
    | `wpq-postgresql` | PostgreSQL; `https://git.postgresql.org/git/postgresql.git`; formal commit/tag/tree not yet written to the case manifest | `reserved-held-out`; it counts toward the required two genuinely held-out projects only after a provenance ledger proves `rule_development_used=false` | `not-run`: accept only the repository-wide scope at the fixed pin. Planning, indexing, translating a few directories/TUs, or compiling only some targets is never complete translation success. If the full scope exceeds Q10 competition resources, the result is `blocked`; it must not shrink to a slice/partial scope and be relabeled pass. |
    | `wpq-linux-kernel` | Linux kernel; `https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git`; formal commit/tag/tree and build config not yet written to the case manifest | `reserved-held-out`; it counts toward the required two genuinely held-out projects only after a provenance ledger proves `rule_development_used=false` | `not-run`: accept only the repository-wide scope at the fixed pin/config. Kconfig/BuildIR planning, indexing, translating a subsystem/TUs, building one image/tool, or running some selftests is never complete translation success. If the full scope exceeds Q10 competition resources, it must be `blocked`, never passed after reducing scope. |

    Before the formal matrix starts, at least two projects must have `held_out=true`, `rule_development_used=false`, and reopenable development provenance. PostgreSQL and the Linux kernel are currently reserved, but a reserved label is not held-out proof. Every later TinyCC, FlashDB, cJSON, libyaml, Lua 5.5, or Expat result belongs only in the development-regression column. If the first formal PostgreSQL or Linux-kernel case drives a rule change, that project also loses held-out eligibility for subsequent runs, and the no-more-than-20 matrix must add a genuinely untouched replacement project.

  The existing 12 P0-A10 cases are a function/fragment input and exact-path regression baseline. A19f covers unknown-repository BuildIR, migration DAGs, Cargo reconstruction, and project-level semantic acceptance. They share anti-special-case rules, but a pass in either scope cannot close the other.

  **Execution order, driven by completion conditions rather than test identity:**

  1. A19d3d2a2g1 is closed: the migration graph, portfolio DAG, integration manifest, and RustProjectIR reopener now share one SCC/target scope that is recomputable from BuildIR. Legacy manifests may use only the v2 compatibility path and cannot impersonate v3.
  2. A19d3d2a2g2-g3 are closed. Now close g4's independent reopen gate. Cargo metadata, actual compilation, and linker/ELF/archive witnesses must agree exactly with RustProjectIR v3 before structural reconstruction is complete.
  3. A19d3d2a2h1-h3 and h4a-h4d close CMake/CTest, bounded Make `test`, content-bound Automake `check`, bounded Meson direct-executable inventory, bounded single-file Make stdin, the dual-sandbox oracle, crash-consistent completion evidence, and the AI repair loop. Now finish h4/A19d3d2a for arbitrary targets, recursive Make, Kconfig, custom runners, remaining stdin sources, fixtures/resources, and bidirectional inventory equality. Formal qualification cannot start before zero silent skips and zero zero-test targets are proved.
  4. Complete the closure upgrade, global AI planner, project verifiers, and CompletionCoordinator in dependency order. A19a7c-A19b4 closes and reopens all toolchain/generated/native-link/CIndex/frontier facts. A19d3c2b4 and A19c4a-A19c4e connect real OpenCode/GLM-5.1 global planning, multiple candidates, and project repair. A19e4e, A19e7, and A19e8 execute project verifiers through independent processes and a non-degrading sandbox. Only the unique A19e5-A19e6 CompletionCoordinator may create the full `project-final` cohort/generation, rerun every gate, and issue a recoverable final receipt. No low-level CLI, model JSON, or partial gate may complete a project early.
  5. Finally freeze the no-more-than-20-case A19f1 formal matrix and run Q1-Q11 row by row on the competition-exact host, exactly once per pinned case. At least two genuinely rule-development-independent held-out projects must retain that eligibility. Record `passed`/`blocked` for every row without selective reruns, then apply the finite A19g Windows/WSL and delivery checks. Never run 1,000/10,000 rounds.

  **Current success-rate and stability closure board (2026-07-16):**

  - The active development slices are A19d3d2a2g4 and A19d3d2a2h4. The former closes independent v3 Cargo metadata, actual-target, and linker-witness reopening. The latter now includes CTest, bounded Make `test`, content-bound Automake `check`, bounded Meson direct-executable inventory, and bounded single-file Make stdin; arbitrary targets, recursive Make, custom runners, remaining stdin sources, fixture, resource, and completeness proof remain. Current inventory and dual-sandbox results are generic gate capabilities, not an eight-project formal pass claim.
  - Success rate uses a fixed discovered denominator: `fully verified target/test count / every target/test discovered from BuildIR and project-owned test metadata`. Skips, repository reduction, selecting only compilable translation units, zero-test results, compile-only substitutes for runtime, or model self-reports remain failures and cannot be removed from the denominator.
  - The AI-first order is fixed: BuildIR/CIndex/target-scope ContextPack -> project migration DAG -> isolated multi-worker candidates -> structured Cargo/C-oracle diagnostics -> bounded project repair -> complete re-verification. C2Rust/typed lowering supplies candidates and evidence; it is not an independent route around AI or validation.
  - Implement a complete capability slice before running its focused gate, one Windows full suite, and one WSL competition baseline. Do not rerun the full suite after every small edit and never run 1,000/10,000 rounds. Two identical no-information failures under the same input and failure SHA suppress the third model launch and preserve the blocker.
  - Stability outranks cosmetic pass rate. Every failure must become an identity-neutral, content-bound, reopenable capability blocker, and every generic repair needs negative, drift, and cross-target-namespace regressions. A project is `passed` only when Q1-Q11 all pass; Cargo success, the original C suite, or partial Rust scenarios alone still leave it `blocked`.

  **A19g staged-delivery rule (repeat for every independent phase):** after a reviewable phase, run one finite Windows/WSL gate and `git diff --check`; verify that credentials, host caches, `target/` runtime artifacts, and test-identity dispatch are absent from the commit. Then create one scoped commit, push the current branch, and verify that local `HEAD` exactly equals the remote branch SHA. A phase that is untested, uncommitted, unpushed, or not remote-verified cannot be checked off.

  **A19h code reduction and module boundaries**

  - [ ] Remove duplicated implementations, obsolete compatibility layers, and unreachable entrypoints from the P0-A19 production path only after call-graph inspection, static scans, and finite regressions prove them unused. Reduction must not remove fail-closed validation, evidence bindings, refusal paths, or code still referenced by CLI, tests, or release entrypoints.
  - [x] P0-A19 `_project_migration_harness` production modules, `test_project_migration_*.py`, and shared test support are split by responsibility to at most 300 lines each and continuously enforced by the source-layout gate. Future edits in that directory retain the same limit. P0-C4 tracks the repository's remaining Python large files; compressed formatting, duplicate thin wrappers, or moving logic into unchecked directories cannot evade the gate.
  - [x] A19h1: remove dispatch's dedicated whole-file bytes collector in favor of the shared streaming artifact verifier and canonical JSON metadata; split Ninja link-command regressions out of the generated-closure test monolith into a dedicated 36-line module. Every production and test file touched in this stage remains at or below 300 lines, with no project-identity adapter added.
  - [x] A19h2: split page binding, plan-time closure proof, and catalog staging/publication out of the old `context_index_store.py`, and remove the unused materialization helper. Artifact writes, content hashes, and ledger portfolio binding now share streaming canonical metadata. Every production/test Python file touched in this stage remains at or below 300 lines, and the production path adds no project, function, directory, or fixture identity dispatch.

  Completion requires one entrypoint to take a held-out repository from zero to a translation inventory, migration DAG, hash-bound ContextPacks, candidate/repair evidence, and reproducible Cargo output. At least one multi-file C project that did not participate in development must pass project-level build and semantic gates within the declared boundary. Identity dispatch, handwritten project adapters, unbound model output, or function-only demos cannot close this item or increase the translator numerator.

- [x] **P0-A1: OpenCode no-progress retry suppression**

  Hash effective request/source/spec/repair-trace/launch-policy inputs as `effective_input_sha256`, and hash structured root cause/status/returncode/diagnostics as `failure_sha256`. After two identical deterministic failures with no current input change, close the repair hint before a third launch, clear its retry command, record `repair_retry_suppressed`, and fail closed as `refused/retry_input_unchanged`. Do not invoke the runner, append a synthetic attempt, or raise semantic status. Preserve retries for timeout, SQLite/OpenCode locks, preflight, credentials, contracts, missing environment, unknown causes, and missing or drifting hashes.

  This gate reduces OpenCode calls and token use that have no information gain. It does not treat AI output as semantic fact or replace the C oracle, Rust replay, or strict validator.

- [x] **P0-A2: align bare `CLANG_PATH` command names with the competition PATH contract**

  The Rust clang frontend now accepts both an existing explicit path and a bare command name resolved by the process `PATH`, such as `CLANG_PATH=clang`; an explicit path containing separators still fails closed when it does not exist. The WSL clang 18 minimum-TU smoke and three real-clang auto-migrate positive/negative tests pass. This restores candidate generation only and does not increase the semantic count.

- [x] **P0-A3: deterministic-first admission gate**

  `mode=auto` selects deterministic execution before OpenCode preflight only when every worker binds accepted evidence, an existing evidence root, a source hash, and a slice spec with no repair policy. Mixed or unbound input is refused before fanout as `auto_route_unbound`; `competition-exact`, hostless rehearsal, and explicit OpenCode attestation cannot auto-downgrade. This route only avoids model calls with no expected information gain and remains `semantic_gate=false`.

  This is historical behavior. After P0-A9, `mode=auto` becomes AI-primary for new translation work; only pure accepted-evidence revalidation retains the deterministic shortcut.

- [x] **P0-A4: compact the OpenCode worker context while preserving the exact contract**

  `.opencode/agents/c2rust-migrator.md` was reduced from 5408 bytes to 1439 bytes while preserving `opencode + GLM-5.1 + c2rust-migrator + max`, Required Preflight, the non-gating Superpowers boundary, the first-and-only exact Command line tool call, prohibitions on exploration/editing/subagents/substitute commands, hash-bound handoff/session/contract verification, and the non-semantic chat boundary. The bundle manifest and profile contract tests were updated. This only reduces model context and ambiguity; it remains `semantic_gate=false`.

- [x] **P0-A5: keep one command representation in OpenCode prompts**

  Worker and preflight prompts remove the duplicate `Command: <JSON argv>` text and retain one executable `Command line:`. Structured argv, command hashes, sessions, and handoff evidence remain unchanged. Representative prompts are 13.2%-16.1% smaller and the full harness suite passes. This only reduces token use and command ambiguity; it remains `semantic_gate=false`.

### 3.4 P0-B: Competition Host and OpenCode

- [ ] **P0-H9: exact OpenCode + GLM-5.1 competition contract revalidation**

  WSL currently resolves provider-qualified `zai/glm-5.1`, but the complete preflight/worker marker chain is not closed on the real competition host. OpenCode + GLM-5.1 is now the default competition translation path. When it is unavailable locally, record blocked/unavailable explicitly; deterministic results must not impersonate the AI competition lane.

  Completion requires `COMPETITION_EXACT_HOST=1` and an exact GLM-5.1 token from `opencode models`. P0-A19 project preflight/workers use `opencode` + `GLM-5.1` + `c2rust-candidate` + `max` and must produce complete hash-bound probe/session/worker artifacts. If the retained slice-level `opencode_agent_harness` command-execution lane ships in the release packet, its separate `c2rust-migrator` + `max` contract must also be revalidated. The judge bundle and public packet must pass again.

  WSL, local, and CI results remain `wsl-local-simulation`, `local-simulation`, and `ci-approximation`; none closes H9.

- [ ] **P0-H10: publish the CRC32 competition-exact before/after**

  Depends on P0-H9. Close only after replaying the completed CRC32 C2Rust+repair before/after on the real competition host and publishing hash-bound workflow metrics.

### 3.5 P0-C: Stage Closure

- [ ] **P0-C1: historical evidence drift**. `20260711T-finite-p0-t31` is an immutable commit-bound historical result; never rewrite its hash-bound artifacts in place. Produce a new replacement run by artifact owner, recompute validator/ledger state, and mark the old run superseded explicitly. Close only when the replacement run passes 33/33 strict checks, in commits separate from translator behavior changes.
- [ ] **P0-C2: all-feature Clippy**. Stop carrying a fixed "eight remaining" count that drifts with HEAD. Run the repository-supported `--all-features --all-targets` command for each actual Cargo manifest and produce an inventory bound to HEAD, toolchain, argv, and raw output before resolving warning classes. New slices add no warnings. Because the repository root is not a Cargo workspace, a fabricated root-level `cargo clippy --workspace` is not evidence.
- [x] **P0-C3: first-party Rust large-file split closure**. Every Git-tracked first-party `.rs` file under `crates/c2r-translator`, `flashDB_rust`, and `validation/l2_slices` is at most 400 lines after formatting. Splits occur only at complete item/test boundaries, the single oversized report function was extracted into a dedicated helper, and the test-source self-check recursively covers split leaves. The only exceptions are the 733/704-line hash-bound historical C2Rust evidence snapshots under `validation/evidence/flashdb/auto-translation/real-fdb-calc-crc32/`; evidence content must not be rewritten merely to satisfy the line limit.
- [ ] **P0-C4: split first-party Python large files by responsibility**.
  - [x] New P0-A19 `_project_migration_harness` production modules, `test_project_migration_*.py`, and shared test-support files are each at most 300 lines. This batch splits controller, gate-authority, and sandbox tests plus provider runtime/session/process/readiness responsibilities, with a source-layout test enforcing the boundary.
  - [x] The AI-exact entrypoint is split into a 236-line compatibility facade and six responsibility modules of at most 189 lines, with a dedicated source-layout test fixing public imports and split boundaries.
  - [x] The AI candidate provider is split into a 61-line compatibility facade plus command, generation, cache, and manifest modules. The former 1,282-line candidate-harness test is split into a 225-line main file and test/support modules of at most 291 lines; all 25 original tests remain unique, and a source-layout gate rejects dynamic loading or renewed growth.
  - [x] AI candidate-cache tests are split by pure cache behavior, provider lifecycle, and concurrent execution into modules of at most 239 lines. The stale fixture now provides a real source-bound external-callee context without weakening production readiness. All nine original cache tests remain unique; with two layout gates, 11 pass, and the complete `test_ai_candidate*.py` group passes 72/72.
  - [x] The BuildIR adapter boundary adds 126/52-line generic facades; its AST import-graph gate is split into a 279-line support module and a 124-line test. Cross-adapter fixture support/tests are 282/298 lines, and the order-preservation test is 173 lines, all within the 300-line gate. The split does not expose private Make shapes, dynamic loaders, or the test-only common contract to production downstream code.
  - [ ] Build a Git-tracked line-count inventory for the remaining active `validation/tools` production modules and tests and reduce them to at most 300 lines module by module, prioritizing AI candidate/context/repair/provider and finite-suite paths. Splits must preserve public imports and test discovery. Hash-bound evidence, generated snapshots, and canonical docs are not rewritten merely to satisfy a line gate.

  Finite phase gate on 2026-07-14: AI candidate 72/72, host-verifier/provider 19/19, and document mirror 4/4; the project-migration suite passed 450/450 on Windows with two platform skips and 450/450 on WSL with no skips. `git diff --check` passed, and a credential-fragment scan of Git-tracked files found zero matches. These results prove only that the current contracts and regressions agree across both local environments; they are not competition-host exact proof and do not close the A19f held-out whole-project build/oracle acceptance.

  Finite A19a7b2b gate on 2026-07-14: the merged focused regression passed 44/44; the project-migration suite passed 462/462 on Windows with two platform skips, the final host-neutral boundary gate passed 6/6 on both Windows and WSL, and the full WSL suite passed 462/462 with no skips. The first WSL run accurately exposed that a Windows-created worktree `.git` path was not portable; direct repository-source enumeration fixed the dependency and the suite passed on rerun without skipping the gate. This remains local contract evidence, not a model run, held-out build/oracle acceptance, or competition-host exact proof.

## 4. Later Backlog

### P1: Expand Generic Translation

1. Expand typed IR according to P0-A10 cross-project failure frequency instead of one-project source order; start with a complete alias/noalias, pointer-provenance, and escape model.
2. Preserve integer promotion, usual arithmetic conversion, narrowing, array/function decay, and ABI-related conversions explicitly in IR.
3. Model compound side effects as composable rules for sequence points, evaluation order, increment/decrement, dereference, index, member, and call.
4. Expand CFG support: classify and evidence `switch`/`goto` fail-closed behavior before adding a relooper or structured lowering.
5. Expand compound literals, designated initializers, function pointers, variadics, unions, bitfields, VLAs, and flexible array members.
6. Model volatile, hardware registers, RTOS/interrupts, filesystems, and power-loss boundaries; host fixtures do not replace target evidence.
7. Add real slices from different construct families across FlashDB, zlib-ng, libuv, and other projects; do not inflate coverage with many similar checksum/parser slices.

### P2: Agents, Routing, and Release

1. Maintain model/prompt upgrade and rollback policies plus offline replay; P0-A10 establishes the golden-set fact source.
2. Use a second model only for independent candidates or audit; majority voting must not replace semantic gates.
3. Publish human intervention, duration, token/evidence cost, and model-upgrade before/after differences.
4. Complete a formal tag/release, external review, CONTRIBUTING guidance, ownership, and release checklist.
5. Keep CFG/SSA/MIR/LLVM/self-hosting as long-term research until P0/P1 are stable.

## 5. Completed Milestones

| Range | Result | Semantic count change |
| --- | --- | ---: |
| P0-T0..T3 | Ordered inc/dec, for-init comma, no-clang AST replay, and do-while assignment-call candidate foundations | Not counted |
| P0-T4..T9 | `tsl_to_blob`, `fdb_is_str`, do-while continue, and if assignment-call named-slice acceptance | 21 -> 24 |
| P0-T10..T13 | Generic record/call/alias expansion and the exact `fdb_kvdb.c:1870` fragment | 24 -> 25 |
| P0-T14..T17 | Iterator tail call, traversed length, KV reset, and sector start | 25 -> 29 |
| P0-T18 | Safe interior reborrow projection for `kv = &itr->curr_kv` | 29 -> 30 |
| P0-T19 | `:1871-:1873` reset/add/current-level continue | 30 -> 31 |
| P0-T20 | Compose the `:1870-:1873` assignment-call condition and branch body | 31 -> 32 |
| P0-T21 | Compose the `:1868-:1874` zero-start and assignment-call branches | 32 -> 33 |
| P0-T22 | Select and implement the `:1885` interior-reborrow do-while tail candidate | 33 -> 33 (candidate only) |
| P0-T23 | Strict source-backed acceptance for the `:1885` owner-interior-alias do-while tail | 33 -> 34 |
| P0-T24 | Select and implement the `:1876` discarded direct-call body candidate | 34 -> 34 (candidate only) |
| P0-T25 | Strict source-backed acceptance for the `:1876` discarded direct-call body | 34 -> 35 |
| P0-T26 | Select and validate the `:1880` u32 mutable record-pointer postfix increment candidate | 35 -> 35 (candidate only) |
| P0-T27 | Strict source-backed acceptance for the `:1880` u32 mutable record-pointer postfix increment | 35 -> 36 |
| P0-T28 | Select and validate the `:1881` owner-interior u32-to-LP64-usize sibling accumulation candidate | 36 -> 36 (candidate only) |
| P0-T29 | Strict source-backed acceptance for the `:1881` owner-interior sibling accumulation | 36 -> 37 |
| P0-T30 | Select and validate the `:1880-1883` bounded ordered stats sequence candidate | 37 -> 37 (candidate only) |
| P0-T31 | Strict source-backed acceptance for the `:1880-1883` ordered stats sequence | 37 -> 38 |

Validation run bindings:

| Validation | Binding | Result |
| --- | --- | --- |
| P0-T31 strict validator | WSL competition clang lane, 2026-07-11 | `schema_status=passed`, `semantic_pass=true`, `generated_draft_semantic_pass=true`; all 12 semantic-binding checks passed; only four finite fixtures ran, with no stress loop or repeated rounds |
| P0-T29 strict validator | WSL competition clang lane, 2026-07-11 | `schema_status=passed`, `semantic_pass=true`, `generated_draft_semantic_pass=true`; all 12 semantic-binding checks passed; only three bounded fixtures ran, with no 1,000/10,000-round or other loop stress test |
| P0-T27 strict validator | WSL competition clang lane, 2026-07-11 | `semantic_pass=true`, `generated_draft_semantic_pass=true`; all 12 semantic-binding checks passed; only three bounded fixtures ran and no loop stress was run |
| P0-T25 strict validator | WSL competition clang lane, 2026-07-11 | `semantic_pass=true`, `generated_draft_semantic_pass=true`; all 12 semantic-binding checks passed; three bounded fixtures cover 1/2/3 ordered body/tail calls; no loop stress was run |
| P0-T24 / P0-A1 / P0-A2 stage validation | WSL local simulation, 2026-07-11 | translator library `228`, bounded `665`, and integer conversion `4` passed, with `133` real-clang opt-in tests ignored by default; three real-clang focused tests, Python auto-migrate `155`, and OpenCode harness `192` passed; no loop stress was run |
| P0-T23 strict validator | WSL competition clang lane, 2026-07-11 | `semantic_pass=true`, `generated_draft_semantic_pass=true`; all 12 semantic-binding checks passed; three bounded fixtures cover 1/2/3 calls |
| P0-T22 translator candidate | current worktree, 2026-07-11 | library `228` passed; bounded `659` passed with `133` real-clang opt-in ignores; integer conversion `4` passed; coverage matrix passed |
| P0-T21 translator candidate | commit `02067028`, 2026-07-11 | library `228` passed; bounded `657` passed with `133` real-clang opt-in ignores; integer conversion `4` passed |
| P0-T20 Python core historical snapshot | 2026-07-11 stage snapshot | `293 passed, 6 skipped`, plus `90` subtests; proves only that revision |
| Full regression | run `20260711T-finite-p0-t31` | 25 of 33 passed; P0-T31 added no failure and the same eight historical evidence drifts failed; `stress_loops=0`, `run_stress=false` |
| P0-T21 strict validator | evidence generated from commit `8a261787` | `semantic_pass=true`, `generated_draft_semantic_pass=true`, and all 12 semantic binding classes passed |
| P0-T20 strict validator | P0-T20 evidence | `semantic_pass=true` and `generated_draft_semantic_pass=true` |
| Accepted-evidence strict status | `libuv/ip4-addr` | verified-unsafe-baseline SHA drift; the ledger count is not a current strict pass |
| All-feature Clippy | Commit `81a772d1` is a historical cleanup point | Current HEAD still lacks a manifest/toolchain/argv/raw-output-bound inventory. P0-C2 stays open and does not reuse a historical fixed warning count |

## 6. Architecture Boundaries

### 6.1 Candidate Routes

| Level | Purpose | Can it claim semantic success alone? |
| --- | --- | --- |
| L0 ContextPack | Bind real source, compile context, diagnostics, and candidate baselines | No |
| L1 AI primary | OpenCode + GLM-5.1 candidate generation or repair | No |
| L2 deterministic alternates | typed IR, raw C2Rust, and C2Rust+repair candidates | No |
| L3 common verification | Compile, oracle/replay, diff/negative, and unsafe/ABI gates | Only after all pass |
| L4 refuse | Fail closed with an actionable next step | Not applicable |

A candidate becomes a semantic pass only after the shared gates in section 7 prove the declared slice boundary.

### 6.2 IR Layers

1. **Semantic IR**: C integer width/signedness, casts, lvalue/rvalue, pointer provenance, volatile/atomic, and undefined/implementation-defined boundaries.
2. **Control IR**: blocks, branches, loops, break/continue/return, and future CFG/relooper data.
3. **Typed Value IR**: scalar, record, array, pointer, function type, qualifiers, and ABI/layout provenance.
4. **Lowering**: Rust ownership, borrows, slices, wrapping/checked arithmetic, raw pointers, or refusal. IR must not invent Rust safety.

## 7. Acceptance Gates

A named slice may increase the translator semantic numerator only when all of these hold:

1. Real source span, commit, fixture, carrier, and generated-candidate hashes agree.
2. The C oracle and Rust replay both compile and execute.
3. Schema-aware diff passes and at least one discriminating negative mutation executes and is detected.
4. Unsafe scan/ledger, route decision, validation profile, and final verification are cross-bound.
5. The independent validator passes with `--require-semantic-pass`.
6. The coverage matrix derives the updated count; manual percentages are not evidence.

Native C builds, rustc compile-only, typed-IR unit tests, no-clang fixtures, C2Rust output, LLM text, route metadata, zero unsafe findings, or agent prose cannot increase the numerator alone.

Proof classes are fixed:

| Proof class | Allowed source | Can close competition-exact work? |
| --- | --- | --- |
| `local-simulation` | Windows/local host | No |
| `wsl-local-simulation` | WSL | No |
| `ci-approximation` | Linux CI | No |
| `competition-exact` | Real competition host with explicit attestation | Yes |

## 8. Development Principles

1. FlashDB is a real test case, not a translator-specialization source. Project names, function names, paths, and fixed fixture values must not select translation behavior.
2. New competition translation work defaults to AI-first. typed IR and C2Rust provide context, alternate candidates, and verifiable fallback; a hand-written lowering rule is not a prerequisite for invoking AI on an unfamiliar construct.
3. Start with the smallest source-backed slice and expand adjacent structure. Every relaxed rule needs a positive and nearest fail-closed negative.
4. The C oracle is ground truth only within its declared fixture, compiler, flags, ABI, and observable contract.
5. Fail-closed results must provide the source span, refusal reason, and next smallest implementation step; refusal volume is not success.
6. Unsafe counts are governance metrics, not complete proof for FFI, concurrency, volatile, ABI, or hardware semantics.
7. Evidence uses repo-relative paths, stable hashes, and explicit retention rules. Never publish secrets or host absolute paths.
8. Split large files by module responsibility. Add tests, schemas, evidence, and docs only when they produce a behavioral or acceptance benefit.
9. AI input is limited to source, compile context, type/API contracts, and failure classification. Candidate and repair must not receive fixture expected/actual values, oracle output, first mismatch, or replay source carrying those values. Validators may read complete hash-bound evidence but may not feed validation answers back to the model.

## 9. Common Validation Commands

```bash
cargo fmt --all --check
cargo test --manifest-path crates/c2r-translator/Cargo.toml --all-features
python3 -B validation/tools/translator_coverage_matrix.py --matrix validation/translator-coverage-matrix.json
python3 -B -m unittest validation.tools.test_doc_mirror_contract
python3 -B -m unittest validation.tools.test_validate_test_translation_coverage
git diff --check
```

Competition config and judge-chain changes also require:

```bash
python3 -B -m validation.tools.resync_sha_bindings --scan-root config/competition-env --dry-run --check
python3 -B -m validation.tools.resync_sha_bindings --scope judge-chain --dry-run --check
```

## 10. Document Maintenance Rules

- Keep only current status, the unique active queue, tiered backlog, milestone summary, and stable rules here.
- When an item closes, update the status table, active queue, milestone table, and validation summary instead of appending a daily log.
- Store exact artifacts, hashes, cases, and validator results in the coverage matrix/evidence; keep only conclusions and boundaries here.
- The Chinese file is canonical and the English mirror must change in the same patch.
- Use Git history for older states. Do not create a second global TODO, handoff, or roadmap.
